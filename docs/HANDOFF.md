# OpenObject: Build & Handoff Specification

> **Document type:** Implementation spec for Claude Code.
> **Status:** Running on real hardware. The web app (Phase 1) plus the Debian and Chromium-kiosk installer (Phase 2B) are built and verified on an actual XXL frame, with over-the-air self-update working. What's left is later milestones (single-file release image, Wi-Fi onboarding AP, real restart/shutdown). See §20 for the decision log.
> **Local project root:** `~/Code/OpenObject`
> **GitHub repo:** `queueue-studios/openobject`, **public** since 2026-06-14 (created private 2026-06-11). Claude drives all git operations on Matt's approval.
> **Website:** https://openobject.io

---

## 1. What OpenObject is

OpenObject is replacement software for the **Infinite Objects XXL** digital art frame, a 26" **square (1:1)** display. The frame originally ran the "White Walls" software (an Android kiosk app that rendered on-chain NFT art). That original setup is no longer reliable: the backend services it relied on have wound down.

The XXL's display is driven by a small **fanless mini PC mounted on the back of the panel**. OpenObject wipes that mini PC and turns it into a clean, self-hosted local art frame: it displays local image and video files, is controlled entirely from a web page the owner opens in any browser, and depends on no external service.

**Two design commitments shape everything:**

1. **Self-contained on the player.** The mini PC is the always-on brain. The owner's Mac/phone are just places files come *from* and a browser to control it; the frame never depends on them to keep running.
2. **Revivable by the next owner.** This is meant to be a shareable kit so *other* stranded XXL owners can revive their units. Design decisions favor "anyone can follow the guide" over "convenient for the original builder only."

---

## 2. Hardware target

The mini PC is a **MeLE Quieter 3Q** (confirmed from the unit's label; the BIOS reports board name **`XXL`**, an Infinite Objects custom badge on the same N5105 platform).

| Attribute | Value |
|---|---|
| Model | MeLE Quieter 3Q (BIOS board name `XXL`) |
| CPU | Intel Celeron **N5105** (Jasper Lake / "JasperLake ULX"), 2.00 GHz, **x86-64**, _bench-confirmed 2026-06-13; earlier "N5100" was wrong_ |
| RAM | 8 GB LPDDR4x _(typical config; not yet confirmed at bench)_ |
| Storage | **~128 GB eMMC**, confirmed from Linux as **116.5 GiB** at `/dev/mmcblk0` (2026-06-13): EFI `p1` 512 MB + ext4 `p2` 116 GB, plus 4 MB `mmcblk0boot0/1`. UEFI device id `A3V012`. _(The earlier "~256 GB" came from the Android storage screen and was wrong, `lsblk` is authoritative; the original "128 GB" was right.)_ |
| Expansion | microSD slot (pressure valve for larger libraries) |
| Networking | Onboard Wi-Fi, Intel Jasper Lake **CNVi** `[8086:4df0]` (`iwlwifi`) + **Gigabit Ethernet** Realtek RTL8111/8168 `[10ec:0123]` (`r8169`), _both confirmed 2026-06-14_ |
| Display panel | 26" **square (1:1)**, **1920×1920** native, over HDMI (Intel JasperLake UHD / `i915`; the iGPU also advertises 1920×1080 fallback modes), _confirmed 2026-06-14_ |
| Power | 12 V / 2 A barrel jack |
| FCC TX ID | PD99560D2 _(use to confirm Wi-Fi module / Linux driver at bench)_ |
| Serial | 8ICH4F310P530467 |
| MAC | 00CE39D53121 |

**Ports on the outward-facing edge:** 1× USB-A 3.0, 3.5 mm audio, microSD, 2× HDMI, 1× USB-C (DP-capable), 1× Gigabit Ethernet, barrel power.

**Critical hardware facts for the build:**

- This is a **normal x86 PC with a UEFI BIOS**, not an Android appliance. Infinite Objects installed **Ubuntu Linux** and runs the original "White Walls" player as an **Android app inside Waydroid**, a **LineageOS Android 11** container (`lineage_waydroid_x86_64`, build `RQ3A.211001.001`) on the Linux host, which is *why the original UI looks like Android*. Underneath it boots like any PC and runs Linux from a USB stick. **Confirmed at bench (2026-06-13):** UEFI firmware (AMI Aptio `ML_JPL1 V1.0.0 x64`, core 2.22.1282), **`Del`** enters Setup, **no BIOS password**, **Secure Boot off**, boot mode **UEFI**; the factory boot entry is `ubuntu (eMMC A3V012)`.
- The mini PC connects to the 26" panel via a captive **HDMI** lead (video) plus a **USB-C** lead (power and/or control). **This cabling is not touched.** OpenObject is a software reflash; the player↔panel wiring stays exactly as the vendor built it. After Linux is installed, video outputs over the same HDMI the panel already uses.
- When wall-mounted, only **one USB-A port is realistically free**, and the physical power button is hard to reach. Both constraints are solved in §4 and §10.

---

## 3. Operating system

**Minimal Debian (stable), UEFI**, locked 2026-06-14 (see the build note at the end of this section). Selection criteria that drove it:

- Recent-enough kernel that the **onboard Wi-Fi works out of the box** (verify the module via FCC ID / `lspci`/`lsusb` at bench; keep a known-good USB Wi-Fi dongle as the only fallback, it would ride in the hub).
- Can run **Chromium in kiosk mode** as the display surface (see §6).
- Can run a small local web server for the control panel and the display page.
- mDNS via Avahi so the panel is reachable at `http://openobject.local`.
- Samba available **only** if the optional SMB pull-source is enabled (see §8); not required for the default web-upload flow.

The OS image should boot directly into the OpenObject display with no desktop, login, or visible Linux chrome. Before that, during POST, the panel briefly shows the **Infinite Objects OEM boot splash** baked into the mini PC's firmware (BIOS board name `XXL`); it sits a layer below our software, is not removable short of reflashing the UEFI, and is expected on every power-up (it is the same splash the Reboot button used to hang on, §20). OpenObject's own first screen is the **OPEN/OBJECT** wordmark on black, so that logo is always the firmware, never us, and seeing it does not mean the wipe failed.

> **Bench note (2026-06-13):** the factory unit already runs **Ubuntu** on this exact hardware (N5105 iGPU, the square HDMI panel, onboard Wi-Fi, the USB hub), so a Debian-based install is low-risk, graphics, panel, and networking are all known-good under mainstream Linux. The factory stack is heavier than ours (Ubuntu → Waydroid container → Android → White Walls) and boots slowly (~1 min to art); our native Node + Chromium-kiosk path should boot markedly faster.

**Built 2026-06-14 (Phase 2B), installer stack locked + built (Mac side):**
- **Base:** minimal **Debian stable** (UEFI; no desktop), installed by the standard Debian
  netinst, which also wipes the eMMC and joins Wi-Fi. Our `installer/install.sh` provisions
  OpenObject on top, idempotent, re-runnable.
- **Display surface:** **`cage`** (a Wayland *kiosk compositor*: one fullscreen app, cursor
  hidden, no blanking) running **Chromium `--kiosk`** at `http://localhost/display`. No X, no
  window manager, no display manager. (X11 + Openbox is the documented fallback if cage
  misbehaves on the N5105 iGPU, see `installer/README.md`.)
- **Services:** **systemd** runs the player and the kiosk (replacing `player/supervisor.js`,
  §15). **Avahi** advertises `openobject.local`. Wi-Fi on the installed frame is owned by
  **ifupdown** (what the Debian netinst sets up: `allow-hotplug` + `wpa_supplicant`);
  **NetworkManager** is installed for the future §11 setup-AP but does not manage Wi-Fi yet.
  Because the ifupdown bring-up runs once at boot and never retries, a small
  **`openobject-netcheck`** systemd timer re-ups Wi-Fi if a cold boot ever leaves the frame with
  no network (the rejoin race; see §20). Intel iGPU + VA-API drivers and `libavcodec-extra` are
  installed for hardware decode (WebM is always safe; the codec package widens MP4/H.264 support).
- The build tooling lives in `installer/` (`install.sh`, `systemd/`, `kiosk/`, `README.md`).

---

## 4. Access & reflash procedure (one-time bench/in-place session)

This is the procedure to convert a stock XXL to OpenObject. It is performed once. The unit can stay wall-mounted; the outward-facing port edge remains reachable.

> **⚠️ Caution, do NOT remove the two screws above the MeLE.** On the back of the XXL, the two screws on the bracket/plate directly **above** the MeLE mini PC retain captive nuts on the *inside* of the chassis. Loosening or removing them drops those nuts inside the unit, and recovering them requires a **near-full disassembly and reassembly** of the XXL. They are **not** part of this procedure, leave them alone.

**Access hardware** (the owner-supplied kit, exact models recorded in the Setup Guide, §16):
- **Left-angle** USB-A extension from the single free port out to a reachable spot, then a USB hub on the end (one port can't host both the installer stick and a keyboard). The **left** angle is deliberate: it routes the cable clear of the frame controller's **power socket**, which sits immediately beside that USB port and must stay reachable to seat the power cord. (Confirmed part: CableCreation **CC0516**, see §16.)
- USB keyboard (a keyboard+touchpad combo on one dongle is ideal).
- USB flash drive (≥16 GB) for the Linux/OpenObject installer.
- External USB drive (≥128 GB; small USB SSD ideal) for the pre-wipe backup image.

**Procedure:**

1. **Connect** the hub to the free USB-A port; attach keyboard + installer flash drive.
2. **Enter the BIOS** at power-on by tapping **`Del`** _(confirmed 2026-06-13)_. The IO splash uses **Quiet Boot**, so no "press a key" prompt appears, tap `Del` repeatedly from the instant you power on, during the black screen (that's POST). The panel then shows the BIOS screen over HDMI. _(On a Logitech K400, `Del` is a direct key; the F-keys are Fn-shifted.)_
3. **Set boot order / boot menu** to boot from the USB flash drive. _(With the stick inserted it appears as a new boot option; use a one-time boot menu, likely `F7`, or **Save & Exit → Boot Override**, or set Boot Option #1. Confirm the boot-menu key with the stick in hand.)_
4. **Auto power-on, nothing to set.** _(Confirmed 2026-06-13:)_ this BIOS exposes **no** "Auto Power On / Restore AC Power Loss / State After G3" toggle (checked `Chipset → PCH-IO Configuration`), and the unit already boots on its own when power is applied, auto-on is the firmware default. So it boots whenever it receives power, no button press, nothing to enable (see §10). _(If a future unit doesn't auto-boot, the only place an ODM might hide it is `Advanced → Customer Exclusive Functions`.)_
5. **Boot the live USB.**
6. **BACK UP FIRST, image the existing eMMC** to the external drive (full-disk image). This preserves the original **Ubuntu + Waydroid (White Walls)** install in case the owner ever wants it back *while the vendor's servers still exist*. This is the clean, ADB-free way to capture it. **Do this before any wipe.**
7. **Install OpenObject** to the eMMC. Two stages (full runbook: `installer/README.md`):
   **(a)** run the standard **Debian stable netinst**, wipe `/dev/mmcblk0`, choose a *minimal*
   system (untick the desktop; keep "standard system utilities"), set hostname `openobject`,
   join Wi-Fi. **(b)** seed the OpenObject checkout to `/opt/openobject` (from a `git bundle` on
   a USB stick) and run **`sudo bash /opt/openobject/installer/install.sh`**, it installs Node
   22, Chromium + cage, Avahi, NetworkManager, the systemd units, and reboots into the kiosk.
8. **Verify on the panel** before considering it done: the display comes up edge-to-edge at
   `/display`, `http://openobject.local` is reachable over Wi-Fi, an uploaded clip plays,
   **Restart** bounces the player, and an unplug/replug auto-boots back to the display.

> **Recorded at bench (2026-06-13):** BIOS-entry key = **`Del`**; there is **no** "auto power on" setting (auto-on is the firmware default, see step 4); eMMC = **~128 GB** (`/dev/mmcblk0`, **116.5 GiB** confirmed from Linux), UEFI id `A3V012`.

---

## 5. Architecture overview

```
┌───────────────────────── MeLE Quieter 3Q (the "player") ─────────────────────────┐
│                                                                                   │
│   Web control panel  ──►  Library (local storage)  ──►  Display (Chromium kiosk)  │
│        ▲                        ▲                              │                   │
│        │                        │                              ▼                   │
│   Browser on Mac/phone     Source layer                  26" square panel (HDMI)   │
│   (http://openobject.local) ├─ web upload (default)                                │
│                             ├─ pull from share (optional)                          │
│                             └─ on-chain NFT (future seam)                          │
└───────────────────────────────────────────────────────────────────────────────────┘
```

The display, control panel, and display-page are all **web-based and served by the player itself**. Files arrive through the **pluggable source layer**, land in the **local library**, and are rendered by **Chromium in kiosk mode**.

---

## 6. Display / render layer

**Render the display through Chromium in kiosk mode**, pointed at a local page that cycles the rotation. This is the deliberate architectural choice and it drives several requirements below.

**Why the browser engine:** the owner's real-world library is a polyglot of formats, AVIF (incl. animated), animated WebP, GIF, PNG transparency, SVG vector art (incl. SMIL/CSS animation), plus MP4/MOV/WebM. A browser engine decodes all of these natively with one renderer, and `fit`/`fill` plus video looping fall out of standard CSS/HTML. A native image viewer + separate video player would require bolting on a separate decoder for half these formats and special-casing the animated ones.

**Supported formats (v1):**

| Category | Formats | Behavior |
|---|---|---|
| Stills | JPEG, PNG | Hold for set duration. PNG transparency renders against **black**. |
| Animated | GIF, AVIF, WebP | Animate and **loop to fill** their set duration. **Never freeze on frame one.** |
| Vector | SVG | Rendered as a **safe image** (its scripts and external references are never run). A clean `viewBox` scales and centers on the black stage; SMIL/CSS animation loops. |
| Video | MP4, MOV, WebM | **Always loop.** "Full length" or fixed duration (see §7). No audio (§12). |

**SVG, rendered as a safe image (added 2026-06-18, §20).** SVG art is served and shown as an `<img>`, the same path as the raster stills, so the browser **does not execute the file's scripts or fetch its external references**; the global CSP (`script-src 'self'`, no inline) blocks inline script even if the file is opened directly. Declarative **SMIL/CSS animation still plays** (it is not script), so animated SVGs loop. This is best-effort, not a generic guarantee: a well-formed SVG with a `viewBox` (the norm) scales and centers cleanly, but a file with no intrinsic size or one that relies on scripting may render unpredictably or sit static, the original reason it was deferred. Vetted, self-contained pieces are the intended use.

**Explicitly ignored (v1):** HEIC, PSD, CR2 (raw), GLB (3D model), and all OS/working-file noise (`.DS_Store`, `.docx`, `.xlsx`, etc.). The player simply skips unsupported files, **no conversion step on ingest** (uploads stay byte-for-byte).

**Display surface, edge-to-edge, no chrome.** The display page is a full-panel **black stage** (`100vw × 100vh`; zero margin, padding, border, scrollbars, or UI). Media renders to the **physical edges of the panel**; OpenObject adds **no decorative frame or border, ever**. In Fit mode the surrounding black is bare stage, not a frame.

**Fit vs Fill (per-clip; v1 default is _Fit_ / original aspect ratio):**

- **Fit** = `object-fit: contain`, entire image visible at its **original aspect ratio**; black fills the leftover. Nothing cropped. **This is the default.**
- **Fill** = `object-fit: cover; object-position: center`, image scaled to cover the full square, cropping the overflow **symmetrically from the center**: left/right trimmed for landscape, top/bottom for portrait. The center is always kept.

The square (1:1) panel makes this choice matter more than on a normal screen, because almost nothing displayed is square, hence per-clip control. **v1 uses center-crop only.**

**Connected pieces (built 2026-06-16).** Beyond files, OpenObject can also show **connected artwork**: generative or hosted pieces from a curated collection (§8), rendered as a **`connected` kind** in a **sandboxed, same-origin `<iframe>`** that fills the stage. The render bundle is mirrored locally first, so it plays offline like any file (with one exception, the live piece below). Fit/Fill does not apply by default (the page sizes itself), though a collection may opt into the per-clip **Fit/Fill** toggle: Erick Calderon (Snowfro)'s *"Chromie Squiggle"* uses it, so its 3:2 can letterbox (Fit) or cover-crop to the panel edges (Fill). Where a collection composes its art in a centered inset, an optional per-collection **crop** zooms the iframe so it still reaches the panel edges (Snowfro's *"send/receive"* fills the middle 60%, so it is zoomed to fill; Azulejo Galo renders a centered square that already fills the 1:1 panel; V4w.enko's *"Perfect Everything"* and *"Perfect Circles"* and Cinzia y Gabriel's *"Pendulum"* likewise render squares that fill the 1:1 panel edge to edge, as does Jeremy Booth's *"Lost in Moffat County"* (a square photo); Alex Kittoe's *"As the Days Go By"* letterboxes its own portrait photo on black). A non-square piece may instead declare an **aspect**: the display sizes the iframe to the largest box of that ratio that fits inside the stage and centers it, a true `contain` on any panel shape (the square frame, or a wider screen when the Mac itself is the display), so the bare black stage forms the letterbox (top and bottom on the square panel, the sides on a wider screen), exactly as a landscape image does in Fit mode (Juicy Julio's *"Golden Lining"*, a 5:4 landscape; Chaz Wesley's *"The Bloom"*, a 16:9 landscape whose "Click to Bloom" gate is hidden from the first paint so the garden shows with no flash; NFTman76's *"Binary Mountains"*, a responsive three.js scene shown 16:9 so its wide mountain vista reads as a landscape image rather than a cramped, stretched-looking square; and Snowfro's *"Chromie Squiggle"*, a 3:2 landscape that letterboxes by default but can be switched to Fill, cover-cropping it to the panel edges; no painted background, no zoom-crop). A connected piece can be a **generative sketch** (Azulejo, whose motion an optional per-collection **Animate** engages on load; or V4w.enko's *"Perfect Everything"* and *"Perfect Circles"* and Cinzia y Gabriel's *"Pendulum"*, which simply loop on their own, so no Animate), a **time-aware piece** that the bundle itself redraws from the viewer's local clock (Kittoe's photographic still has nothing to engage, so no Animate; Jeremy Booth's *"Lost in Moffat County"* crossfades a day photo into a night one and adds an optional **Animate** that swaps in an animated easter-egg overlay), a **live/networked piece** that reads on-chain state at display time and animates from it (send/receive, self-animating, with an offline caveat: see §8), or an **interactive piece we auto-animate**: Golden Lining is a black-and-white to color crossfade the artist drives by mouse, which a per-collection **motion speed (0 to 10)** instead runs as a hands-free, fluid cosine sweep (0 = static full color), and Snowfro's *"Chromie Squiggle"* maps that same 0-to-10 control onto its own colour-cycle speed (0 = the static, as-minted still, default 1) and pairs it with a White/Black background choice, the first connected piece to carry more than one control; or a **self-animating piece with a discrete mode control**: NFTman76's *"Binary Mountains"* orbits and snows on its own, and a per-collection **choice** control (a small dropdown) sets one of the artwork's snowfall modes (Light, Moderate, Heavy, Blizzard) by replaying the artwork's own tap that many times at load. OpenObject's own *"Bouncing OpenObject Logo"* is a self-contained HTML piece that bounces the wordmark across the bare stage and recolours at each edge, self-animating, filling the square stage edge to edge (no Animate control); it is the first collection to carry a **general multi-control** set of its own (Speed and Size sliders, and a Corner effect choice), surfaced in Settings and applied at display time. See §8.

---

## 7. Content model

**Library + Rotation + Pin.**

- **Library**, everything ever uploaded; stays on the player's local storage.
- **Rotation**, the subset currently cycling on the panel, in a chosen order. Order is **Sequence** or **Shuffle**:
    - **Sequence**, plays in the order you set, looping.
    - **Shuffle**, a randomized *pass*: every clip plays once before any repeats, then reshuffles. Good coverage, no near-term repeats.
- **Pin (a.k.a. "permanent"/"hold")**, optionally elevate **one** clip to display permanently, overriding the cycle until unpinned. Supports the workflow "upload several, then manually promote whichever one to permanent," and the degenerate case "I just want one image up forever."

Uploading **adds to the library**. A "daily refresh" habit is achieved by curating which library items are in the rotation (clearing/selecting), so both mental models, daily-replace and growing-curated-library, are served by the same structure. _(**Confirmed 2026-06-11:** library+select, not hard replace-on-upload. **Built 2026-06-13:** per-item add/remove toggle in the Library tab; Sequence order user-arranged by drag or ↑/↓ in the Rotation tab; §20.)_

The **Library grid is sortable** (built 2026-06-24): a **Sort** control on the Library tab orders the grid by **Recent** (newest first, the default), **Oldest**, **Title** (A to Z, case-insensitive), or **Artist** (A to Z by the artist shown on the card, with un-credited pieces last). The choice is a persisted server setting, and the **install sample (the Bouncing OpenObject Logo) stays anchored to the bottom in every order** (§20). This is grid presentation only; the Rotation keeps its own curated order.

The grid also has a **view filter** (built 2026-06-25): a **Show** control beside the Sort offers **All** (the default) or **In rotation**, which hides every piece not currently in the rotation so a large Library can be decluttered to just what is playing, without deleting anything. It is a persisted server setting like the sort, but applied in the browser (so the Library tab count stays the whole library, not the filtered view), and it is fully reversible: setting Show back to All brings the hidden pieces straight back. It changes only what the grid shows, never rotation membership. See §20.

Uploaded pieces can carry an **optional title and artist** (built 2026-06-24): tapping a piece's name on its Library card edits them in place, and a blank field falls back to the filename / file size. Connected pieces keep their chain/registry title and artist (authoritative), so they are not editable. This is **control-panel metadata only**, the display stays zero-chrome (§6), and the **Name** sort keys on the displayed title (a custom title when set, else the filename). See §20.

**Duration, one global, equal time for every piece (confirmed 2026-06-12).**

- A **single global duration** applies to **every** piece in the rotation, equal time per content piece. There is **no per-clip duration**.
- **Stills** (JPEG/PNG) hold for the duration.
- **Animated** (GIF/WebP/AVIF/SVG) and **video** (MP4/MOV/WebM) **loop to fill** the duration: a piece shorter than it repeats; a piece **longer** than it is cut off when the timer advances. **Video always loops; it never freezes on a last frame.**
- **Connected** (§8) pieces hold for the duration like any other piece; with **Animate** on, the piece's own motion runs continuously to fill it. The rotation times each piece's slot from when it actually appears on screen, not from when its render is kicked off (fixed 2026-06-17). This matters only for connected pieces: their generative sketch runs a heavy **synchronous** `generate()` inside a **same-origin** iframe, which briefly blocks the shared render thread (about 4 to 5 seconds on the frame's N5105). Timed from the render start, a connected piece was on screen for only *(duration minus generate time)*, roughly 7 to 8 seconds of a 12 second setting; the visible piece *before* it lingered by the same amount. Reveal-gated timing gives every piece its full on-screen duration. The "appears on screen" signal is the iframe's `load` event, which fires when `generate()` finishes; connected pieces therefore use a **long safety fallback** instead of the brief one files use (`render()` in display.js), because a 500ms fallback would win the race on a slow frame and start the clock mid-generate, the very thing this fixes (refined 2026-06-17). The unavoidable generate stall now lands between pieces (it freezes the outgoing piece for a beat, invisible when that piece is a still). A future option is to keep connected iframes warm so `generate()` runs once instead of on every appearance.

---

## 8. Source layer (pluggable)

Design the source layer as a **clean interface**: *a source provides files to the library.* The uploader is not hard-wired as the only way files can arrive. This is good architecture regardless, and it is what lets future sources slot in without a teardown.

**v1 sources:**

- **Web upload (default).** Open the control panel in a browser → drag art onto the page, or tap to choose files → it lands in the library. Works identically from Mac, iPhone, iPad, Windows. **No mounting, no credentials.** This is the everyday path.
  - **Upload guards (added 2026-06-16).** Each file is size-capped (default **512 MB**, env `OO_MAX_UPLOAD_MB`), with up to `OO_MAX_UPLOAD_FILES` (default 50) per request; an oversize file is refused with a clear message (HTTP 413) and no partial file is left on disk. Before accepting any upload the player checks free space and refuses when the disk would fall below a safety margin (env `OO_MIN_FREE_MB`, default **2048 MB**, HTTP 507), so no client on the LAN can fill the eMMC and wedge the frame.
- **Pull from a network share (optional).** For power users who'd rather point at an existing folder on their machine. Player connects to an SMB share (Samba speaks both macOS and Windows file sharing) and syncs from it. Requires address + credentials in the control panel.

**Connected Collections (built 2026-06-16; §20).** A curated way to show art that has no downloadable file: generative or hosted pieces (web/on-chain), starting with Bryan Brinkman's *"Azulejo Galo"* (a p5.js sketch on Arweave) and Alex Kittoe's *"As the Days Go By"* (a day/night photographic still on IPFS that the bundle swaps from the viewer's local clock). OpenObject ships a **built-in registry of supported collections** (code, not user-authored); the owner picks one and enters the piece's **Token ID**. Where a collection is restricted to specific tokens, the add modal shows an always-visible **"Supported Token IDs"** hint (a single-piece collection's one id, or a curated subset such as Lost in Moffat County's 3/4/5/6, sourced from `fixedToken` or a registry `supportedTokens` list); open collections show none. It is display-only, to set expectations, and is independent of the resolve/error path. Resolution uses the official on-chain path: the player reads the token's **`tokenURI`** (a free `eth_call` to a public Ethereum node), follows it to the canonical metadata, and stores the **official `animation_url` verbatim** (never a marketplace render). The metadata or bundle may be hosted on Arweave (plain https) or **IPFS**; an `ipfs://` URL is dereferenced through a public gateway at fetch time, while the official `ipfs://` URL itself is what we store. The shared render bundle is **mirrored locally** and served same-origin, so the piece plays fully offline after it is added; the title and a cached thumbnail are derived from the metadata. Every add-time read (the RPC `eth_call`, the metadata fetch, and each bundle/asset download) is bounded by a flat **30-second timeout**, so a slow or dead gateway/node fails with a clear error instead of hanging the add forever; a half-downloaded bundle is removed so a retry starts fresh. From then on it is a normal library row (Rotation, Pin, duration as usual), shown as a **`connected` render kind** in a sandboxed iframe (§6). Per-collection **Animate** (auto-engages the piece's own motion, e.g. Azulejo's "Toggle Rotation") and **Hide/Unhide** curation live in Settings (§12); Animate appears only for collections with motion to engage (a time-aware still like Kittoe's has none). A third collection, Snowfro's *"send/receive"* (Art Blocks Flex on-chain), is the first **live/networked** piece and needs two departures from the model above. (1) It is **per-token**: the Art Blocks generator returns a different fully-inlined HTML per token (token id in the URL path, no query seed), so each token is mirrored into its own bundle dir (`collections/<slug>/<tokenId>/`) rather than sharing one; the display builds the per-token path, and shared-bundle collections are unchanged. (2) It reads the collection's **global on-chain state at display time** to animate, so it is the one collection that is **not fully offline**: a narrow `connect-src` exception scoped to just this collection's bundle path (§6) lets the iframe reach **one public Ethereum node**, and we override the artwork's embedded endpoint with our own swappable node via the piece's documented `?rpc_url=` query, so we never depend on the artist's key. Offline it falls back to a static sprite carrying the artwork's own network-error badge. Its art is composed in the centered 60% of the panel, so a per-collection **crop** (measured off the static sprite, constant across the collection) zooms the iframe to fill the panel edge to edge (§6); it self-animates, so it has no Animate control. A fourth collection, Juicy Julio's *"Golden Lining"* (token 1 of *"Dune Reveries - Editions"*), adds four more wrinkles. (1) It is the first **ERC-1155** piece, so resolution reads **`uri(uint256)`** rather than `tokenURI` (substituting the `{id}` template if present). (2) Only this one edition needs special handling (the collection's other two are plain static images), so its registry entry carries a **`fixedToken`** and the add flow skips the Token-ID prompt, still resolving on-chain for faithfulness. (3) The piece is a p5.js sketch that loads its photo and the p5 library by **absolute** URL (the image from inside an inline `loadImage('…')` the relative-asset scan can't see), so a **`localizeAbsolute`** step downloads every absolute asset into the bundle, rewrites the references to local files, and strips now-pointless `integrity`/`crossorigin`, keeping it fully offline. (4) The artist's interaction is a horizontal mouse drag that crossfades the whole image from black-and-white to color; rather than feed input, an injected hook drives the sketch's global crossfade amount as a fluid cosine sweep, exposed as a per-collection **motion speed (0 to 10)** in place of the Animate switch (0 = static full color, default 2, anchored on the resolved color photo). It also declares a 5:4 **aspect** so the display letterboxes it on the bare stage (§6), and its Library thumbnail is sourced from the artwork's own color master, not the token's stylized split preview (half grayscale, half color). Like Lost in Moffat County it is a heavy p5.js piece (a 5 MB library plus a 5 MB photo), so a per-collection `hideSelectors: ['#p5_loading']` rule hides p5's built-in loading screen from the first paint (the same seam), trading the stray "loading..." flash during the crossfade in for a brief blank while the photo settles. (5) The crossfade is built on a per-frame full-canvas `filter(GRAY)` (a whole-canvas pixel read-back every frame), which **WebKit/Safari tile-corrupts** on a GPU-backed canvas this size: gray/black blocks on iPhone/iPad, while Chromium (the XXL kiosk and the "Mac as display" Chrome kiosk) renders it perfectly. Two serve-side fixes were tried and **both failed on iOS** (§20 2026-06-30): forcing a CPU-backed canvas (`willReadFrequently`), and a `preRender` override that dropped the per-frame `filter(GRAY)` for a pre-greyed crossfade (pixel-identical on Chromium, but on the iPhone it rendered blank then hung). Both reverted. Conclusion: Golden Lining is **Safari/iOS-limited** and rendered as-is; it is perfect on every Chromium display (the XXL kiosk and the "Mac as display" Chrome kiosk, which are the supported surfaces), and the blocky Safari/iOS render is an accepted engine limitation, not pursued further. A fifth collection, V4w.enko's *"Perfect Everything"* (an EditART generative series), is the **first on Tezos** rather than Ethereum, so the only real departure is the resolve trail. Tezos has no `eth_call` / `tokenURI`, so `resolveToken` branches on a registry **`chain`** field: a `Tezos` entry reads the token's **FA2 metadata from the TzKT public indexer API** (`GET /v1/tokens?contract=&tokenId=`, one free read keyed off a swappable **`tzkt`** base), where the Ethereum entries keep the `eth_call` path. Tezos names its metadata fields differently, so the artwork URL is **`artifactUri`** (not `animation_url`) and the preview is **`displayUri`** (not `image`); both are normalized in one small chain-aware read. Everything downstream is reused unchanged, because the `artifactUri` is `ipfs://<cid>?m0..m4`, a single shared bundle for the whole collection with the per-edition seed carried in the query string, exactly the Azulejo shape: the IPFS mirror, the **verbatim** official-URL storage, the query-seed-at-display, the cached thumbnail, and fully offline playback all apply as-is. (TzKT only surfaces the contract's own metadata, not a marketplace render, so the verbatim rule holds. TzKT is an indexer service, not a node; if it is ever unavailable the same `token_metadata` can be read straight from a public Tezos RPC node's big_map, recorded as a refactor seam, no code today.) The piece is a self-contained p5.js sketch (p5 inlined, no external assets) whose square canvas fills the 1:1 stage edge to edge (no `crop`, no `aspect`) and loops on its own, so it has **no Animate control**; the owner enters their Token ID like the other shared-bundle collections. A sixth collection, Cinzia y Gabriel's *"Pendulum"* (an EditART series), is a **second Tezos collection** and introduces no new mechanism: it is resolved and rendered exactly like *"Perfect Everything"*. The TzKT FA2 read off the swappable `tzkt` base, the verbatim `artifactUri`, the `ipfs://<cid>?m0..m4` shared bundle with the seed in the query, the cached `displayUri` thumbnail, and offline playback all apply unchanged, and the owner **enters their Token ID** like the other shared-bundle collections (the contract holds three editions, #0 to #2, all the same self-contained p5.js sketch, and any edition resolves). It is a square sketch that fills the 1:1 stage edge to edge (no `crop`, no `aspect`) and loops on its own, so it has no Animate control. A seventh collection, Jeremy Booth's *"Lost in Moffat County"* (Ethereum), is a **time-aware photographic series with an opt-in animated overlay**; it reuses three mechanisms already built plus one new hook. It is **per-token** like send/receive (each token's `tokenURI` resolves to its own media bundle, with metadata split across Arweave and IPFS over the collection), and it uses **`localizeAbsolute`** like Golden Lining (the p5 bundle loads the library and its day/night photos plus easter-egg GIFs by absolute URL from a public gateway, so they are pulled local and the refs rewritten for offline play). Like Kittoe it is **time-aware**: the sketch reads the viewer's local clock and crossfades a day photo into a night one (sunrise 6 to 8, sunset 18 to 20), so the background is chosen automatically with no app timezone UI (correctness = the frame's OS clock). The new part is a click **easter egg**: a tap toggles the sketch's global `easterEgg`, swapping in an animated GIF overlay (itself day/night aware); that is exposed as the per-collection **Animate** (default **off**, background only), engaged hands-free by a dedicated hook (`animateHook: 'easterEgg'`) that flips the flag once the sketch has loaded. The square photos fill the 1:1 stage edge to edge (no `crop`, no `aspect`). p5.js shows its built-in loading screen (`#p5_loading`, here restyled by the artist) while the sketch preloads those heavy photos, and the display reveals a connected piece on its iframe's document `load` (before p5 finishes loading the images), so that screen flashed during the crossfade in; a per-collection `hideSelectors: ['#p5_loading']` rule hides it from the first paint (the same seam as The Bloom's `#startOverlay`), trading the stray "loading..." text for a brief blank as the image loads. The dynamic piece renders the artist's HTML, which loads the day/night photos at 2500², mirrored byte-for-byte; the metadata's `image` is a separate static 3840² day-only JPEG (a snapshot, no time-of-day or easter egg), preserved as the high-res still but not used for the dynamic render. Two tokens (Desert Steel #1 and Resolute #7, both static images with no `animation_url`) are intentionally **not supported** here: each resolves with a clear "no artwork URL" message and can be added as a normal upload instead. An eighth collection, V4w.enko's *"Perfect Circles"* (a second EditART series from the same artist), is a **third Tezos collection** and introduces no new mechanism: it is resolved and rendered exactly like *"Perfect Everything"* (the TzKT FA2 read off the swappable `tzkt` base, the verbatim `ipfs://<cid>?m0..m4` shared bundle with the seed in the query, the cached `displayUri` thumbnail, and offline playback all apply unchanged). The contract holds ten editions and the owner **enters their Token ID**; the square self-looping sketch fills the 1:1 stage edge to edge (no `crop`, no `aspect`, no Animate control). A ninth collection, Chaz Wesley's *"Chazstract"* (Tezos), supports a single piece, token 28, *"The Bloom"* (a p5.js interactive flower garden). "Chazstract" is the artist's general objkt.com contract where every token is a different artwork, so like *"Golden Lining"* the entry carries a **`fixedToken`** and enables just the one vetted piece; Tezos resolve via TzKT is reused as-is. It adds two reusable mirror seams. (1) Its `artifactUri` is a bare IPFS **directory** CID whose `index.html` loads its scripts by **relative** ref, so a **`dirBundle`** flag takes the asset base from the directory itself rather than the gateway root (path-carrying collections are unchanged). (2) The bundle also ships **`p5.sound`**, which the sketch never uses (its audio is raw Web Audio) and which hangs the renderer in restricted contexts; since OpenObject is always muted, a **`dropScripts`** flag removes that one unused `<script>` from the mirror, so the visual is identical and renders reliably everywhere (the desktop archival copy keeps it). The piece is a fixed **1920×1080** canvas that self-fits to the viewport; it declares a 16:9 **`aspect`** so the bare stage letterboxes it (full-screen on a 16:9 display, §6). It opens behind a **"Click to Bloom"** overlay that also gates the bloom (the flowers stay frozen as buds until it fires), so an injected hook auto-starts it on every display, silently (the music is no-op'd); because that overlay is static HTML it would otherwise flash briefly before the hook fires, so a per-collection **`hideSelectors`** rule hides it (`#startOverlay`) from the first paint. Its hand interactions (click a flower to spin, the music panel) are not supported on a passive, muted frame; the garden self-animates regardless (flowers sway and spin, a spaceship drifts). Opt-in and off the default path; normal uploads and playback are unchanged. A tenth collection, NFTman76's *"Binary Mountains"* (Ethereum, token 17 of the artist's general *"Code art"* contract), is the first to expose a **discrete choice control**. Like *"Golden Lining"* / *"The Bloom"* the contract is a grab-bag where every token is a different piece, so the entry carries a **`fixedToken`** and enables just the one vetted token (still resolved on-chain via `tokenURI` for faithfulness). The artwork is a single self-contained HTML file, a three.js wireframe mountain range orbited by a drifting camera under falling snow, that loads three.js by **absolute** URL, so **`localizeAbsolute`** pulls that one script local and rewrites the ref for offline play. The scene is responsive (its camera adapts to the viewport) with no inherent shape, and the artist's export is near-square (804×760); rather than fill the square panel (where the wide mountain vista reads as a cramped, stretched-looking crop) it declares a 16:9 **`aspect`** so the display letterboxes it as a landscape image (no `crop`), and the snow hook also nudges a resize after load so a responsive WebGL canvas can't latch onto a stale, stretched size. The camera and the snow run on their own, so there is no Animate control; what the piece exposes instead is snowfall intensity. The artist drives that by tap (a global `snowLevel` 0..3 that the click handler cycles), so OpenObject adds a generic **`choice`** control: the registry entry declares a labeled option set (`Snow`: Light/Moderate/Heavy/Blizzard, the artist's full set), the owner picks one from a **dropdown** in Settings (stored in a new `collection_state.choice` column, validated server-side against the declared options), the display passes it as **`?oochoice=N`**, and a per-collection hook (`choiceHook: 'snow'`) reaches that level by calling the artwork's own click handler N times once it exists (`snowLevel` is a lexical binding, not a window property, so it cannot be assigned directly, but the handler is a global function; from a start of 0, N taps land on level N, the faithful equivalent of the artist's tap). The same hook hides the artwork's two on-screen overlays (a "tap to snow" prompt and a level pill), interaction chrome that is meaningless on a passive, zero-chrome frame (§6). The `choice` plumbing (the DB column, the API field, the `?oochoice` param, the dropdown) is a reusable seam for any future collection with a curated set of modes; only the hook is piece-specific. Opt-in and off the default path; normal uploads and playback are unchanged. An eleventh collection, Erick Calderon (Snowfro)'s *"Chromie Squiggle"* (Ethereum), is the OG Art Blocks piece (project 0 on the platform's shared Art Blocks Core contract `0x059edd72cd353df5106d2b9cc5ab83a52287ac3a`) and is the connected feature's first **multi-control** piece: it carries a speed slider, a White/Black **background** choice, and a per-piece **Fit/Fill** toggle all at once (every other collection has at most one control), and its controls are the first to **drive the artwork's own globals** directly. Because that one Core contract hosts hundreds of unrelated projects, a raw Token ID could resolve a completely different artwork, so the entry carries a **`requireProject`** guard: resolution still reads `tokenURI` to the official metadata, but a token whose `project_id` is not 0 is rejected with a clear "isn't a Chromie Squiggle" message while any actual Squiggle resolves. The Squiggles are a real ~10,000-piece seeded series (not a grab-bag), so the owner **enters their Token ID** rather than a `fixedToken` (the Pendulum lesson: don't pin a true series). It is **per-token** like send/receive (the Art Blocks generator inlines that token's unique hash into a different HTML per token, token id in the path), so each mirrors its own bundle; but unlike send/receive it reads no live state, so once **`localizeAbsolute`** pulls its one absolute asset (p5.js 1.0.0 from cdnjs) local, it plays fully offline. Animation is a **speed control**, not on/off: the sketch cycles the squiggle's colour each frame only while its global `loops` is true, at a rate set by its global `speed` (the artist toggles `loops` by click and nudges `speed` with the arrow keys). Both are top-level `let` bindings, but a classic `<script>` injected into the same document shares that top-level scope, so the hook sets them directly, surfaced as a per-collection **motion speed (0 to 10)** (Golden Lining's slider): 0 freezes the as-minted still, 1 to 10 turn the colour loop on and set its pace (default **1**, the official piece's own gentle `speed`, so it animates faithfully out of the box). The metadata declares `aspect_ratio` 1.5 and the canvas is locked to 3:2, so by default it is shown faithfully at 3:2 (**Fit**), letterboxed on the bare stage over the artist's white field, the canonical Art Blocks look. It is the **first connected piece to offer Fit/Fill** (a per-collection **`fitFill`** flag): **Fill** sizes the iframe to cover the stage and the layer clips the overflow (the iframe equivalent of `object-fit: cover`, centre-cropping the 3:2), so the squiggle reaches every edge with its ends, or top and bottom, trimmed depending on the screen shape. The control panel shows the Fit/Fill button on the piece's card (it was hidden for connected pieces before; now shown when the collection opts in), and the display's connected signature now includes `fit` so a Fit/Fill flip re-renders the piece live. The **Background** choice reuses the same `choice` dropdown mechanism as Binary Mountains' Snow, here mapping White and Black to the artist's own `backgroundArray` index (white = 0, black = 10) set by the same combined hook that applies the speed. Because the squiggle carries both a `speedControl` slider and a `choice` dropdown, `control.js` now renders a collection's controls as a list (speed, then background) rather than one mutually-exclusive control; the Fit/Fill button stays on the Library card, while the speed and background live in Settings. Matt's call (2026-06-20); the multi-control treatment is squiggle-only, and Fit/Fill stays off for every other connected collection (their bundles size themselves). A twelfth collection, OpenObject's own *"Bouncing OpenObject Logo"* (Ethereum ERC-1155 contract `0xa6279e1bcc8dfab0fef4e556d1cf9d6eee864de7`, token 1), is the first piece authored by the project itself and the first to use a **general multi-control model**: where earlier collections carry at most a single `speedControl` slider or `choice` dropdown, a collection may now declare a **`controls`** list of sliders and selects, and this piece exposes three (Speed and Size sliders, plus a Corner effect select: Confetti/Flash/Pulse/Shockwave/None). It is an ERC-1155 piece resolved by `uri(1)` (the Golden Lining path) with a `fixedToken` (the whole collection is this one piece, so no Token-ID prompt), its `animation_url` stored verbatim; the artwork is a fully self-contained HTML file (an inline SVG wordmark plus CSS and JS, no external assets), so the mirror is a clean one-file copy. The mark drifts DVD-screensaver style and recolours at each edge, self-animating, filling the square stage edge to edge (its size is a % of the short side, so no `crop`/`aspect` and no Animate control); its three controls are stored per collection as JSON (`collection_state.controls`), passed to the display as `?oo_<key>` params, and applied by a `BOUNCE_HOOK` that sets the artwork's own globals (the Chromie Squiggle technique). It is also the sample piece seeded into every fresh Library, so a new install starts with it already present (the install ships its bundle and copies it in on first boot; see §20).

**Not a source / explicitly out of scope:** Git/GitHub is **not** part of the content path in any form. GitHub is used solely for **source-code management and distribution** of OpenObject itself (§15). Art never touches the repo.

---

## 9. Storage & sync model

**Progressive sync, full local mirror at rest, buffered fallback only when forced.**

- **Progressive sync (first run):** grab the first few clips, **start the rotation as soon as the first clip lands**, then continue pulling the rest into local storage in the background while the panel is already showing art. The display waits only for the first clip, not the whole library. The rotation must **fold in newly-arrived clips gracefully without restarting or stuttering** the loop.
- **Full local mirror (steady state):** once synced, the entire library lives on the eMMC and **playback is always local**, a 30 MB clip loads from local storage in a fraction of a second. The network is used **only when the library changes** (add/remove). No clip is ever downloaded or deleted at display time. There is **no per-clip download→display→purge loop**, that design is explicitly rejected because it puts Wi-Fi in the playback path and churns the eMMC.
- **Capacity:** the target library (hundreds of ~30 MB clips ≈ 3–30 GB) fits comfortably in ~100+ GB of free eMMC after a slim Linux install. microSD expands it further.
- **Buffered mode (optional fallback):** engages **only** when a library genuinely exceeds free storage (e.g. a future owner with a 300 GB 4K archive). It is **not** strict per-clip purge; it is a **prefetch buffer with least-recently-shown eviction**, keep the next N clips staged ahead of playback, evict oldest-shown only under space pressure, so the network is never what playback waits on. Matt's default never engages this; it exists for the next-owner kit.

A single **storage-mode setting** governs this: default **"full sync"**, with **"buffered"** offered only when the library outgrows the disk.

---

## 10. Power handling

The physical power button is hard to reach when wall-mounted. This is fully solved in software/BIOS:

- **Auto Power On (BIOS):** enabled during reflash (§4). The unit boots the moment it receives power. Power control becomes the wall outlet (or an optional smart plug). Also means the frame **self-recovers after a power blip** instead of waiting for a button press.
    - **Confirmed (Matt, 2026-06-13):** this unit **boots when the power cord is unplugged and replugged**, so cycling the outlet reliably boots it and the hard-to-reach physical power button is a **non-issue**. After a **Shut down**, you power the frame back on by replugging (or toggling a smart plug). **Resolved at bench:** there is **no Auto-Power-On toggle** in this BIOS (checked `Chipset → PCH-IO Configuration`, no "State After G3" / "Restore AC Power Loss"); auto-on is the **firmware default**, which is exactly this replug-boot behavior. Nothing to set.
- **Soft restart / reboot / shutdown (web UI):** the control panel exposes **Restart** (the player), **Reboot** (the whole device), and **Shut down**, initiated from the owner's browser. Handles all intentional reboots without reaching behind the panel.
- **Hard lockup (rare):** power-cycle the outlet; an optional ~$15 smart plug makes this a phone tap. Not required.

**Built 2026-06-13 (§20).** The control panel's **Settings → Power** card ships **Restart**, **Reboot**, and
**Shut down**. **Restart is real now**: an app-level soft-restart via the supervisor (exit →
relaunch, the same path as self-update, §15), so it works browser-only with no hardware and behaves
identically once systemd runs it on the device. **Reboot** (`systemctl reboot`) and **Shut down** (`systemctl poweroff`) are real on the installed
Linux frame, carried out by systemd via a one-time polkit grant the installer adds for the
`openobject` user; off-device (the dev Mac, which must never be powered off) they stay inert stubs
that only return a message. Per the
Auto-Power-On point above, a real power-off returns when power is restored, so a true "stays off"
is the outlet / smart plug.

---

## 11. Network & first-run onboarding

A fresh, wall-mounted box has no art and isn't on Wi-Fi yet, but the control panel is reached *over* Wi-Fi. Solve the chicken-and-egg with a **self-broadcast setup network** (the standard headless-device pattern; keyboard-free and next-owner-friendly):

1. On first boot with no known network, the player **broadcasts its own temporary Wi-Fi network** (e.g. `OpenObject-Setup`).
2. The owner joins it from a phone/laptop; a **captive setup page** appears.
3. They pick their home network and enter the password.
4. The player **switches over** to the home network and the setup AP shuts down.
5. This **self-heals** if home Wi-Fi credentials later change (box falls back to setup AP).

**Reaching the control panel:** `http://openobject.local` (mDNS), works natively on Mac/iPhone. The setup page also **displays the raw IP** as a fallback (e.g. for Windows clients without mDNS).

**Wired option:** the free Gigabit Ethernet port is a fully supported bonus, but **Wi-Fi is the baseline**, the kit must not require running a CAT5 cable, since the next owner may not be able to.

**Built 2026-06-13 (§20, Phase-1 stub).** The control panel's **Settings → Network** card explains the
first-run onboarding flow above; the setup AP + captive page themselves are **Phase 2** (they need the
device's networking). The card also shows **how to reach the control panel now**, the live LAN
address(es) (`http://<ip>:<port>`, from `GET /api/system`) plus the `openobject.local` name **only where it
resolves**: the installed frame, detected as `process.platform === 'linux'`. On a Mac/standalone that
line is hidden, so the panel never advertises an address that would not work.

**Reference, observed Infinite Objects onboarding (2026-06-13), and our refinements.** Matt captured
the stock XXL's Wi-Fi setup; it's a proven flow worth building on. Observed: the frame's screen shows
**two QR codes**, (1) a Wi-Fi-join QR for its setup AP (`IOXXL-<id>`, WPA password `infinite`), and
(2) a URL QR to a config page at `ioxxl.local`. On the page you enter **SSID + Pass Phrase → Connect**;
the phone then shows "configuration in progress, wait for confirmation on the screen," and the frame
confirms "Successfully Connected… searching for updates and rebooting."

Refinements to adopt for OpenObject (Phase 2, build with the hardware):
- **QR codes on the frame's setup screen**, the biggest win. The branded boot/idle screen, in its "no
  network yet" state, *becomes* the setup screen: mark + a **join-AP QR** (one-tap join, no typing) + an
  **open-config QR** (one tap to the captive page) + a manual fallback (network name, password, URL).
- **Plain labels** on the config page, "Wi-Fi network name" and "Password", not "SSID" / "Pass Phrase".
- **Pick the network from a scanned list** rather than typing the SSID (typo-proof; manual entry as a
  fallback for hidden networks).
- **Two-screen confirmation**, when the frame leaves its own AP to join home Wi-Fi, the phone loses
  contact with it, so success is confirmed on the **frame's screen** ("Connected ✓") while the phone
  shows "applying, watch the frame."
- **Recovery line + self-heal**, on failure the setup AP returns; tell the user to re-join it and retry.
- **AP naming/password**, `OpenObject-<id>` (a short unique suffix so two frames don't collide); open
  network vs. a QR-encoded password is a Phase-2 call (the join-QR makes a password basically free).

The stock screens themselves are reference only, captured here as learnings, not committed (art and
photos never enter the repo, §8). Build none of this in Phase 1.

---

## 12. Settings & defaults

| Setting | v1 default | Notes |
|---|---|---|
| Audio | **Muted, always** | Silent art on a wall. The frame has no speaker, and muted is the intent regardless. |
| Control-panel access | **Open on LAN**, optional password (built 2026-06-16) | Off by default (no login friction). When set in Settings it gates the control panel + every mutating API; the kiosk display stays open. HMAC-signed httpOnly cookie session, scrypt-hashed password, no new dependency. |
| Idle / empty screen | **Branded card**, not black | Shows OpenObject mark + an "add art at …" line whose address is the host the display was opened at, falling back to the advertised name on a loopback host (openobject.local on the frame, localhost:3000 when a Mac is the display), so it is never a false instruction. Takes a **logo asset** (Matt-supplied) so the mark drops in without redesign. |
| Display name / mDNS | `openobject.local` | IP fallback shown on setup page. |
| Fit/Fill default | **Fit** (original aspect ratio); settable | Applies to new clips; per-clip override always available. |
| Display duration | **Settable global** (seconds / minutes / hours) | One equal-time duration for **every** piece; no per-clip override. |
| Rotation order | **Sequence** | Sequence / Shuffle (§7); settable. |
| Sleep Schedule | **Off** (no windows) | Up to three day-aware windows (12h clock, per day of week) + week-at-a-glance strip + manual Sleep/Wake toggle; dimmed-logo sleep screen (§13). |
| Updates | **Manual check; track `main`** | Self-update from GitHub via the control panel (§15). Owner-initiated; fast-forward only. |
| Connected Collections | **Supported list; Animate on where applicable, none hidden** | Curated web/on-chain collections, added by Token ID (§8). Per-collection Animate (auto-motion), a motion-speed slider (0 to 10), or a mode dropdown (e.g. snowfall) where the piece offers one, shown only for collections with something to set, plus Hide/Unhide. |

---

## 13. Sleep Schedule (v1 feature)

An **optional schedule to blank or dim the panel** when the owner is out or asleep (configurable times, by day of week). The panel otherwise runs 24/7; this is a longevity and preference feature (the owner dislikes it running at night). When asleep, the panel is blanked/dimmed and resumes the rotation on schedule. Pairs cleanly with the display layer (no playback during the sleep window).

**Built 2026-06-13, reworked to a day-aware schedule 2026-06-21.** Up to **three windows**, each
a start/end time plus the **days of the week** it applies to (seven day chips, Sunday first). A
window with no days selected is inactive, so the chips carry on/off; there is no separate enable
box, and a fresh install ships with **no windows** (off). Times use a **12-hour clock with an
AM/PM toggle**. A window may cross midnight: it is anchored to the day it **begins** (its chips
read **"Nights"**, with an auto **"overnight"** tag), so "10 PM to 4 AM on Friday" is Friday
night into Saturday; a same-day window reads **"Days"**. The server applies that anchoring (a
window's evening half counts as its start day, its after-midnight half as the day before) and
remains the single source of `asleep`. Older two-window data (`{enabled,...}`) migrates on read,
an enabled window becoming all seven days, so behavior is unchanged across the upgrade. Windows
are capped at three: more is a complexity most owners will never want, and the cap can lift later
if asked.

A **week-at-a-glance strip** under the windows draws one 24-hour bar per day, filled where the
screen sleeps, with a marker at "now". It makes the schedule **verifiable by eye**, so an
overnight window cannot be silently attached to the wrong day. The card header shows a live
status: **"Sleeping until 7:00am"** while asleep, or **"Awake until 10:00pm"** otherwise. The
Sleep Schedule and Connected Collections cards (the lists that can grow), plus the
infrequently-used Password and Network cards, are **collapsible** from their headers and **start
closed** to keep the tab short; each card's open/closed choice is then remembered per browser, so
one the owner opens stays open until closed. Software Update, Power, and About are not. Strip positions are set
through the CSSOM, so the strict `style-src 'self'` CSP (no inline styles) stays intact.

A manual **Sleep / Wake** toggle in the control-panel header turns the art off (or back on) on
demand, independent of the schedule, the answer to "how do I stop the display right now?". The
button reflects the **live** state: it reads **Sleep** while the art is showing and **Wake**
(highlighted) while the panel is asleep for any reason. **Sleep** blanks the panel until it is
woken. **Wake** is more than a plain un-blank: pressed during a scheduled sleep window it holds
the schedule off until the **next** window begins (a `wakeUntil` timestamp from `nextSleepStart`),
so the art stays on for the rest of the night and the schedule resumes on its own; pressed
otherwise it just clears the manual sleep. Editing the schedule cancels an active wake-override.
While asleep, **playback stops** and the panel shows the **sleep screen**: the same boot/idle mark
at the **same size and placement, dimmed (~0.05 opacity) and with no caption** underneath. To rest
the panel it does a slow, imperceptible **pixel-shift** every ~90 s (the standard anti-burn-in
technique, chosen over a periodic fade as sufficient on its own; on this LCD it's
belt-and-suspenders anyway). The server computes `asleep` from the schedule plus two manual
overrides, manual Sleep (`manualBlank`, off until woken) and manual Wake (`wakeUntil`, schedule
suppressed until the next window), and the display renders on that one signal, flipping within
~5 s of a boundary. Phase 1 blanks in **software**; dimming the actual **backlight** is a Phase 2
hardware hook.

---

## 14. Branding asset

Matt is producing an **OpenObject mark** and will store it in the project (`~/Code/OpenObject`). Design the **fresh-boot screen** and **idle/empty screen** to consume a logo asset (with a tasteful text fallback if absent), so the mark drops in without layout changes. Aesthetic direction: understated, functional, no clutter.

---

## 15. Repository & distribution

- **Local root:** `~/Code/OpenObject` (already created).
- **GitHub:** Claude Code initializes the repo, structure, and first commit. The repo is **private during development** (2026-06-11) and intended to go **public** later to serve the next-owner mission. Claude drives all git operations; Matt approves pushes at checkpoints (this replaced the original "walk Matt through it step-by-step" plan, see §20).
- **Releases:** publish the **prebuilt USB installer image as a release asset** so a non-technical owner downloads one file instead of building anything. (Release assets hold large binaries; the repo proper does not.)
- Suggested repo layout:

```
OpenObject/
├─ README.md                ← public next-owner narrative + quickstart (generated during build)
├─ docs/
│  ├─ HANDOFF.md            ← this build spec (working doc; kept current)
│  ├─ SETUP-GUIDE.md        ← casual-user guide (§16; kept current in tandem)
│  └─ appendix-original-reset.md  ← original software reset procedure (§18)
├─ player/                  ← OpenObject stack (web server, control panel, display page, sync, etc.)
├─ installer/              ← bootable USB build
├─ assets/                  ← OpenObject mark, idle/boot screens
└─ ...
```

### In-place updates (self-update from GitHub): Phase 1

The player updates itself from this GitHub repo, **from the control panel**, no
reflash, no SSH, no rebuild. This is GitHub's only runtime role; **art still never
touches the repo** (§8). The player runs as a **git checkout of the repo**, so "update"
is a `git` fast-forward plus a restart.

**Owner flow:** the control panel shows the running version and a **Check for updates**
button → a `git fetch` compares the local checkout to its upstream tracking ref → if
newer, it shows what's available → **Update & restart** fast-forwards the checkout
(`git pull --ff-only`), reinstalls dependencies if they changed, and relaunches the
player. The idle/boot screen flashes briefly; the panel returns on the new version, and
`/healthz` reports the version that came back up.

**Guardrails:**
- **Fast-forward only**, never a force-reset. If the checkout has diverged (local
  edits) the update refuses and says so rather than clobbering it.
- **App-relevant changes only.** A check counts only commits that change what the frame
  runs (`player/`, the served `assets/`, `installer/`). Commits confined to docs, the
  website (`site/`), or repo meta report **up to date** instead of nagging a restart.
- **Runtime data is never touched.** `player/data/` and uploads are gitignored, so a
  pull never disturbs the library, settings, or art.
- **One update track:** the frame always fast-forwards to the upstream of `main`
  (`origin/main`). There is no separate "stable" channel, so a fix can never sit on
  `main` waiting for a release before frames see it; the cost is that `main` must stay
  always-deployable (only vetted commits land there). Tags / GitHub Releases remain a
  point-in-time **publishing** concept (the cited version, the prebuilt USB image),
  not an update lane (§20, 2026-06-30).
- **Local-first and offline-safe** (§9): if GitHub is unreachable the check fails
  gracefully and playback is unaffected. Updating is always **owner-initiated**, never
  automatic, never in the playback path.

**Phase split:** Phase 1 builds and tests the whole mechanism on macOS, check,
fast-forward, dependency reinstall, version reporting via `/healthz`, and a dev-friendly
restart (the supervisor relaunches `node server.js`). It lands **after the core control
panel**, since the UI lives there. Phase 2 only swaps the restart for the OS service
manager (a systemd unit restarts the player on the device); the mechanism above is
unchanged.

**Built 2026-06-13.** Lives in the control panel's **Settings** tab. `npm start` runs a tiny
**supervisor** (`player/supervisor.js`) that relaunches `node server.js` when the player exits
with restart code 75 after an update; `npm run start:direct` runs the server without it (manual
restart). Routes: `GET /api/update` (no-network status for page load), `POST /api/update/check`
(fetch + compare to the upstream of `main`), `POST /api/update/apply` (fast-forward + deps-if-changed
+ restart). `/healthz` returns `{version, commit}` so the panel can
confirm the relaunched version. All git runs via `execFile` against the **repo root**; the player
app and its `package.json`/lockfile live in `player/`, so a dependency reinstall runs there. The
card shows `version · build date · commit` (no jargon). The frame tracks a single ref, the upstream
of `main` (`origin/main`); there is no channel setting (the dormant `update_channel` track was
removed, §20 2026-06-30). Verified on macOS against a
throwaway bare-clone "fake origin" (no real upstream commits, never touches GitHub): check →
fast-forward + restart (commit flips on `/healthz`) → divergence refusal, plus the offline and
not-a-git-checkout paths. Phase 2 swaps the supervisor for a systemd unit; the mechanism is
unchanged.

**Built 2026-06-14 (Phase 2, device).** On the frame, **systemd is the supervisor**:
`openobject-player.service` runs `node server.js` directly with `OO_SUPERVISED=1` and
`Restart=always`, and treats the player's exit-75 (self-update + Restart) as a clean relaunch
(`SuccessExitStatus=75`). **No player code changed**, only what relaunches it. The checkout
lives at **`/opt/openobject`** (a real git checkout, `origin` → the GitHub repo) and runtime
data at **`/var/lib/openobject/{data,uploads}`** via `OO_DATA_DIR`/`OO_UPLOADS_DIR`, so a pull
or a re-seed can never touch the library. The player listens on **port 80**
(`CAP_NET_BIND_SERVICE`, still non-root) so the panel is plain `http://openobject.local`.
**Self-update is wired but only goes live when the repo is public**, while private, a
`git fetch` can't authenticate (we set `GIT_TERMINAL_PROMPT=0` so *Check for updates* fails fast
and reports gracefully rather than hanging); making the repo public is the one-step follow-up.
The bench unit is **seeded from a `git bundle`** of the local repo (private repo → no clone
auth needed; full history preserved so self-update works once public).

### Servicing the installed frame (Phase 2)

**What a self-update can ship: Tier 1 vs Tier 2.** Self-update (and a plain Restart) only
fast-forwards the `/opt/openobject` git checkout and relaunches the player, so changes split into
two tiers:

- **Tier 1 (rides self-update):** anything that is a file in the checkout. The player code, the
  kiosk scripts under `installer/kiosk/`, the cursor theme. These reach the frame with a normal
  *Check for updates -> Update & Restart*. Caveat: display or kiosk-launcher changes also need a
  **cold power-cycle** to reload Chromium/cage (a player restart does not reload the kiosk page).
- **Tier 2 (does NOT ride self-update):** anything outside the checkout. systemd unit files
  (installed under `/etc`), apt packages, polkit rules, the kernel command line. These need the
  provisioner re-run: `sudo bash /opt/openobject/installer/install.sh` (idempotent). When a change
  is Tier 2, call it out, otherwise a self-update alone silently no-ops it.

**Update view: control-panel auto-reload + reboot hint (built 2026-06-20; planned 2026-06-18).** Two related fixes so an update is actually visible afterward. (1) The control panel now **reloads itself** once the new version is confirmed up (`waitForRestart` polls `/healthz`, then `location.reload()`), so a control-panel change shows without a manual refresh. The earlier staleness was not caching: the static assets are served `Cache-Control: max-age=0` + ETag, so a reload revalidates and fetches the new `control.js`/`.css`; the page simply never reloaded, only the version line did. (This supersedes the parked "cache-bust the assets" idea, there was nothing to bust.) (2) The kiosk display is different: a player restart does not reload the kiosk Chromium, so an update touching the display page (`player/public/display.*`) or the kiosk launcher (`installer/kiosk/`) still needs a **frame reboot**. `updater.check()` sets a **`requiresReboot`** flag (from `git diff --name-only HEAD..<target>` via `isKioskFacingPath`), the *What's in it* summary shows a line ("After updating, reboot the frame to finish applying the display changes."), and after such an update the panel keeps that reminder visible (Reboot is in the Power card) instead of auto-reloading away from it. Detected automatically per update, not a manual tag.

**Console access at the panel.** If the kiosk ever needs a shell at the frame itself, switch to a
virtual terminal with **`Ctrl+Alt+Fn+F2`** and return to the art with **`Ctrl+Alt+F1`**. Both
halves of the chord matter: cage is launched with `-s` so VT switching is allowed, and on a compact
keyboard whose top row are media keys (e.g. the Logitech K400) the function keys need the **Fn** key
(hence Ctrl+Alt+Fn+F2). SSH is the other door and is on by default (see the 2026-06-17 §20 entry).

---

## 16. Documentation requirement (two audiences, kept in lockstep)

Maintain **two** living documents as the build proceeds, not one written once and left to drift:

1. **`docs/HANDOFF.md` (this doc)**, the engineering working doc. Accumulates design tweaks, troubleshooting notes, bench discoveries, and test results.
2. **`docs/SETUP-GUIDE.md`**, the **casual-user** guide. No engineering. For someone who has never seen the handoff and never will.

Since 2026-06-19 there is also **`docs/MAC-DISPLAY-SETUP.md`**, a standalone **no-frame** guide (running OpenObject on a Mac as the display, for someone who has no frame); keep it accurate to shipped behavior the same way.

**Discipline:** whenever a change affects what a new user does, a different BIOS key, an added step, a renamed setting, **update the Setup Guide in the same change.** The Setup Guide must always reflect shipped behavior.

### Setup Guide scaffold (fill placeholders as values are confirmed)

> **What you need before you start**
> - A functioning **Infinite Objects XXL** unit (the 26" square frame) with its **MeLE Quieter 3Q** mini PC on the back.
> - **USB hub:** UGreen USB 3.0 Hub, 4-port, 2 ft (model **25946**).
> - **Left-angle USB-A extension:** CableCreation USB 3.0 Extension, Left Angle, 1 ft (part **CC0516**). _(The **left** angle is the one that fit, it clears the controller's power socket beside the USB port.)_
> - **USB keyboard:** Logitech **K400 Plus** (keyboard + touchpad on one receiver).
> - **USB flash drive:** ≥16 GB (for the installer).
> - **External USB drive:** ≥128 GB (only if preserving the original White Walls backup).
>
> **Steps** (kept in sync with the real procedure)
> 1. Plug the extension into the mini PC's free USB port; attach the hub; attach keyboard + installer drive.
> 2. Power on and enter the BIOS by tapping **`Del`** (the splash hides the prompt, tap from the black screen); set USB boot. _(No "Auto Power On" to enable, the frame already powers on by itself when it has power.)_
> 3. Boot the installer. (Optional: back up the original software first.)
> 4. Install OpenObject; wait for the panel to show the OpenObject screen.
> 5. Join the temporary **OpenObject-Setup** Wi-Fi from your phone; pick your home Wi-Fi; enter the password.
> 6. Open **http://openobject.local** in any browser; drag your art onto the page; set durations and fit/fill; done.

---

## 17. Future enhancements (documented seams, not built in v1)

**One seam is currently open, documented below: Folder Collections.** Earlier seams once listed here have since shipped (Connected Collections, §8; SVG support, §6) or were dropped as out of scope; build notes for what shipped live in §20.

### Folder Collections (designed 2026-07-08; Phase A next, Phase B committed)

**Goal.** Let an owner show a whole folder of files as the display, without the tedium of uploading them one by one. A folder of, say, 500 short clips becomes a single selectable source.

**Either/or with the Library.** A Folder Collection is a saved, configured **source**, a sibling of the Library, not a set of Library rows. The display shows **either** the Library rotation (the default) **or** exactly one Folder Collection, never both. The switch is lossless: the Library rotation is preserved untouched and resumes when the owner switches back. Adding or editing a folder is a **staging** action; it never changes what is on screen until that folder is selected as the source.

**Two phases (build order), one committed destination (the frame).**

- **Phase A, local.** The player and the folder live on the same machine (for example a Mac used as the display, §20 2026-06-19). Files are served **in place** straight from the folder (a scoped static route): nothing is copied, nothing is cached, nothing can be stranded. This is the whole feature minus the network, and it is the foundation Phase B builds on.
- **Phase B, the frame (committed, not optional).** The folder lives on the owner's Mac and the XXL frame is the display, so the frame reads the folder over the LAN and keeps a **bounded, hard-capped local cache** (not necessarily the whole folder) so playback stays local and smooth **without a Folder Collection ever crowding out the Library**. This honors §9 (the network is never in the playback path). Phase A and Phase B share one **source contract** (a folder yields a list of items plus an HTTP media URL per item), so the display code is identical across phases; Phase B changes only where the bytes come from and how much is held locally (see the storage note below).

**What plays.**

- **Compliant files only.** The existing formats allowlist (§6) is reused, so unsupported files and OS noise are silently skipped: no conversion, no error.
- **Duration** is the one **global** equal-time value (§7); there is no per-folder duration.
- **Order** is **per-folder**: Sequence or Shuffle (the §7 set; no Random, dropped as before). It is an **override** that does not touch the Library's own order, so leaving folder mode restores the Library's setup with no re-editing (the same non-destructive principle that preserves the Library rotation).
- **Fit** is **per-folder**: one value (Fit or Fill, §6) for the whole folder, not per clip.
- **Pin** (§7) does not apply in folder mode (there are no individual rows to pin). **Sleep Schedule** (§13) applies unchanged.

**Always live (no include toggle).** New compliant files that appear in the folder enter the rotation automatically, and removed files leave; there is deliberately no per-file or on/off inclusion control. Local folders use an OS **file-watch** (near-zero idle cost, near-instant pickup, debounced so a bulk copy coalesces into one re-scan); a network mount, where file-watch is unreliable, falls back to a **relaxed periodic re-scan**. Either way the change is folded into the running loop by the display's existing 5-second poll, with no restart or stutter (§9 progressive sync).

**Data model.** A `folder_collections` table (`path`, `name` defaulting to the folder's own name, `artist`, `fit`, `order`) plus one `display_source` setting (`library`, the default, or a folder id) that is the either/or switch. No per-folder duration column (global), no include column (always on). `/api/display` branches on `display_source`: for an active folder it returns that folder's compliant files in the same item shape the display already consumes (a synthetic key per file, `format`/`kind` from the allowlist, the folder's `fit`, and a media URL under a per-folder route such as `/folder-media/<id>/<file>`) and that folder's `order` as `mode`; the Library path is unchanged. The **media route resolves by folder id plus file key**, pointing at the local folder in Phase A and at the frame's local cache in Phase B, so `scan folder to items` becomes `read the cache manifest to items` with the display none the wiser (whatever is not yet cached is fetched on demand and folded in). An unreachable-source status slot exists from the start (inert locally; on the frame an unreachable folder keeps showing the last synced copy and retries, rather than blanking).

**UI: setup in Settings, activation in Rotation.**

- **Settings** gains a **Folder Collections** card built as a **twin of the Connected Collections card** (§8, §12): a collapsible card with a count, a **Choose folder** picker, and one row per saved folder carrying its Name, Artist, Fit, and Order plus delete. The picker is a small server-side folder browser (a web page cannot hand the server a filesystem path, and the directory-upload control would copy every file, the very thing being avoided), with a typed path as a fallback, both behind the control-panel password and scoped to a safe root. **Everything about a folder is edited here and nowhere else.**
- The **Rotation** tab gains a **Source** control under the interval box: a dropdown of the Library (default) and each saved folder. Selecting one makes it live at once. In **Library** mode the tab is exactly as today (the curated rotation list and its order control). In **folder** mode the per-piece list is replaced by a **read-only summary** of the folder (name, artist, piece count, Fit, Order) with a **Manage in Settings** link; the global interval stays editable because it governs any source. **Nothing about a folder is editable from Rotation; the only action is selecting it** (a folder is a configured object the owner selects, not something tuned while it plays).
- The header (Sleep, Open display) stays clean, with **no folder name** crammed in. A quiet one-line note on the **Library** tab while a folder is live ("A Folder Collection is on screen; switch sources in Rotation") heads off confusion about why the curated Library rotation is not showing.

**Phase B specifics (open, to settle just before Phase B is built).** Two questions are deferred to that point. **(1) How the frame reaches the Mac's folder:** preferred is OpenObject's own **Bonjour plus HTTP host rails** (the frame discovers the Mac host and pulls over the LAN, no share to configure, best fit for the next-owner mission), with a mounted **SMB** share as the alternative (§8's long-standing pull-from-a-share seam). **(2) How much is stored on the frame,** the load-bearing question, because **a Folder Collection must never crowd out the Library, which is the hero of the product.** The guiding priority (Matt, 2026-07-08) is **storage frugality over maximum smoothness**: the owner would rather accept some lag than have a folder replicate wholesale onto the frame. So the model is a **hard-capped cache, not an unbounded mirror.** Folder Collections share a single, **aggregate** storage budget on the frame (a setting, with a conservative default): the ceiling is the total across **all** Folder Collections combined, **not** a per-folder allowance, so no number of folders can add up to crowd the Library. Within that budget the cache is a **prefetch-ahead plus least-recently-shown eviction** buffer (the §9 buffered mode, here promoted from fallback to the Phase B default; the Library keeps its own full-sync default from §9, and the cap is precisely what protects it). This **auto-adapts**: because only one Folder Collection is ever live (the either/or), the shared budget serves the **active** folder's working set. An active folder whose clips fit within the budget ends up effectively fully cached and plays smoothly (Matt's own case, a few GB, fits easily); a folder that exceeds it rolls through a bounded window, trading a little lag under storage pressure for a guaranteed ceiling. Switching sources reuses the same pool, evicting the previously-shown folder's now-idle clips. The buffer stays a few clips ahead of playback, so the **network feeds the buffer, not the screen**, and lag appears only if the network cannot keep pace with the rotation, not on every clip. The cache **persists across reboots** (an instant, offline-tolerant boot) rather than clearing each time (which would re-pull over Wi-Fi every boot, the churn §9 avoids), and a manual **re-sync / clear cache** control covers unusual events (a wholesale folder swap, or freeing space). Full local storage of a folder that fits is a *consequence* of the cap, never a mandate: there is no mode in which folders consume the eMMC without limit. (Phase A stores nothing at all: it serves in place, so this whole storage question is Phase B only.)

**Open storage questions to settle at the Phase B kickoff** (captured here so they are not lost; the aggregate cap above already guarantees the Library is never crowded, so these are about reclaiming space sooner and more politely, not about safety):

- **Cache lifecycle when a folder is switched off.** Keep it warm until the aggregate cap's least-recently-shown eviction reclaims it (the current default: fast to re-show, but a shown-once folder lingers), versus **eagerly evicting** the folder you switch away from (nothing lingers, but flipping between two folders re-downloads each time), versus a **per-folder "keep cached" / "discard when not showing"** flag so one-off folders self-clean while frequently-shown ones stay warm.
- **Manual controls.** A per-folder **Clear cache** action and/or a global **Clear all folder cache**, for "I am done with this one, reclaim it now" (the direct answer to the shown-once case).
- **Age-out.** Optionally evict a folder's cache not shown in N days, a gentle automatic cleanup layered on top of the cap.
- **The budget's default value and where it is surfaced** in Settings.

Matt's lean (2026-07-08): the hard aggregate cap stays the automatic backstop; add a manual per-folder **Clear cache** as the low-complexity answer to shown-once folders; weigh a per-folder discard-when-idle flag only if it proves wanted. Decide for real at Phase B.

**Documentation.** The Setup Guide (§16) is untouched until this ships (it reflects shipped behavior only); it gains a Folder Collections section when Phase A lands.

---

## 18. Appendix: original White Walls reset (for owners who want to keep it)

Preserved from the vendor's tutorial so a future owner can re-register the **original** Android software *while the vendor's servers still exist*. Not part of OpenObject; offered as a courtesy in the repo.

The original software is a standard Android app running in **Waydroid** (a LineageOS Android 11 container) on **Ubuntu Linux**. To manually reset its account registration (all within the Android container's UI):

1. Connect a USB-A mouse to the mini PC.
2. Reveal the system menu: click-and-drag **downward from the very top** of the screen (this is the Android notification shade; "begin click at top and drag down", it's fiddly).
3. Expand the menu; click the **Settings** cog.
4. **Apps & Notifications** → the **White Walls** app → **Storage & Cache** → **Clear Storage** → **OK** to delete app data.
5. Click the circular **home** button to exit; the unit can re-register as a new White Walls device.

> The full pre-wipe **eMMC image** (§4, step 6) is the more complete preservation: it captures the entire original install, not just a registration reset.

---

## 19. Open items for Matt to confirm/supply

- [x] **Hardware models** (2026-06-13), UGreen 4-port USB 3.0 hub (**25946**); CableCreation **left-angle** USB 3.0 extension (**CC0516**); Logitech **K400 Plus**. Filled into §16 / Setup Guide.
- [x] **Logo / OpenObject mark**, supplied by Matt; optimized marks in `assets/branding/` (source masters in `Logo/`, gitignored). Transparent / white-on-dark variants derived in Phase 1.
- [~] **Bench-verified specs**, **BIOS-entry key `Del`** ✓; **Auto-Power-On = none / firmware auto-on** ✓ (no toggle exists); **CPU N5105** ✓; **UEFI + Secure-Boot-off** ✓; **eMMC = ~128 GB** (116.5 GiB at `/dev/mmcblk0`; corrected 2026-06-13 from the wrong "~256 GB") ✓; **onboard Wi-Fi works under Ubuntu 26.04 live** ✓ (2026-06-13 smoke test). **Wi-Fi module CONFIRMED at the bench (2026-06-14, `lspci -nnk` on installed Debian 13.5 / kernel 6.12.90):** **Intel Jasper Lake PCH CNVi Wi-Fi `[8086:4df0]`**, driver **`iwlwifi`** (this is the FCC TX ID `PD99560D2` part). **Debian's in-box `firmware-iwlwifi` drives it, Wi-Fi joined in the installer, no dongle needed**, closing the §3 Wi-Fi risk. Also captured: iGPU **Intel JasperLake UHD Graphics `[8086:4e61]`** driver **`i915`**; wired NIC **Realtek RTL8111/8168 `[10ec:0123]`** driver **`r8169`**. **Panel resolution CONFIRMED 2026-06-14: 1920×1920** (running framebuffer + native DRM mode; the iGPU also advertises 1920×1080 fallback modes). **Still TBD:** RAM.
- [x] **GitHub repo**, created **private** (2026-06-11); goes public later.
- [x] **Content model confirmed** (2026-06-11), library+select, not replace-on-upload.

---

## 20. Build decision log

Living record of decisions taken during the build (newest first). When any of these affect user-facing behavior, the Setup Guide is updated in the same change (§16).

### 2026-07-08: Folder Collections designed (play a whole folder as the display source; §17)

Agreed the design for **Folder Collections**: an either/or **Display Source** (the Library rotation, or one selected folder played as a unit) so an owner can show a large folder of files without uploading them piece by piece. **Setup is staged in Settings** (a card built as a twin of Connected Collections: Choose folder, Name, Artist, Fit, Order), and **the source is switched in the Rotation tab** (a Source dropdown under the interval; a folder plays as configured, and nothing about it is editable from Rotation, only selected). Duration stays the one global value (§7); order is per-folder and overrides, without touching, the Library's order, so leaving folder mode restores the Library with no re-editing; Fit is per-folder; compliant files only (§6 allowlist); always live (new files fold in, removed files leave, no include toggle). **Phase A** (local: the player and the folder on the same machine, served in place, no copy or cache) is built first as the foundation; **Phase B** (the committed goal: the XXL frame plays a folder hosted on the owner's Mac, reached over the LAN and cached locally under a single **hard storage cap across all Folder Collections combined** so they can never crowd out the Library, storage frugality favored over maximum smoothness, honoring §9) follows on the same source contract. The exact reach mechanism (Bonjour/HTTP vs SMB), the cache budget, and the cache lifecycle (when a switched-off folder's bytes are reclaimed: LRU only, eager evict, a per-folder discard flag, or a manual clear) settle just before Phase B is built. True Random order was considered and deferred (Shuffle already covers random order; §7). Full spec in §17. Nothing built yet; the Setup Guide stays untouched until Phase A ships (§16).

### 2026-07-04: OpenObject 1.1.0 shipped: first signed Mac app release (Phase C4)
The Mac app is live as a notarized, self-updating `.dmg`. `release.sh 1.1.0` bumped the shared version (engine + Mac app), built Release, signed (Sparkle helpers + node + app), notarized and stapled both the app and the dmg (Apple: Accepted for each), and generated the Sparkle appcast (EdDSA-signing the dmg). A first run mislabeled the app `1.0` / build `1` because the custom Info.plist carried xcodegen's literal version defaults; fixed by referencing `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)`, and re-ran (now `1.1.0` / build `3`).

**Published identity-clean.** The GitHub Release is authored by **queueue-dev**, not Matt's personal `mattonchain`: `queueue-dev` was added as a repo collaborator (Write) and the Release created while logged in as it, so the public "released this" line shows the neutral account, matching the commit identity. (The org admin `mattonchain` still exists behind the scenes, but its membership is private and a git push is not publicly attributed, so nothing personal surfaces.) The `v1.1.0` Release carries `OpenObject-1.1.0.dmg`; the Sparkle `appcast.xml` lives on gh-pages, served at `https://openobject.io/appcast.xml` (the app's `SUFeedURL`), with its enclosure pointing at the Release asset plus the EdDSA signature.

**Note:** Sparkle compares `CFBundleVersion`, which `release.sh` increments each run (monotonic; now 3). **Files.** version bumps (`player/package.json` + lockfile, `mac-app/project.yml` + `OpenObject-Info.plist` + regenerated `project.pbxproj`), `site/appcast.xml` (new), this entry. **`installer/` untouched. Setup Guide:** the Mac app now has a real download, so its guide is Phase D. (Matt, 2026-07-04.)

### 2026-07-04: Mac app release infrastructure: dmg + Sparkle (Phase C2/C3)
Wired the two delivery pieces so a signed, self-updating release is a script away.

**The `.dmg` (`build-dmg.sh`).** Packages a signed `OpenObject.app` into a drag-to-Applications disk image (the app beside an `/Applications` alias), then signs, notarizes, and staples the image itself. Dependency-free (`hdiutil`). Packaging validated (mounts as "OpenObject" with the app + alias); the window layout is plain for now, a branded background is a later polish.

**Sparkle (C3).** Added Sparkle 2.9 via SPM (embedded framework, nothing vendored). A **Check for Updates** menu item under About is backed by an `SPUStandardUpdaterController` (`UpdaterViewModel` in `OpenObjectApp.swift`). The Info.plist is now a real committed `OpenObject-Info.plist` (not the auto-generated one) so it can carry Sparkle's keys: `SUFeedURL = https://openobject.io/appcast.xml` (the stable gh-pages URL) and `SUPublicEDKey`. Matt generated the EdDSA keypair with Sparkle's `generate_keys`; the private half lives in his keychain (same model as the notary credential), the public half is embedded. The SPM pin (`Package.resolved`) is committed for reproducible builds.

**Signing (`sign-and-notarize.sh`).** Now signs Sparkle's nested helpers (the `Downloader.xpc` / `Installer.xpc` XPC services, `Updater.app`, and `Autoupdate`), then the framework, all hardened-runtime and inner-to-outer before node and the app; without this, notarization rejects the nested code.

**Decisions.** Appcast on the site (gh-pages) at a stable URL, dmg assets on GitHub Releases. First Mac release is **1.1.0** (a minor bump for the new Mac app and the engine's per-Host rename; `v1.0.0` is already tagged and the code is at 1.0.1, so 1.0.0 is off the table). Sparkle only; App Store not pursued.

**Next (C4):** a `release.sh` (bump to 1.1.0, build, sign, notarize, dmg, sign the update, generate the appcast), then the outward publish (GitHub Release + gh-pages appcast). **Files.** `mac-app/scripts/build-dmg.sh` (new), `sign-and-notarize.sh`, `project.yml`, `OpenObject-Info.plist` (new), regenerated `project.pbxproj`, `Package.resolved` (new), `Sources/OpenObjectApp.swift`, this entry. **`player/` and `installer/` untouched. Setup Guide unchanged.** (Matt, 2026-07-04.)

### 2026-07-03: Mac app polish: stale-Host pruning, Dock-icon Settings, button margins (Phase C)
Two logged polish items plus a UX fix, all Mac-app-only.

**Prune stale Hosts (discovery).** The picker rebuilds from the live mDNS browse set, so gracefully-stopped Hosts (systemd SIGTERM, or quitting the Mac app; both send an mDNS "goodbye") already drop at once. The gap was UNgraceful departures (power-yanked or crashed), which linger until the mDNS TTL expires (a couple of minutes). `HostDiscovery` now runs a lightweight reachability sweep every ~10s: a 3s-timeout TCP probe (on a serial queue, so the timeout and the connection cannot double-resume the continuation) to each browsed Host, hiding one that fails twice in a row. It publishes the browse list minus the struck ids; a strike resets when a Host answers again or the browser drops it. Best-effort and additive; a probe never throws.

**Dock-icon Settings (Auto/Light/Dark).** The app icon can now be chosen in a proper **Settings** pane (app menu, Cmd-comma) labeled "Dock icon": **Auto** (follows the system appearance, updating live via `AppleInterfaceThemeChangedNotification`), **Light** (the default), or **Dark**. It is a runtime `NSApplication.applicationIconImage` swap, persisted in UserDefaults and re-applied on launch, so it affects only the **Dock** (and Cmd-Tab) while running; the Finder/Launchpad icon stays the bundle's white icon. macOS does not round or inset a runtime icon image, so `DockIconWhite`/`DockIconBlack` are pre-rounded copies of the app-icon masters, inset to the macOS icon grid (art at 824 of 1024) so they match neighboring Dock icons in size. An earlier iteration put this in the menu-bar menu, full-bleed (which showed oversized) and White/Black only; Matt moved it to Settings, relabeled to Auto/Light/Dark, made Light the default, and dropped the explanatory text.

**Button margins.** The window's full-width action buttons (Access &lt;Host&gt;, Open Control Panel, Open/Stop Display, Host on this Mac) gained horizontal label padding so a long Host name no longer crowds the button edge.

**Verified:** the app compiles and builds clean; the on-device checks (Dock-icon size vs neighbors, Auto following Light/Dark, the stale-Host prune) are Matt's build. **Files.** `mac-app/Sources/HostDiscovery.swift`, `AppDelegate.swift` (AppIcon + appearance observer), `OpenObjectApp.swift` (Settings scene + pane), `ContentView.swift` (button padding), `mac-app/Assets.xcassets/DockIcon{White,Black}.imageset` (new), this entry. **`player/` and `installer/` untouched. Setup Guide unchanged** (the Mac app is not documented until the `.dmg`). (Matt, 2026-07-03.)

### 2026-07-03: Per-Host renaming via a Name field in the control panel (Phase C polish)
The first Phase C polish item. An owner can now give a Host a friendly name so multiple frames do not all read "OpenObject Frame" in the Mac app picker. This is a **host-side rename**: the engine already persisted `host_name`, it just had no writer. A Mac-app-native rename was rejected (the app never authenticates to a remote Host, it only discovers and launches Chrome, so it would mean new auth plumbing); the control panel is the natural home, works for a frame too, and every client then reflects the name via Bonjour and `/api/identity`.

**Server.** `currentSettings()` now returns `hostName` (effective), `hostNameCustom` (the raw override; empty means using the default), and `hostNameDefault` (the per-machine fallback). `PUT /api/settings` accepts `hostName`, trimmed and capped at 40 chars; an empty value clears the override back to the default. On a change it calls a new module-level `readvertise()` (stops the current Bonjour advertisement and publishes a fresh one from the current identity) so discovery clients update the name without a restart. Best-effort, never throws, off the playback path; the listen path and SIGINT/SIGTERM cleanup now share the same module-level handle.

**Panel.** The Name field lives inside the **Network** card, under the reachable addresses (Matt's placement), separated by a hairline that shows only when addresses are present. Copy: "This name helps you identify this player in the app and on your network." Empty shows the machine default as the placeholder. It saves on blur/enter (conventional, no hint text), then reloads to reflect the stored value. Spacing around the divider was tuned with Matt (the paragraph's own top margin was stacking, making it lopsided; zeroed it so the line sits evenly, 16 above / 16 below).

**Verified via the preview server:** the field loads the default as placeholder; setting "Living Room Frame" persisted and re-advertised (the log confirmed the new Bonjour name); whitespace trimmed to empty reverted to the default and re-advertised; no server or console errors. **Files.** `player/server.js`, `player/public/control.html`, `control.js`, `control.css`, plus this entry and the Setup Guide. **Frame-safe:** additive and off the playback path; the frame's rotation and display are untouched. (Matt, 2026-07-03.)

### 2026-07-03: Mac app branding — real OPEN/OBJECT app icon (Phase C step 1, app icon)
The Mac app had no icon (generic macOS placeholder). Added a real **AppIcon** from Matt's official **OPEN/OBJECT wordmark**: a `mac-app/Assets.xcassets/AppIcon.appiconset` generated (via `sips`) at all macOS sizes (16 through 1024) from the **white-background 1024 master** (`assets/branding/logo_orig-padded_1410_to_1024_for_app_icon.png`), wired via `ASSETCATALOG_COMPILER_APPICON_NAME`. **Full-bleed art, no rounding baked in** — verified on-device that **macOS rounds it** into the standard squircle (Matt confirmed the Dock icon looks right). **Deployment target bumped 13 → 15** (`project.yml`, both the top-level and the target `deploymentTarget`): Matt does **not** want to support older macOS, and macOS 15 is the floor for the modern icon handling. **Correction to an earlier claim:** macOS does **not** auto-switch the app icon by light/dark system appearance — that is an iOS feature, not macOS. So the **white** icon is the single bundle icon; the **black** master (`logo_alt-padded_...`) is kept in `assets/branding/` for a **later in-app "switch icon" toggle** (a runtime Dock-icon swap via `NSApplication.applicationIconImage`, which does work on macOS). **Menu-bar mark: done** — a custom **sharp-cornered square-frame template** image (2.5pt stroke, `mac-app/Assets.xcassets/MenuBarIcon.imageset`, drawn with PIL at 1x/2x/3x), replacing the `systemImage: "photo"` placeholder; as a template it renders white on a dark menu bar and black on a light one. Stroke was tuned with Matt against an enlarged preview (2.8 too thick, 2.5 chosen). **In-app hero wordmark: done.** The onboarding window's placeholder (`Image(systemName: "photo")` plus a redundant "OpenObject" title) is replaced by the **OPEN/OBJECT wordmark** as a **template** image (`mac-app/Assets.xcassets/Wordmark.imageset`), so it tints white in dark mode and black in light mode (the app follows the system, no forced scheme). The asset is a **tight-cropped copy** of the repo's `openobject-logo.svg` (the Potrace trace): its `viewBox` is set to the measured letter bounds (`114 330 1824 1384`) so the mark fills its box instead of floating in the SVG's built-in clear-space, while the repo master keeps its clear-space untouched. Sizing and spacing were tuned with Matt to `maxWidth 220` and window margins of top 29, sides 40, bottom 30, plus a little extra under the wordmark. **Branding (Phase C step 1) is now complete** (app icon, menu-bar mark, in-app wordmark). **Files.** `mac-app/Assets.xcassets/**` (new), `mac-app/project.yml`, regenerated `mac-app/OpenObject.xcodeproj/project.pbxproj`, this entry. **`player/` and `installer/` untouched. Setup Guide unchanged** (the Mac app isn't documented until it's a real `.dmg`). (Matt, 2026-07-03.)

### 2026-07-03: Repo moved to the Queueue Studios LLC org + license changed to proprietary
The whole project now publishes under the LLC, with Matt's personal identity kept off the public face. Two changes, done together (plan: `docs/GITHUB-MIGRATION-PLAN.md`).

**GitHub org migration.** Created a Free organization **`queueue-studios`** (display name "Queueue Studios LLC"), owned and administered by Matt's personal account with **private membership** (admin, but not publicly listed), and **transferred** `mattonchain/openobject` into it. The repo is now **`queueue-studios/openobject`** (still public; GitHub keeps redirects from the old path). Every in-repo reference was rewired from `mattonchain/openobject` to the org, including `install.sh`'s `OO_ORIGIN` default, so **fresh frame installs clone from the org**; the one already-deployed frame had its git remote re-pointed by hand (`sudo -u openobject git ... remote set-url`). A dedicated attribution account **`queueue-dev`** (`developer@queueue.tv`) holds the commit identity; the repo-local git config now authors as **`Queueue Studios LLC <299584394+queueue-dev@users.noreply.github.com>`** (the account's noreply, so no personal or role email lands in commits). **No history rewrite** (Matt's call): the 47 older commits carrying his personal Gmail stay as-is, since a rewrite would change every SHA and force the frame to re-clone; everything from here is clean LLC identity.

**License changed to proprietary (all rights reserved).** Replaced the PolyForm Noncommercial license with a short **proprietary `LICENSE`**: the source stays public, but publishing it grants no license to reuse it, plus a narrow permission to download, install, run, and update OpenObject to operate your own display or frame for personal noncommercial use. Copyright holder is now **Queueue Studios LLC** (was "The Museum of Digital Art"). This tightens control ahead of paid tvOS/iPad apps, and the "free of charge" language was dropped so a paid app stays possible. Matt is acting as his own counsel for now (low risk); a lawyer blesses the exact EULA wording before charging. Swept the old "source available for noncommercial use" / "Free for noncommercial use" language everywhere user-facing: README license section + status blurb, the control panel About legal line (now "© 2026 Queueue Studios LLC · All rights reserved."), the homepage status line, `player/package.json` + lockfile `license` (now "SEE LICENSE IN LICENSE") and `author`, the Mac app's `NSHumanReadableCopyright`, CLAUDE.md, and MAC-APP-PLAN §7. **Deferred:** the homepage gh-pages republish + the openobject.io custom-domain re-verification and the `www` CNAME (still `mattonchain.github.io` in the DNS note), handled with the DNS step. **Files.** `LICENSE`, `README.md`, `player/public/control.html`, `player/package.json`, `player/package-lock.json`, `site/index.html`, `mac-app/project.yml` + `OpenObject.xcodeproj/project.pbxproj`, `CLAUDE.md`, `docs/MAC-APP-PLAN.md`, `docs/GITHUB-MIGRATION-PLAN.md`, plus the earlier URL-rewiring commit `56c775a`, this entry. **Setup Guide unchanged** (no frame behavior change; the guide does not quote the About card's copyright line). (Matt, 2026-07-03.)

### 2026-07-03: Phase C signing pipeline validated end-to-end (smoke-test); LLC Developer ID set up
**Agreed Phase C sequencing (Matt, 2026-07-02, logged here):** **0.** signing smoke-test *early* (prove the pipeline) → **1.** branding (real OPEN/OBJECT icon + wordmark) → **2.** remaining functional polish (e.g. per-Host renaming) → **3.** Phase C *release* (sign / notarize / `.dmg` / Sparkle) → **4.** Phase D docs. Rationale: the final release comes late (don't re-sign/re-notarize for every tweak; ship a *branded* app), but **before** the docs that announce the download; validate the signing plumbing early so surprises surface now. This entry is step 0.

**LLC identity set up (2026-07-03).** Matt's new **Queueue Studios LLC** org Developer account (owned by a new Apple ID `developer@queueue.tv`) was wired into Xcode and a **Developer ID Application** cert created under it. Confirmed the org attribution is correct: the cert's org field is **`O=Queueue Studios LLC`** (CN `Developer ID Application: Queueue Studios LLC (J87JRCN9RM)`, **team `J87JRCN9RM`**), vs the personal cert's `O=Matt L'Heureux (U6ZXDAF8Y9)` — so anything signed with the LLC cert is publicly attributed to the LLC, never Matt (his goal). We avoided the "wrong-identity trap" (both teams are reachable under his Apple IDs). Notarization credentials (an App Store Connect API key under the LLC) are stored as the keychain notary profile **`openobject-llc`**.

**Signing infrastructure (new).** `mac-app/signing/node.entitlements` — the bundled Node needs `com.apple.security.cs.allow-jit` + `com.apple.security.cs.allow-unsigned-executable-memory` (V8 JIT under the hardened runtime); the app itself signs with the default hardened runtime (no extra entitlements) and there are **no native binaries** in the bundle (the deps are pure JS), so `node` is the only nested Mach-O to sign. `mac-app/scripts/sign-and-notarize.sh` signs inner-to-outer (Node first with its entitlements, then the app bundle), `--options runtime --timestamp`, then notarizes (`notarytool submit --wait --keychain-profile openobject-llc`), staples, and runs the Gatekeeper assessment. Signing is done in this script (manual Developer ID flow), so the Xcode build stays unsigned and the engine-bundling post-build step is unaffected — B1b's "sign the nested node before codesign, re-sequence for Phase C" note is thus satisfied by signing the finished bundle here.

**Smoke-test result — all green (on real hardware):** a Release build was signed + notarized + stapled. **Notarization: Accepted.** `spctl --assess` → **`accepted, source=Notarized Developer ID`**; `stapler validate` → ticket present (launches offline); `codesign` shows the app and the nested `node` (with `flags=runtime`) both signed by **Queueue Studios LLC (J87JRCN9RM)**. Crucially, the **signed, hardened app runs**: in Host mode the bundled Node spawns and serves `localhost:3000/healthz`, and discovery works — the app found the frame now advertising as **"OpenObject Frame"** (confirming the 2026-07-02 rename went live after Matt updated the frame) and Viewer mode drove it. (A brief false alarm — the app appeared not to spawn Node — was just leftover *Viewer-mode* persisted state from earlier onboarding tests, which correctly runs no local host; resetting the prefs / switching to Host mode cleared it.) **New finding to remember:** the signed app (a fresh code identity) triggers the **macOS Local Network permission prompt** on first launch — because of Bonjour discovery + the LAN-bound server — which is expected and legitimate for OpenObject; the user grants it once. **Files.** `mac-app/scripts/sign-and-notarize.sh`, `mac-app/signing/node.entitlements` (new), this entry. **`player/` and `installer/` untouched.** **Setup Guide unchanged** (nothing shipped; the app isn't documented until it's a real `.dmg`, Phase C/D). (Matt, 2026-07-03.)

### 2026-07-02: Mac app onboarding/controls UX polish + frame default name → "OpenObject Frame"
A round of Mac app UX fixes from Matt reviewing the running app, plus a frame-name change that motivated them. **Button standard:** the window's action buttons are now **full-width and equal** (they were mismatched — a `.borderedProminent` button filled while a default-style one stayed label-width); all primary/secondary actions use `.borderedProminent` / `.bordered` with a full-width label. **Onboarding copy:** heading is now **"OpenObject found on your network"** (was "…is already on…"); the per-Host button is **"Access \<name\>"** (was "View \<name\>") because "View" over-promised — clicking it only *selected* the Host, so "Access" is honest and it goes to the **main window in Viewer mode** (where you then choose Open Display or Open Control Panel), not straight to full-screen. The blue primary is the Access button; the secondary is "Host OpenObject on this Mac". The onboarding **subtext was dropped** as redundant — the two buttons say the same thing (Matt, 2026-07-02). **Mode-switch de-emphasized:** in Viewer mode, switching back is now a **quiet link** ("Host OpenObject on this Mac instead", `.link` style), not a prominent button competing with Open Display / Open Control Panel — because once you've deliberately chosen to access a Host, offering the opposite as a co-equal button is odd. Copy note: avoided "your own" everywhere (both the found Host and a local one are the owner's; "on this Mac" vs "on your network" carries the real distinction). **Frame default name:** `identity.js` `defaultHostName()` for a Linux Host now returns **"OpenObject Frame"** (was "OpenObject") — descriptive and **distinct from the app name**, so a client picker reads "OpenObject Frame" vs "OpenObject on \<Mac\>" instead of the confusing "View OpenObject". This is the **friendly host_name default only** (overridable per-Host), NOT an OS setting — the Debian hostname / `openobject.local` is untouched; it takes effect on the frame's next update. **Deferred, logged:** with **multiple frames**, all would default to the same "OpenObject Frame" and today show as indistinguishable rows (distinct only by internal id); the real fix is **per-Host user renaming**, which the engine already supports storing (`host_name` is a settable persisted setting) — a UI addition for another day, and the clean answer to the multi-frame case. **Verified (on-device):** the Mac app onboarding shows "Access OpenObject" (blue, full-width) + "Host OpenObject on this Mac" (equal width); clicking Access lands on the Viewer window (Open Control Panel + Open Display + the quiet switch link), not full-screen. (Button says "OpenObject" not "OpenObject Frame" only because the frame still advertises the old name until it is updated.) **Files.** `player/src/identity.js` (frame default name), `mac-app/Sources/ContentView.swift` (button widths + onboarding copy + Access + quiet link), this entry. **Setup Guide unchanged** (frame display/control behavior identical; the frame's advertised name isn't in user docs, and the Mac app isn't documented yet). (Matt, 2026-07-02.)

### 2026-07-02: Control panel is device-aware — hides Host-inapplicable Power / Software Update
The control panel now adapts to the **Host that serves it** (where the engine runs), not the device browsing it, so the same panel shows the frame's system controls on a frame and hides them when a Mac is the Host. Keyed off server-side flags the engine already computes (Matt, 2026-07-02). **Gated (hidden unless the Host has the capability):** **Reboot** + **Shut down** need OS power (`isDevice` = `process.platform === 'linux'`, i.e. the frame); **Restart** needs a supervisor to relaunch (`supervised` = `OO_SUPERVISED`, set by the frame's systemd unit and by `supervisor.js`, not by the Mac app which runs `server.js` directly); the **Software Update** card is the **git-pull** self-updater and needs a git checkout (`/api/update` → not `not-a-git-checkout`). On the **frame** all three are true, so everything shows exactly as before; when the **Mac app is the Host** (darwin, unsupervised, non-git bundle) all four hide. **Rationale:** the Mac app updates via **Sparkle** (app-level, Phase C), not git-pull, so a git-pull card there is not just clutter but wrong; OS power and soft-restart are frame/systemd concepts. **Kept universal:** **Network** (it shows how to reach this Host, valuable on any Host, e.g. the frame-Host + Mac-browser setup the owner uses; its mDNS line already only appears where it resolves), plus Sleep + Sleep Schedule, Connected Collections, Library, Rotation, Duration/Order, Password, About, and opening `/display`. **Implementation:** `/api/system` now returns **`isDevice`** alongside `supervised`; `control.js` `loadSystem()` hides the Power rows/card by `isDevice`/`supervised`, and `loadUpdate()` hides the whole Software Update card on `not-a-git-checkout` (previously it only disabled the button). **Verified via preview:** a darwin **git-checkout** Host hid Power but kept Software Update (correct, it is git); a **non-git** darwin Host (the Mac-app case) hid **both** Power and Software Update while keeping Network + Sleep Schedule + Connected Collections + Password + About; frame-safe (all flags true on the frame → nothing hides). **Deferred, separate (on-frame):** the "Shut down leaves the panel cycling RGB" report — almost certainly the **panel's own no-signal test pattern** (the MeLE mini PC halts and drops HDMI; the separate panel shows a test pattern rather than going dark), confirmable on-device by checking the PC is unreachable after Shutdown; a true cold-off would need HDMI-CEC or a smart plug (HANDOFF §10/§17 already note a real off is the wall/plug). **Files.** `player/server.js` (`/api/system` `isDevice`), `player/public/control.js` (Power + Software Update gating), this entry. **Setup Guide unchanged** (the frame's control panel is identical; the Mac app's panel isn't documented yet). (Matt, 2026-07-02.)

### 2026-07-02: Display idle hint shows a LAN address on a Mac (not `localhost`)
The follow-up queued in the B4+B5 entry below. The idle screen's "add art at …" hint pointed at `localhost:<port>` when a Mac drives the display, which is only reachable **on that Mac**. Now, on a loopback host, the hint prefers (in order): the server's **mDNS name** (`openobject.local` on the frame), else the **first LAN address** + port (reachable from a phone or another computer), else whatever host the page was opened at (`player/public/display.js`, reading `/api/system.addresses`). **Frame-safe:** the frame sets `mdns`, so the mDNS branch still fires there and the frame's hint is unchanged (`openobject.local`); the LAN-address branch only affects loopback hosts with no mDNS (a Mac, or the Terminal dev path). **Verified via the preview server:** `/api/system` returns the Mac's LAN addresses, and `/display`'s idle hint rendered "add art at 192.168.1.34:3010" beneath the OPEN/OBJECT wordmark (also confirming the assets/logo fix in a normal browser). `display.js` is a kiosk-facing file, so applying this on the frame flags a reboot on update, but nothing about the frame's display changes. **Files.** `player/public/display.js`, this entry. **Setup Guide unchanged** (frame behavior identical). (Matt, 2026-07-02.)

### 2026-07-02: Phase B4 + B5 — real Chrome kiosk display + the controls (window, app menu, menu-bar remote)
Combined checkpoint (`MAC-APP-PLAN.md` §B4/§B5, and the 2026-07-01 decision); B4 and B5 landed together because the kiosk's navigation problem forced the controls design. **The display (B4).** A new **`DisplayController`** (`mac-app/Sources/DisplayController.swift`) launches **real Google Chrome** as a child `Process` in **kiosk mode** at a Host's `/display`, with flags mirroring the frame's `installer/kiosk/chromium-kiosk.sh` (`--kiosk`, `--no-first-run`, `--noerrdialogs`, `--disable-infobars`, `--disable-session-crashed-bubble`, `--disable-features=Translate,TranslateUI`, `--disable-pinch`, `--overscroll-history-navigation=0`, `--hide-scrollbars`, `--autoplay-policy=no-user-gesture-required`, `--disable-component-update`, `--password-store=basic`) minus the Linux/Wayland-only ones. **Real Chrome, not `WKWebView`** (Apple WebKit tile-corrupts Golden Lining etc., §20 2026-06-30; real Chrome renders identical to the frame). A **dedicated `--user-data-dir`** (`~/Library/Application Support/OpenObject/chrome-display`) isolates it from the user's own Chrome (verified untouched). Target URL: Host → `localhost:<port>/display`; Viewer → the chosen remote resolved via `NWConnection` to `http://host:port` + `/display` (the A4 same-origin contract). **Keeps the Mac awake while showing art** via an OS power assertion (`ProcessInfo.beginActivity` with display + system idle-sleep disabled), released on stop — more reliable than the web Wake Lock that was dropped (§20 2026-06-30); confirmed on-device the Mac stays awake. Stops on app quit; missing Chrome shows an "Install Google Chrome" note. **The controls (B5) — driven by a real UX failure:** a Chrome kiosk is a macOS **full-screen Space**, which hides the app's window AND makes the top-left menu bar **Chrome's**, not ours — so from inside the kiosk the app was a trap (no visible way out; Chrome's own "Exit full screen" drops to a broken windowed state). The fix puts the controls in **three coordinated places** via a shared **`DisplayActions`** (`mac-app/Sources/DisplayActions.swift`) so all three drive identical behavior: **(1)** the **window** — decluttered to two buttons (Open Control Panel + Open/Stop Display); **(2)** a top-left **"Display" app menu** (`DisplayCommands` in `OpenObjectApp.swift`) where Mac users expect app controls, shown when OpenObject is active; **(3)** the **menu-bar status item** (`MenuBarContent.swift`) — the ONE surface reachable from inside the kiosk, because the macOS menu bar reveals over any full-screen app on hover-at-top (confirmed on-device). It carries **Return to Display** (activate the kiosk's Space), **Show OpenObject** (bring the window forward), Stop/Open Display, Open Control Panel, Quit. Both menus use the **stable-and-gray convention** (all items always present; inapplicable ones disabled) rather than show/hide (Matt, 2026-07-02). Confirmed on-device: from inside the kiosk, hover-top → the OpenObject icon → **Return to Display** and **Show OpenObject** both navigate correctly; the trap is solved. **Two bugs fixed in passing.** *(a) Missing display logo:* the player serves brand assets (`/assets/branding/…`, incl. the idle- and sleep-screen OPEN/OBJECT wordmark masked from `openobject-logo.svg`) from the **repo-root `assets/`**, a sibling of `player/` that `bundle-engine.sh` never copied — so the bundled Mac app **404'd** on `/assets` and showed no wordmark. Fixed by having `bundle-engine.sh` also stage `assets/` into the bundle (`Resources/assets`, where the player's `REPO_ROOT` resolves); confirmed `/assets/branding/openobject-logo.svg` now 200s and the wordmark renders on the Mac's idle screen. *(b) Window width:* a latent B3b-2 flaw where `maxWidth: .infinity` + `.contentSize` let a long Host name stretch the window sideways; pinned to **fixed width 420** (the name wraps), restoring the compact window (height stays content-driven; verified the sizing was otherwise byte-for-byte the committed B3b-2 state). **Queued follow-up (Matt, agreed):** the display's idle hint reads "add art at **localhost:3000**", which is only reachable on that Mac; should show the Mac's **LAN address** (reachable from a phone) instead — a small shared-engine (`display.js`) tweak, frame-safe (the frame keeps `openobject.local`). **This completes Phase B4 + B5.** **Files.** `mac-app/Sources/DisplayController.swift`, `DisplayActions.swift` (new), `MenuBarContent.swift` (rewritten as the remote), `AppDelegate.swift` (own `display`/`actions`; stop on quit), `OpenObjectApp.swift` (inject; `DisplayCommands` app menu), `ContentView.swift` (2-button window + shared actions + width fix), `mac-app/scripts/bundle-engine.sh` (+ `assets/`), `mac-app/.gitignore` (`Resources/assets/`), regenerated `project.pbxproj`, this entry. **`installer/` and `player/` untouched** (assets are read from the repo root, only copied). **Setup Guide unchanged.** (Matt, 2026-07-02.)

### 2026-07-02: Phase B3b-2 — first-run onboarding + content-sized window; Phase B3 complete
Sixth Phase B checkpoint (`MAC-APP-PLAN.md` §B3/§8, the presentation): the **first-run onboarding** and the **list/window sizing** deferred from B3b-1. **Onboarding.** `RoleStore` gains a persisted **`hasChosen`** flag (set the first time the owner picks Host or Viewer). `ContentView` shows a first-run **choice card** only when there's a real choice to make: `!hasChosen && engine.hostId != nil && <another Host is present>`. The card ("OpenObject is already on your network") offers **View <that Host>** (per found Host) and **Run OpenObject on this Mac**; choosing either records the choice and dismisses it for good. A no-frame Mac with no other Host around **never sees it** and just runs as a Host (zero friction) — the choice is surfaced only when another Host (e.g. a frame) is actually discovered. During onboarding the normal status + Hosts list are hidden so the card is the focus. **Window / list sizing (Matt, 2026-07-02).** The window now **sizes to its content** (`.windowResizability(.contentSize)`): compact for one or two Hosts, taller for several, and the discovered-Hosts list **caps at ~5 rows and scrolls** beyond that (`ContentView.maxRowsBeforeScroll`) so the window never runs off-screen. Chosen over a fixed or per-Host-jittering window; and chosen over keeping the window **manually resizable** — with the connected Host's full name already shown untruncated in the top section, the earlier drag-to-widen need is largely gone, so a clean auto-sizing window (not user-resizable) is the better trade. `WindowConfigurator` no longer forces a size (it kept `isRestorable = false` + autosave-off, now moot under content sizing). **Verified (visual):** with a frame present on a fresh install the onboarding card appears and its explainer wraps correctly; choosing "Run OpenObject on this Mac" dismisses it into the normal Host view and persists (`hasChosenRole = 1`, no re-prompt); a single Host shows a compact window; seven Hosts (a real second engine on 3010 + fakes via `dns-sd -R`) grew the window and the list capped + scrolled. **This completes Phase B3** (B3a discovery, B3b-1 role model, B3b-2 onboarding + sizing). **Deferred / still open:** pruning stale mDNS rows after a Host goes offline (an mDNS-TTL nicety). **Next: B4** — drive real Chrome at the chosen Host's `/display` (the Mac actually showing art). **Files.** `mac-app/Sources/RoleStore.swift` (`hasChosen`), `ContentView.swift` (onboarding card + content-driven list/scroll + simplified `WindowConfigurator`), `OpenObjectApp.swift` (`.contentSize`), regenerated `project.pbxproj`, this entry. **`installer/` and `player/` untouched.** **Setup Guide unchanged.** (Matt, 2026-07-02.)

### 2026-07-02: Phase B3b-1 — role model + role-aware window + selectable Hosts (Host vs Viewer)
Fifth Phase B checkpoint (`MAC-APP-PLAN.md` §B3/§8, the selection *mechanism*): the Mac can now be either a **Host** (runs its own bundled engine) or a **Viewer** of a chosen remote Host it found on the network, and the choice is **persisted**. New **`RoleStore`** (`mac-app/Sources/RoleStore.swift`) holds an `AppMode` (`.host` or `.viewer(hostId, name)`) in `UserDefaults`. **`AppDelegate`** coordinates the engine's lifecycle with the mode via a Combine subscription (`@Published` replays the current value, so it also does the initial start): `.host` → `engine.start()`, `.viewer` → `engine.stop()` (no competing local server, per §8). The **window is role-aware** (`ContentView`): Host mode shows the Host status + Open Control Panel (localhost); Viewer mode shows "Viewing <name>" + Open Control Panel (the remote) + a **"Run OpenObject on this Mac"** button to switch back. The **discovered-Hosts list is now selectable and ordered**: the active/wired Host is shown **first**, as a prominent **green-checkmark, semibold label** (this Mac is also tagged "· This Mac"), and every *other* Host follows in alphabetical order as a **clickable row** that switches to viewing it (Matt, 2026-07-02 — connected-first beats plain alphabetical). Note (Matt, 2026-07-02): the active row was first rendered as a `.disabled` button, which SwiftUI grays out — inverting the visual weight so the wired Host looked dimmed; fixed by making the active row a plain label, not a disabled control. Viewer mode resolves a remote Host's Bonjour endpoint to a real `http://host:port` URL on demand via **`NWConnection`** (`HostDiscovery.resolveURL`, `nonisolated` helper for the URL build) to open its control panel; this is also the resolution B4 will use to point Chrome at the remote `/display`. **First-run default is zero-friction** `.host` — a fresh no-frame Mac just runs its own Host; the explicit first-run *presentation* + list-growth/scroll polish are **B3b-2**. **Verified (functional + visual):** using a throwaway second Host (a real bundled-engine instance on port 3010, advertised through Apple's own responder via `dns-sd -R` because two Node `bonjour-service` responders on one machine don't both reach the system resolver — a same-machine test limitation, not an app issue; a real frame on a separate machine advertises cleanly), the app listed both Hosts, clicking the remote switched to Viewer mode and stopped the local server (port 3000 down), quit+relaunch resumed Viewer mode from `UserDefaults`, and "Run OpenObject on this Mac" switched back and restarted the engine (3000 up). Screenshots confirm the corrected row styling. **Deferred to B3b-2:** first-run onboarding presentation, list growth→scroll (Matt's Q2), and pruning stale mDNS rows. **Files.** `mac-app/Sources/RoleStore.swift` (new), `AppDelegate.swift` (mode↔engine), `EngineHost.swift` (publish `hostId` from `/healthz`), `HostDiscovery.swift` (`resolveURL`), `OpenObjectApp.swift` (inject `RoleStore`), `ContentView.swift` (role-aware view + selectable rows), regenerated `project.pbxproj`, this entry. **`installer/` and `player/` untouched.** **Setup Guide unchanged.** (Matt, 2026-07-02.)

### 2026-07-02: Phase B3a — Mac app discovers Hosts on the network (native NWBrowser)
Fourth Phase B checkpoint (`MAC-APP-PLAN.md` §B3, first half): the app now **finds OpenObject Hosts on the LAN** — the Display/Control client side of the discovery seam built in Phase A2/A3. A new **`HostDiscovery`** (`mac-app/Sources/HostDiscovery.swift`) uses Apple's **native `NWBrowser`** for `_openobject._tcp` (not the Node `bonjour-service`, which is only the Host *advertising* side inside the engine) and publishes a deduped `[Host]` list, each carrying the stable **id / name / version / role read from the TXT records** plus the `NWEndpoint` (kept for B4 to resolve into a display URL). Dedupe is by the stable TXT `id`, because mDNS reports the same Host once per active interface (Wi-Fi + Ethernet) and we want one row. `AppDelegate` starts browsing on launch and stops on quit; the window shows an **"OpenObject Hosts on your network"** list (shared `HostDiscovery` via `@EnvironmentObject`). Browsing is additive and self-healing (a browser `.failed` state just resets it; no list, never a crash). **Window sizing (Matt, 2026-07-02):** the status window opens **compact by default** (420×380) but is **user-resizable** (`.windowResizability(.contentMinSize)`), so a long host name — middle-truncated in the list — can be widened to read in full; the resize is **not persisted** (a `WindowConfigurator` sets `isRestorable = false`, clears the frame autosave name, and forces the default content size on each launch, since SwiftUI autosaves the frame independently of `isRestorable`), so it always reopens compact. Verified by widening the window (the full name reveals) then quitting and relaunching (back to the compact default). **Verified (functional + visual):** with the app's own Host running, `dns-sd` sees the service on two interfaces and the app's window lists a single deduped row — "OpenObject on Matt's Mac Studio…  v1.0.1" — with name + version from the TXT record (screenshot). A second Host (an online frame, or another Mac) would appear as another row. **Next (B3b):** turn this list into the first-run role choice — "view/control an existing Host (e.g. your frame)" vs "run my own Host here" (§8) — persist it, and make launch consult it instead of always auto-starting the Host. **Files.** `mac-app/Sources/HostDiscovery.swift` (new), `AppDelegate.swift` (start/stop browsing), `OpenObjectApp.swift` + `ContentView.swift` (inject + show the list), regenerated `OpenObject.xcodeproj/project.pbxproj`, this entry. **`installer/` and `player/` untouched.** **Setup Guide unchanged.** (Matt, 2026-07-02.)

### 2026-07-02: Phase B2 — the Mac app runs the bundled engine as a managed Host (+ B6 data location)
Third Phase B checkpoint (`MAC-APP-PLAN.md` §B2): the Swift shell now actually **runs the Host**. A new **`EngineHost`** (`mac-app/Sources/EngineHost.swift`) locates the bundled `node` + `player/server.js` (staged by bundle-engine.sh, §B1b), spawns `node server.js` as a child `Process`, and polls `/healthz` until the Host answers, then reads its friendly name. An **`AppDelegate`** (via `NSApplicationDelegateAdaptor`) owns the lifecycle: **start on launch, stop cleanly on quit** — `stop()` sends SIGTERM (the server withdraws its Bonjour record and exits, A2/A3), waits briefly, then hard-kills as a last resort so quitting never hangs or orphans the node child; an unexpected child exit surfaces as a `failed` status rather than a silent death. **B6 landed here too (it's a prerequisite):** the app bundle is read-only and ships no `data/`, so `EngineHost` points **`OO_DATA_DIR` / `OO_UPLOADS_DIR` at `~/Library/Application Support/OpenObject/{data,uploads}`** (db.js already honors those env vars, §B1b) and creates them on first run — the Mac equivalent of the frame's `player/data/` + `uploads/`. The window and menu bar share one `EngineHost` (`@EnvironmentObject`) and reflect its status (idle / starting / running-with-name / failed); when running they offer **Open Control Panel** (opens the default browser at the Host). Driving Chrome for the display is B4; the fuller menu UX is B5. Port is fixed at **3000** for now (the app owns it and will hand it to the Display in B4). **Verified (functional + visual):** a Debug build launches, the app spawns the bundled node child, `/healthz` returns the real identity on `localhost:3000`, and `~/Library/Application Support/OpenObject/` is created with the SQLite DB + uploads; a screenshot confirms the window showing "Host running" + the Host name + Open Control Panel, the OpenObject app menu (Dock presence), and the `MenuBarExtra` photo icon in the menu bar (both presences, per the plan). Clean quit stops the child. (For the screenshot the unsigned Debug build was copied to `/Applications` so macOS could resolve it by name — a throwaway local install, not part of the repo or the build.) **Files.** `mac-app/Sources/EngineHost.swift` + `AppDelegate.swift` (new), `OpenObjectApp.swift` / `ContentView.swift` / `MenuBarContent.swift` (wire in the shared `EngineHost`, status UI, Open Control Panel), regenerated `OpenObject.xcodeproj/project.pbxproj`, this entry. **`installer/` and `player/` untouched.** **Setup Guide unchanged** (not shippable until Phase C). (Matt, 2026-07-02.)

### 2026-07-02: Security — bump `multer` 2.1.1 → 2.2.0 (clears two high-severity DoS advisories)
Standalone dependency fix, independent of the Mac app work (it was surfaced when `npm install` ran for the Bonjour dep, A2/A3). `npm audit` flagged **multer 1.0.0–2.1.1** with two **high-severity** advisories: **GHSA-72gw-mp4g-v24j** (DoS via deeply nested field names) and **GHSA-3p4h-7m6x-2hcm** (DoS via incomplete cleanup of aborted uploads). Fixed by bumping the upload dependency to **`^2.2.0`** — a **minor** bump within the same major, so no API changes; the upload route (`POST /api/upload`, `upload.array('files')`) is unchanged. Real-world exposure was modest (the DoS needs someone on the LAN abusing the upload form on a home frame), but it is worth clearing in a public repo. **Verified:** `npm audit` now reports **0 vulnerabilities**; a smoke test (start the player on a temp data dir, POST a real PNG) confirmed the upload still succeeds, is added to the Library, and persists to disk. This touches `player/` so the **frame benefits too**: it is a Tier-1 update (dependency bump, **no reboot**), and the self-update flow reinstalls deps (`npm ci`) automatically because the manifest changed (§15). **No version bump / release** cut here (mid-Phase-B; not a release milestone) — the fix rides the next release; version stays 1.0.1, unified. **Files.** `player/package.json` + `player/package-lock.json`, this entry. **Setup Guide unchanged** (uploads behave identically). (Matt, 2026-07-02.)

### 2026-07-02: Phase B1b — bundle the engine (pinned Node + a snapshot of `player/`) into the app
Second Phase B checkpoint (`MAC-APP-PLAN.md` §B1b): the Mac app now ships the engine **inside** each build, the key structural difference from the frame (a notarized `.app` is sealed, so it can't `git pull` a mutable checkout the way the frame does; MAC-APP-PLAN §4). A build script **`mac-app/scripts/bundle-engine.sh`** stages two things into the built app's `Contents/Resources/`: **(1)** a **pinned Node runtime** — **v24.18.0** (Node 24 LTS "Krypton"), `darwin-arm64`, chosen because it is ≥22.5 and runs `node:sqlite` **flag-free** (no `--experimental-sqlite`; db.js suppresses only the experimental-API warning in-code), downloaded from nodejs.org and **SHA256-verified against the official `SHASUMS256.txt`**, then cached in `mac-app/.node-cache/` so rebuilds don't re-fetch; **(2)** a **snapshot of `player/`** via `rsync`, **excluding `data/` and `uploads/`** (runtime data must never ship) but **including `node_modules`** (the three deps — express, multer, bonjour-service — are pure JS, so the tree is portable; a safety net runs `npm ci --omit=dev` if the source had none). **One engine, no fork** (guardrail §9): `player/` is copied at build time, never duplicated in the repo; the staged payload is gitignored (`Resources/node`, `Resources/player/`, `.node-cache/`), never committed. Wired as an XcodeGen **`postBuildScripts`** phase (always runs) so plain `xcodebuild` yields a self-contained app. **Verified end-to-end:** a clean build downloads + verifies Node, copies `player/` (~5.7 MB), and the built `.app` contains `Resources/node` (v24.18.0, arm64) + `Resources/player/server.js` + its `node_modules`, with `data/`/`uploads/` absent; running the **bundled** node on the **bundled** player served `/healthz` (200, real identity) and `/display` (200), created its SQLite DB under a temp `OO_DATA_DIR` (confirming the B6 data-relocation seam — db.js already honors `OO_DATA_DIR`/`OO_UPLOADS_DIR`), and advertised over Bonjour. (`/healthz` `commit` is `null` in the bundle, expected: no git checkout, and the Mac updates via Sparkle not git; the frame is unaffected.) **Phase C notes recorded so they are not re-derived:** for a SIGNED build the nested `node` binary must be codesigned with the hardened runtime and the bundling must happen BEFORE the codesign step (B1b runs it post-build because it is unsigned/local); the app will also need the entitlements that let a hardened app run a bundled interpreter child. **Files.** `mac-app/scripts/bundle-engine.sh` (new), `mac-app/project.yml` (postBuildScripts phase) + regenerated `OpenObject.xcodeproj/project.pbxproj`, `mac-app/.gitignore` (`.node-cache/`), this entry. **`installer/` and `player/` source untouched** (the frame path is unaffected; `player/` is only read, to copy). **Setup Guide unchanged** (not shippable until Phase C). (Matt, 2026-07-02.)

### 2026-07-02: Phase B started — Mac app skeleton (`mac-app/`), buildable Dock + menu-bar shell (B1a)
First Phase B checkpoint (`MAC-APP-PLAN.md` §B1): stand up the native Swift shell as a **buildable skeleton** — a Dock icon + window AND a menu-bar item, nothing else yet (no bundled Node, no server, no discovery; those are B1b/B2/B3). **Decisions locked this session (Matt, 2026-07-02):** **(1) Build system = a checked-in Xcode project built headlessly with `xcodebuild`** (chosen over XcodeGen-at-build-time and SPM+script), because it is the best-supported path for Developer ID signing, notarization, hardened runtime, and Sparkle in Phase C. Implementation detail: a plaintext **`mac-app/project.yml`** is the maintainable source of truth and **`xcodegen generate`** produces `OpenObject.xcodeproj`; the generated `.xcodeproj` is **also committed**, so the app builds with only `xcodebuild` and no extra tooling (regenerate after editing the yml). `xcodegen` was already installed locally. **(2) Architecture = Apple Silicon only (`arm64`) for v1** (universal Intel+arm64 is a clean Phase C expansion if reach is wanted later; no corner painted). **Other choices:** bundle id **`io.openobject.OpenObject`** (sticky — Sparkle/prefs/keychain key off it), `MARKETING_VERSION` **1.0.1** to match the engine (Phase C's release workflow will auto-sync it), min macOS **13.0** (`MenuBarExtra` needs 13), **unsigned/local** for now (the Queueue Studios LLC Developer ID + hardened runtime arrive in Phase C), `LSUIElement = NO` so the app keeps a Dock presence in addition to the menu-bar item (the plan's "both"). **Shell tech:** SwiftUI — a `WindowGroup` (Dock + window) plus a `MenuBarExtra` (menu-bar item); the menu is a placeholder (name + Quit) until the real start/stop / open-control-panel / open-display / role-status actions land in B5. **Verified:** `xcodebuild … build` succeeds; the built `.app` launches; `Info.plist` confirms bundle id, display name, version 1.0.1, `LSMinimumSystemVersion` 13.0, `LSUIElement` false, and the binary is `arm64`. (A computer-use screenshot of the running window/menu-bar was blocked by a macOS Screen-Recording permission that needs a Claude-app restart to take effect; not blocking, deferred to next session.) **Files.** `mac-app/project.yml` (new), `mac-app/OpenObject.xcodeproj/` (generated, committed), `mac-app/Sources/{OpenObjectApp,ContentView,MenuBarContent}.swift` (new), `mac-app/.gitignore` (new), this entry. **`installer/` and `player/` untouched** (the frame path is unaffected). **Setup Guide unchanged** (no user-facing behavior yet; the Mac app isn't shippable until Phase C). (Matt, 2026-07-02.)

### 2026-07-01: Phase A4 — Display-target contract documented; Phase A (engine seams) complete
Last Mac app engine-seam checkpoint (`MAC-APP-PLAN.md` §A4): let the **Display** role target a chosen Host rather than assuming localhost, without disturbing the frame. Auditing the display path (`player/public/display.js`, `display.html`) confirmed the mechanism **already exists and needs no code change**: every data read (`/api/display`, `/api/system`) and every asset (`/display.css`, `/arcade.js`, `/assets/…`, the same-origin connected-bundle mirror) is a **root-relative, same-origin URL**, so "which Host a Display shows" is decided entirely by **the URL the page is opened at**. The frame's kiosk opens `http://localhost/display` and every fetch resolves to the frame itself, **exactly as before**; the Mac app (Phase B) will open Chrome at the **chosen** Host's `/display` (its own localhost when hosting, or a discovered Host's LAN address) and the same relative fetches resolve to that Host. The only `localhost` reference in the display path is cosmetic (the idle-screen "reach me at" hint falls back to the mDNS name on a loopback host); it does not gate rendering. **Deliberate non-goal recorded:** no cross-origin `?host=` targeting (a Display served by Host A rendering Host B) — that would force CORS plus a per-Host CSP relaxation and risk the frame's zero-config kiosk, for no gain when pointing Chrome at the right URL already achieves it; the Display is always **same-origin to its Host**. A4's deliverable is therefore the **documented contract** (a header note in `display.js`) so the Mac app author and future maintainers keep every URL relative and never hardcode an origin. **This completes Phase A** (A1 role vocabulary, A2 Bonjour advertisement, A3 Host identity, A4 Display-target contract); all four are additive, off the playback path, and verified not to change the frame. Next is **Phase B** (the `mac-app/` shell). **Files.** `player/public/display.js` (contract header note), this entry. **Setup Guide unchanged** (internal seam, no user-facing behavior). (Matt, 2026-07-01.)

### 2026-07-01: Phase A2 + A3 — Host identity + Bonjour/mDNS advertisement (`_openobject._tcp`)
Second Mac app checkpoint (`MAC-APP-PLAN.md` §A2/A3), additive and off the playback path so the XXL frame is untouched. **(A3) Host identity.** A stable **`host_id`** (a UUID) and a friendly **`host_name`** now live in the `settings` key/value store (`player/src/identity.js`). The id is generated once and persisted, so it survives restarts/reboots/self-updates (the opposite of `BOOT_ID`, which stays fresh-per-process to detect a restart). The name is persisted only once the owner customizes it; until then it falls back to a **context-aware default** (Matt, 2026-07-01): the frame (Linux) is plainly **"OpenObject"**, and a Mac is **"OpenObject on \<computer name\>"** (the macOS `scutil --get ComputerName`, e.g. "OpenObject on Matt's Mac Studio"), self-disambiguating when someone runs both a frame Host and a Mac Host. A new **`GET /api/identity`** returns `{ id, name, role: "host", version, port }`; `/healthz` now carries `id` + `name` too. `/api/identity` is **auth-open** (added to `AUTH_OPEN`): it exposes only what the Bonjour TXT records already broadcast unauthenticated, so a Host stays discoverable-by-name even behind a password. **(A2) Advertisement.** On start, the Host publishes itself as **`_openobject._tcp`** with `id`/`name`/`version`/`role` in the TXT records (`player/src/discovery.js`, via **`bonjour-service`** — pure JS, no native module, no build step, chosen to honor the same revivable ethos as `node:sqlite`; the same lib later does the Mac app's browsing side). On the frame this sits **alongside** Avahi's existing `openobject.local` hostname advert (different mDNS record types: host A record vs service record), so they coexist. **Crash-proofing gotcha, recorded so it is not re-derived:** `bonjour-service`'s underlying mDNS responder **re-throws async socket errors by default** (e.g. `send EHOSTUNREACH` on a network that forbids multicast); a plain `try/catch` around `publish()` does NOT catch these because they fire asynchronously. The fix is to pass the **second constructor argument** `new Bonjour(undefined, errorCallback)` — without it, a no-multicast network would crash the player. Verified locally: with the handler, a failed multicast send just logs and the player keeps serving (identity + `/display` both 200, process stays up, clean stop on SIGTERM); the advertisement is withdrawn (`unpublishAll`) on SIGINT/SIGTERM, which does not change the frame's stop/restart semantics (the supervisor short-circuits on `stopping`). **Not verifiable from here:** this dev environment is sandboxed with no LAN/multicast, so actual on-network discoverability (and Avahi coexistence on the real frame) can't be exercised here; that is an on-device check for Matt (commands handed over separately). **Note:** `npm install` surfaced a **pre-existing** high-severity `multer` advisory (unrelated to this change; `multer` is the upload dep); it was deliberately **not** folded into this checkpoint and is flagged for a separate fix. **Files.** `player/src/identity.js` (new), `player/src/discovery.js` (new), `player/server.js` (require + `AUTH_OPEN` + `/api/identity` + `/healthz` + advertise-on-listen), `player/package.json` + `player/package-lock.json` (`bonjour-service`), this entry. **Setup Guide unchanged** (internal seam, no user-facing behavior yet). (Matt, 2026-07-01.)

### 2026-07-01: Mac app execution begun; Phase A (engine seams) started with role vocabulary (A1)
Execution of the Mac app plan (`MAC-APP-PLAN.md`, agreed earlier today, entry below) has **begun**. Work order per the plan: **Phase A** (shared engine seams, additive and off the playback path, so the XXL frame is untouched) lands first and independently, **then** the `mac-app/` shell (Phase B), then signing/notarization/delivery (Phase C). Phase A breaks into checkpoints: **A1** role vocabulary (this entry); **A2 + A3** a persistent Host identity plus Bonjour/mDNS advertisement (`_openobject._tcp`); **A4** confirm the Display-target story. **What landed here (A1):** the **Host / Display / Control** roles are now named in the engine (`player/server.js` header comment) and referenced from the code, matching §2 of the plan. This is **vocabulary only, no behavior change**: the server was already the Host (binds `0.0.0.0`, serves the API + `/display`), the control panel was already the Host-agnostic Control, and the display page was already a Display that targets whichever Host served it (`display.js` fetches same-origin). A useful consequence recorded so it is not re-derived: because the Display uses **same-origin** fetches, "which Host a Display shows" is decided by the **URL the Display is pointed at** (the Mac app will open Chrome at the chosen Host's `/display`), not by any switch inside `display.html`, so **A4 needs little or no `player/` change** and the frame's localhost kiosk is preserved for free. Next checkpoint (A2 + A3) adds a stable Host id + friendly name and the mDNS advertisement; the specific cross-platform mDNS Node dependency will be vetted against Debian + macOS (no native module, per the no-build-step ethos) and flagged for an explicit OK before it is added. **Files.** `player/server.js` (role vocabulary in the header comment), this entry. **Setup Guide unchanged** (no user-facing behavior yet; Phase A is internal seams). (Matt, 2026-07-01.)

### 2026-07-01: Mac app direction agreed (notarized `.dmg`, Swift + bundled Node); full plan in `MAC-APP-PLAN.md`
Agreed the direction to turn the Mac path (today a Terminal `npm start`, see `MAC-DISPLAY-SETUP.md`) into a **proper signed Mac app**: a notarized Developer ID **`.dmg`** (drag to Applications), positioned so a future **tvOS companion** (a Display-only client that finds a Host on the network) is a natural next step, not a rework. **This is a plan entry, nothing built yet.** The detailed brief lives in **`docs/MAC-APP-PLAN.md`**; the durable decisions, so they are not re-derived: **(1)** the XXL frame path is **untouched** (keeps Debian + kiosk + systemd + `git pull` self-update); **(2) one engine, not two forks**, `player/` stays the single source of truth and runs the same on the frame and the Mac, with a new thin `mac-app/` shell that bundles a Node binary + a copy of `player/`; **(3)** think in three roles, **Host** (serves the API + `/display`), **Display** (renders one Host full-screen), **Control** (the panel, already Host-agnostic), which is what makes the Mac app and the future Apple TV both fall out of one primitive; **(4)** shell is **native Swift + bundled Node** (not Electron/Tauri), presence is **both** a Dock icon and a menu-bar item; **(5)** the display is driven by **real Google Chrome**, NOT an in-app `WKWebView`, because `WKWebView` is Apple WebKit, the same engine that tile-corrupts Golden Lining (the two 2026-06-30 entries below), so real Chrome keeps rendering identical to the frame; **(6)** the Mac cannot update by `git pull` (a signed `.app` bundle is sealed by notarization), so it uses **Sparkle** (appcast + a new signed build per update) with the feed + `.dmg` on **GitHub Releases**, from day one and permanently since **App Store is explicitly not a goal**; **(7)** discovery is **Bonjour/mDNS at the engine level** (`_openobject._tcp`, a small cross-platform Node dep) so one code path serves both platforms and later feeds the Apple TV server-picker; **(8)** **unified versioning** off `player/package.json`; signing uses Matt's **new LLC Developer ID** (being established, not needed until the notarize step). Parked note for tvOS: a LAN-client app is not rejected merely because reviewers find no server, but needs a graceful empty state + demo/sample mode to pass Guideline 2.1. **Files.** `docs/MAC-APP-PLAN.md` (new), this entry. **Setup Guide unchanged** (nothing shipped yet). (Matt, 2026-07-01.)

### 2026-06-30: Golden Lining Safari fix, take two: pre-render the grey (`preRender`) also FAILED on iOS, reverted
After `willReadFrequently` failed on iOS (entry below), the theory sharpened (Matt): not the image size (large static images load fine) but the **per-frame full-canvas `filter(GRAY)`** that WebKit tile-corrupts, so the attempt removed the readback entirely. A per-collection `preRender` flag appended a script after the artist's sketch that overrode `draw()`: pre-grey the photo once with p5's own `GRAY` into a 2048px-capped buffer, then each frame draw grey + colour at the opacity, no `filter()`. On **Chromium it was verified pixel-identical** to the original (both crossfade endpoints, patched real bundle) and the colour stayed full-res. **On the iPhone it was WORSE:** first load showed an all-white screen with the image never appearing, and a later rotation hung on the OpenObject splash until the loop timed out and restarted at image 1. So the override broke rendering on iOS (likely the `createGraphics` grey buffer or the `clear()`-to-transparent path against WebKit memory). **Reverted in full** (`collections.js` back to its pre-`preRender` state). **Both serve-side fixes are now exhausted** (`willReadFrequently` and `preRender`); do not re-try either. **Decision (Matt): stop here.** Golden Lining renders as-is: perfect on every Chromium display (the supported surfaces), blocky on Safari/iOS, which is an accepted engine limitation. The only untried lever is downscaling the colour image, which Matt has declined (he does not want image-size changes and does not want a revised concept for the same result). **Broader lesson, reinforced:** the preview is Chromium-only, so a Chromium "pixel-identical" verification says nothing about whether a change works, or even renders, on WebKit; only Matt's Apple devices can. **Files (revert).** `player/src/collections.js`, `docs/HANDOFF.md`.

### 2026-06-30: Golden Lining gray/black on Safari/WebKit: `willReadFrequently` tried, did NOT work, reverted
Golden Lining shows gray/black tile garbage on **iPhone and Mac Safari** while rendering perfectly on the XXL. Root cause is the **browser engine**, not the screen: the sketch runs a per-frame full-canvas `filter(GRAY)` (the b&w<->colour crossfade reads the whole canvas back every frame), and WebKit tile-corrupts a GPU-backed canvas under that on a large surface; Chromium handles the same readback cleanly. The supported display surfaces are all Chromium (the XXL kiosk; the "Mac as display" path launches Google Chrome `--kiosk`), so this only bites casual Safari viewing over `http://openobject.local` and iOS (where every browser is WebKit). Matt's framing: the platform owes the artist's **visual result**, not the literal code, so a corrupted render is the unfaithful outcome and worth fixing. **Attempt (reverted):** a per-collection `cpuCanvas` flag injected a `<head>` script patching `getContext` to add `willReadFrequently: true` (force a CPU-backed, non-tiled canvas) before the sketch's canvas existed. It was verified safe on Chromium (correct injection placement, patch applied, full-canvas readback worked, identical visual/motion) and is harmless, but **on Matt's iPhone the blocky garbage still showed**, so `willReadFrequently` was not enough for that iOS WebKit. Reverted in full (the commit after `11a9f47`). **Lesson: do not re-try `willReadFrequently` for this.** The remaining option is the stronger one: replace the per-frame `filter(GRAY)` with a **pre-rendered grey image + alpha crossfade** (avoids the whole-canvas readback entirely, same visual and motion, so WebKit has nothing to tile-corrupt). Not built; Matt may pursue it later or accept that Golden Lining is Safari/iOS-only-broken (it is perfect on every Chromium display, which are the supported ones). Standing reminder: the preview is Chromium-only, so WebKit fixes can only be confirmed on Matt's actual Apple devices. **Files (revert).** `player/src/collections.js`, `docs/HANDOFF.md`.

### 2026-06-30: Version bumped to 1.0.1; a keep-awake (Screen Wake Lock) was tried, then dropped
Bumped `player/package.json` 1.0.0 → 1.0.1 so the panel's version line moves on the next update; it had stayed `1.0.0` while commits advanced past the v1.0.0 tag, which read as "did it even update?". Patch bump (the fixes batched since 1.0.0), no new tag/Release (the updater tracks `main` only now, so a bump propagates without one). Separately, a Screen Wake Lock on `/display` was added and then **removed the same day** (Matt, 2026-06-30): the only goal was keeping a **phone** awake while it shows art over `http://openobject.local`, but the Wake Lock API needs a secure context (https or `localhost`) and is absent on that non-secure origin, so it could never serve that case. The XXL already disables sleep at the OS level, and a Mac has its own display-sleep settings, so the feature met no real need. Net: `player/public/display.js` is back to no wake-lock, and OpenObject leaves each device's own power settings in charge. (If keeping a phone awake over plain http is ever wanted, the only route is an invisible looping-video trick, a hacky, battery-heavy seam, not built.) **Files.** `player/package.json` (version). **Setup Guide unchanged.**

### 2026-06-30: Self-update simplified to a single track (the dormant `releases` channel removed)
The updater carried two channels: `main` (default, tracks `origin/main`) and `releases` (advances only to the newest semver tag). The `releases` track had **no UI** (it was settable only via `PUT /api/update/channel`, which nothing called), so it was dormant machinery, but it created a real failure mode and confusion: a fix could sit on `main` while the newest tag lagged behind it, so a frame on `releases` would report "up to date" and never receive the fix (this nearly bit the 2026-06-30 display fixes, which are on `main` but not in any tag past v1.0.0). Matt's model: the updater should always track `main`; tags / GitHub Releases are a **point-in-time publishing** concept (the cited version, the Phase 2 USB image asset), not an update lane (Matt, 2026-06-30). So the channel was removed entirely: `getChannel`/`setChannel`/`CHANNELS`/`DEFAULT_CHANNEL` and the `update_channel` setting are gone, `fetchUpstream`/`resolveTarget` take no argument and always target the current branch's upstream (`@{u}` → `origin/main`), and the `channel` field is dropped from every status response. The tradeoff, recorded plainly: with no "stable" gate, a frame pulls whatever is on `main` at its next owner-initiated check, so **`main` must stay always-deployable** (only vetted commits land there, which is already the practice). This supersedes the two earlier channel entries below (2026-06-17 "toggle removed from the UI" and the original "Channel setting"); the tags themselves and GitHub Releases are untouched. **Files.** `player/src/updater.js` (channel logic removed; `db` require dropped, now unused), `player/server.js` (the `PUT /api/update/channel` route removed; check comment reworded). **Setup Guide unchanged:** it never described an update channel. **Verified on Mac:** `localStatus()` and `check()` run clean with no `channel` field, `check()` resolves `target: origin/main` and reports up-to-date correctly; both files parse.

### 2026-06-30: Add-connected modal opens with no collection pre-selected
The **Add connected artwork** dialog used to **pre-check the first collection** in the list, but that is rarely the one the owner wants, so the alphabetical first piece looked chosen by default (Matt, 2026-06-30). It now opens with **nothing selected**: the owner must pick a collection first. The Token ID field (and the "Supported Token IDs" hint) stay hidden until a collection is chosen, then appear for a normal collection (focus moving into the field) or stay hidden and auto-resolve for a single-piece `fixedToken` collection. The preview/add calls were already guarded against a null selection, so this is a panel-only change with no new failure mode. **Setup Guide unchanged:** it says "enter your Token ID" and never described a default collection. **Files.** `player/public/control.js` (`renderPicker` no longer defaults to the first slug; `syncTokenInput` hides the field while nothing is picked; the open path drops the now-moot auto-preview and focus). **Verified on Mac:** the modal opens with 0 of 12 rows checked and **Add** disabled; picking Azulejo reveals and focuses the Token ID field, picking Golden Lining keeps it hidden and auto-resolves; only one row selects at a time.

### 2026-06-30: Four display fixes from iPhone-landscape testing (wide-screen aspect contain; two stray-chrome hides)
Matt tested `/display` on an iPhone held in landscape (Safari toolbar hidden, so effectively full-screen) and surfaced four art-display issues. All four manifest only on a screen **wider than the art**; the square 1:1 frame takes the correct path in every case, so the on-frame render was never affected. Two root causes:
- **Wide-screen aspect bug (fixed two issues at once): `display.css`.** The `.layer.aspect iframe` rule (the `contain` sizing for a connected piece that declares an **aspect**, §6) set `width: 100%; height: auto; max-height: 100%`. On a screen wider than the art's ratio, `width: 100%` forced a height past the viewport, `max-height` clamped the height, but width stayed pinned at 100%, so the iframe **stretched to full screen and the ratio broke**. The 1:1 panel is never wider than these landscape pieces, so it never reached that clamp, which is why the frame looked perfect. Two visible symptoms: **Chromie Squiggle** rendered too tall with too little white margin; **Golden Lining** ran its p5 canvas at the wrong, oversized shape, and because that sketch never clears its background and the canvas then exceeded the photo, a phone showed gray rectangular garbage tiles and (under iOS canvas-memory pressure) an all-black frame. Fix: size the iframe to the largest box of `--ar` that fits **inside** the stage, each axis capped by `min()` against both the stage and what the other axis allows, so the ratio holds on any viewport (the square frame result is byte-identical to before). Uses `dvw`/`dvh` (visible-viewport units) so a phone's address bar cannot oversize it; on the toolbar-less frame these equal the old behavior. **Verified on Mac** by measuring the iframe box at a square 900×900 (dune 900×719, chromie 900×600, identical before and after) and a wide 1280×600 (dune was 1280×600 / ratio 2.13, now 751×600 / 1.25; chromie was 2.13, now 900×600 / 1.5), and by rendering the real Golden Lining bundle wide: the p5 canvas is now 750×600 (1.25), the photo fills it, no gray or black artifact regions. No console/CSS-parse errors.
- **Two stray-chrome hides (mirror step): `collections.js`.** Both reuse the existing `hideSelectors` seam (the rule is baked into the bundle at mirror time, §8). (1) **Golden Lining** gets `hideSelectors: ['#p5_loading']`, hiding p5's built-in loading screen that flashed during the crossfade into this heavy piece (the same fix already shipped for Lost in Moffat County). (2) **Azulejo Galo** gets `hideSelectors: ['#menu-hover']`: its bundle carries a hover/tap export menu (Save PNG / Print Sheet / Toggle Rotation) meant for standalone desktop use, which popped over the art on a tap (mobile) or corner hover (desktop); `display: none` takes it out of both hover and tap, while the programmatic `toggleRotation()` animate path is untouched. Controls belong in the panel's Connected Collections, not on the zero-chrome stage (§6). Only Azulejo ships such a menu. The faithful desktop archival bundles deliberately keep this menu (it is a useful local export feature); the hide is a serve-side display accommodation only, so no archive bundle is rebuilt.
- **Mirror-bake caveat.** The `display.css` fix is live everywhere the moment it ships (no re-mirror), so **Chromie Squiggle** and **Golden Lining**'s sizing both correct themselves at once. The two `collections.js` hides only take effect on a fresh mirror, so an already-added piece needs a **remove + re-add** to pick them up: that re-mirror set is **Golden Lining and Azulejo** (Chromie is not in it). On the frame the same applies after the OTA update reaches it.
- **Files.** `player/public/display.css` (the `.layer.aspect iframe` contain rule), `player/src/collections.js` (the two `hideSelectors` entries). **Setup Guide unchanged:** it is XXL-frame (square) only, where none of this changes behavior; the **Mac display guide** (`MAC-DISPLAY-SETUP.md`) describes setup, not per-piece rendering, so nothing in it is contradicted. (Matt, 2026-06-30.)

### 2026-06-26: First observed player hang (alive but unresponsive); watchdog is the candidate fix
First time the control panel became unreachable on the real frame. **Symptom:** Safari could not load the panel by name (`openobject.local`) or by IP (both hung, did not error), while **the art kept playing** on the panel. A power cycle (unplug/replug, firmware auto-power-on) restored it.
- **Finding: a silent hang, not a crash.** `sudo journalctl -u openobject-player` showed the running process (commit `12e37cf` / v1.0.0, started 2026-06-25 21:01) logged "listening" and then **nothing for ~21 hours**, with no `Deactivated`, no `Scheduled restart`, and no new PID, until the manual power cycle (2026-06-26 18:29). So the Node process stayed **alive but deaf**: it stopped answering HTTP without exiting. The display kept looping because Chromium already held the loaded art client-side; only fresh server requests (the panel, and the display's own polling) hung.
- **Why it could not self-recover.** systemd's auto-restart only fires when a process *exits*. A hang is not an exit, so the restart never triggered and a physical replug was required. (This violates the next-stranded-owner goal: a non-technical owner should not have to power-cycle a wedged frame.)
- **Cause not yet known.** Nothing was logged to stdout/stderr at the time of the hang, so the cause is not in the journal. One soft lead only: memory peaks on the *completed* runs climbed with runtime (32M, 70M, 169.8M on a ~3-hour run), across different code versions, so a slow leak over a 21-hour run is plausible but unproven. Persistent journaling is confirmed on (journal back to 2026-06-14), so a recurrence will be captured across the reboot.
- **If it recurs, capture live state BEFORE replugging** (SSH in as `mattlhx@openobject.local`): `systemctl status openobject-player`, `ps -o pid,%cpu,%mem,etime,rss -C node`, `curl -v --max-time 5 http://localhost/`. That distinguishes CPU-pegged vs memory-bloated vs simply deaf, which points at the cause.
- **Candidate fix (not built, n=1):** a liveness watchdog that periodically curls `http://localhost/` and restarts `openobject-player` if it does not answer within a timeout. This slots into the existing **`openobject-netcheck.timer`** pattern (the Wi-Fi rejoin watchdog, §20 2026-06-17) and is a Tier-2 change (systemd unit, re-run `installer/install.sh`). Held off pending a recurrence or an identified root cause. **Docs-only entry; no code or Setup-Guide change.** (Matt, 2026-06-26.)

### 2026-06-25: Add-connected dialog: auto-resolving Token ID, focus trap + Esc, Cancel/"Add"; Cancel-left standard; quieter keyboard focus ring
A pass over the **Add connected artwork** modal to make it conventional enough to need no instructions (the question that started it: "what key do I press to look the Token ID up?"). Five related, panel-only changes:
- **The Token ID resolves itself.** It used to look up only on Enter or when the field lost focus, with nothing on screen saying so. It now resolves automatically a short moment after you stop typing (a 500ms debounce in `control.js`); editing the number drops the previous preview at once (`resetResolve`), and **Enter** still resolves immediately rather than waiting. No key to learn, no "look up" button. The old blur/`change` trigger is removed; the debounce covers tabbing away.
- **Focus stays in the dialog.** The modal had no focus management, so Tab could walk off the field and out of the page; in Safari that lands in the toolbar and pops the Favorites view (the actual cause of the original complaint). A focus trap now wraps Tab/Shift+Tab around the dialog's own controls, and **Esc** closes it, matching the in-place title/artist editor. Focus lands in the Token ID field on open.
- **A Cancel button, and a shorter primary.** The footer gained a **Cancel** (its only prior exits were the ✕ and a backdrop click), and the primary **"Add to library"** is now just **"Add"** (the destination is obvious inside a dialog titled "Add connected artwork"). The footer is split: Cancel far-left, Add far-right.
- **Cancel-left standardized.** With the dialog using Cancel-left / primary-right (the macOS convention), the Library title/artist editor was flipped from **Save | Cancel** to **Cancel | Save** to match (the divider border moved from `.meta-save` to `.meta-cancel`). The rule is now consistent: dialogs put Cancel/secondary on the left, the primary on the right. Card action toolbars are a separate pattern and unchanged (Pin/Fit on the left, the destructive Delete kept last, set apart so it is harder to hit by accident).
- **Quieter, keyboard-only focus ring.** Buttons showed the browser's default focus ring (a bright glow in Safari). They now use the same quiet lightened edge the text inputs use (`#4a4a52`), via `:focus-visible` so it appears only when navigating by keyboard, not on mouse clicks. Built from brightness, not a hue, so it reads regardless of color. Card action buttons take an **inset** ring because cards clip their content (`overflow: hidden`). It is an objectively subtle ~2:1 step against the panel and can be lifted to ~`#6a6a72` (~3:1) later if it proves too faint to track.
- **Files.** `player/public/control.html` (Cancel button + `cxBox` id, "Add" label), `control.css` (`button:focus-visible` ring + `.actions` inset override, `.cx-foot` split, `.meta` divider move), `control.js` (debounced auto-resolve, the trap + Esc handler, the editor's button order). **Setup Guide unchanged:** it never described the dialog's buttons, keys, button order, or focus styling, and its wording ("enter your Token ID", "then Save (or press Enter)") stays accurate.
- **Verified on Mac** (Phase 1 preview): typing a Token ID fires the lookup only after the ~500ms pause (nothing at 60ms, "Looking up…" by ~700ms), editing clears the prior result instantly, and Enter resolves at once; the dialog traps Tab/Shift+Tab (wrapping ✕ to Token to Cancel/Add) so focus never leaves it, and Esc and Cancel both close it; the footer reads Cancel (left) / Add (right) and the Library editor reads Cancel (left) / Save (right); the `:focus-visible` rules load as `2px solid #4a4a52`, inset on `.actions`; no console errors. (Matt, 2026-06-25.)

### 2026-06-25: Rename the Settings "Wi-Fi" card to "Network"
The Settings card was labelled **Wi-Fi**, but in Phase 1 its only content is **how to reach the control panel** (the live LAN address plus `openobject.local` on the frame), a network *address*, not a Wi-Fi setting, so the label over-promised: tapping "Wi-Fi" suggested joining or switching a wireless network. Renamed the visible label to **Network**, which (a) describes today's reach-address content, (b) matches this spec's own §11 title ("Network & first-run onboarding"), (c) covers the officially-supported **wired Ethernet** option (§11), which "Wi-Fi" would mislabel, and (d) still fits once Phase 2 folds the real Wi-Fi onboarding (the OpenObject-Setup AP + captive page) into this same card. It is a **label-only** change: the word "Wi-Fi" is kept everywhere it genuinely means joining the home wireless network (the Setup Guide's install step and troubleshooting, "set the frame's Wi-Fi while installing it"), and the internal ids stay `wifiCard`/`wifiToggle`/`wifiBody` (the card's Phase-2 job is Wi-Fi onboarding; renaming the ids/collapse wiring would be churn and breakage risk for no user benefit, the common display-label-≠-id pattern).
- **Files.** `player/public/control.html` (the `section-title` label, and the card's lead comment), this doc (§11 stub note, §12 collapsible-cards list), Setup Guide (the Settings overview, the collapsible-cards line, and the "Reaching the frame" heading + body). Historical §20 entries that say "Wi-Fi card" are left as the record of that date.
- **Verified on Mac** (Phase 1 preview): the Settings card header now reads **Network**, still collapses/expands, and still shows "Reach this panel from another device at …"; no console errors. (Matt, 2026-06-25.)

### 2026-06-25: Rotation rows show the artist on the second line; the format/Connected becomes a pill after the title
Each Rotation row's smaller second line used to carry the file's format (`JPEG`, `MP4 · Fill`) or the word `Connected`. It now carries the **artist** instead, and that type indicator moves up to a small **pill after the title**. The artist is sourced exactly like the Library card's subtitle: a connected piece reads its **collection registry artist** (`collectionsBySlug[...].artist`, the same value the server's `effArtist` uses for the Artist sort), an upload reads its **owner-set `artist`** field. It is shown as the bare name, **no "by " prefix**, and the line is **omitted entirely when there is no artist** (most uploads), so the row collapses to a single, vertically-centered title rather than leaving a blank line.
- **The pill is the format, or "Connected" for connected pieces** (the teal-accented `rot-pill-connected`, matching the Library card's thumbnail badge `badge-connected`), restyled for the panel's dark meta row. The format is lower-case in the data and the pill uppercases it in CSS (`svg` -> `SVG`). The per-clip **Fill** marker that used to ride along in the old second line (`· Fill`) is kept as its own second pill, shown only when the clip center-crops, so no information is lost from the row.
- **The title (first line) is unchanged: still `original_name`.** This matters for a connected piece whose *official* title embeds the artist, e.g. Bryan Brinkman's *"Azulejo Galo by Bryan Brinkman #101"*: the full official title still reads on line 1 and the artist line below it reads just "Bryan Brinkman". The artist is taken from the registry, never parsed out of the title, so the embedded "by Bryan Brinkman" is left alone and not treated as a duplicate. (Uploads still show their filename on line 1 here, not their custom title, as before; only the second line and the pill changed.)
- **Files.** `player/public/control.js` (`rotRow`: artist + `typePill`/`fillPill`, restructured `.rot-name`), `control.css` (`.rot-name` now a flex row, new `.rot-title` ellipsis, `.rot-pill`/`.rot-pill-connected`). Setup Guide unchanged: it never described the row's two lines, so it stays accurate.
- **Verified on Mac** (Phase 1, the dev rotation of 12: connected pieces + uploads): connected rows show the registry artist (Bryan Brinkman, Erick Calderon (Snowfro), Alex Kittoe, …) under a `CONNECTED` pill; *Azulejo Galo by Bryan Brinkman #101* keeps its full title with "Bryan Brinkman" below; uploads show a `PNG` pill and no second line, and a Fill upload shows `PNG` + `FILL`; the title still truncates with the pills held to the right; no console errors. (Matt, 2026-06-25.)

### 2026-06-25: Pruned §17 (Future enhancements) to empty
Cleared the Future-enhancements list. With Connected Collections (§8) and SVG (§6) shipped and the remaining ideas dropped, nothing was left worth carrying as "to build later," so §17 keeps only its header and a short placeholder (it stays because it is cross-referenced from §6 and §8).
- **Dropped the on-chain / NFT source seam** (a generic "pick a chain, contract, and token ID, sourced live from the blockchain" resolver, the possible future for "live pieces"). It would lean on a third-party resolver API and possible fees, and a live remote source cuts against the local-first, no-external-dependency design (§9). Curated Connected Collections already resolve on-chain at add time and then mirror locally, which is the path kept.
- **Dropped the adjustable-crop-position seam.** v1 stays center-crop only; the §6 note that pointed here was trimmed to match.
- **Dropped the "paste any hosted URL and name it yourself" sub-item** (same local-first and untrusted-remote-code concerns).
- **Removed the two shipped records** (Web/HTML art, now Connected Collections in §8; SVG support in §6). Their build notes stay in this log, so §17 is no longer also a done-list.
- (Matt, 2026-06-25.)

### 2026-06-25: Seed the sample piece OUT of the Rotation (a fresh install boots to the idle screen, not the sample)
The first-run seed (the Bouncing OpenObject Logo, 2026-06-24 below) added the sample as a normal connected row **in the Rotation**, so a brand-new install came up playing it. That pre-empted the branded idle screen (the logo with the "add art" hint), which is what a new owner should see first: the player invites them to add art rather than auto-playing a sample. The sample is still seeded (still in the Library, still bundled offline, still bottom-anchored in the grid), but now **dormant** (`in_rotation = 0`). With nothing in the Rotation, `display.js` already falls back to the idle screen (it shows the idle mark whenever the rotation is empty), so the boot experience is the welcome screen, and the owner opts the sample into the Rotation (or adds their own art) when they want playback.
- **Mechanism.** `db.addConnectedItem` gained an optional `in_rotation` (defaults to 1, so an ordinary connected add is unchanged); `seed.js` passes `in_rotation: 0`. `position` is still computed but dormant for an out-of-rotation row (ignored until the piece is added, when `setLibraryRotation` recomputes it from the end), so nothing else shifts. No migration: this only affects a fresh seed, and an install already seeded (the `sample_seeded` flag set) keeps whatever rotation state the owner has since chosen.
- **Verified on Mac** (Phase 1, throwaway temp DB via `OO_DATA_DIR`): a fresh seed lands the sample in the Library (one connected row, `in_rotation = 0`) but leaves the Rotation **empty** (so the display shows the idle screen), a re-seed does not duplicate (library 1 / rotation 0), and opting the piece into the Rotation from the panel plays it normally. (Matt, 2026-06-25.)

### 2026-06-25: Rename "Blank screen" to Sleep / Wake, and make Wake suppress the schedule until the next window
"Blank screen" and the Sleep Schedule were already the same display state (one `asleep` signal; §13), so the manual header toggle is renamed **Sleep / Wake** to share the schedule's vocabulary and read like a standard device control. The button now reflects the **live** state (the server's `asleep`), not just the manual flag: **Sleep** while the art shows, **Wake** (the amber `aria-pressed` highlight) while the panel is asleep for any reason, so it stays honest even during a scheduled sleep.
- **Wake means it.** Pressing Wake during a scheduled sleep window can't just clear the manual blank (the schedule would re-sleep it on the next ~5 s poll), so a second override was added: a `wake_until` timestamp set to `nextSleepStart(now)` (the next moment any window begins, scanning the week ahead). `isAsleep` is now: manual Sleep (`manualBlank`) forces asleep; else if `now < wakeUntil` the schedule is suppressed (awake); else the schedule decides. So Wake keeps the art on for the rest of the current window and the schedule resumes on its own at the next one (the "stay up tonight, normal tomorrow" model). Editing the schedule clears `wake_until`, so new windows take effect at once. Manual Sleep stays indefinite (off until woken); the asymmetry is intentional (off can wait for you; on shouldn't silently disable the schedule).
- **No new UI verbiage** (Matt's call): the button is two words, the verbose tooltip was dropped, and the only status text is the existing Sleep-card line, now "Sleeping now" for a manual sleep and "Awake until <next sleep>" during a wake-override (replacing "Blanked now").
- **Plumbing.** `wakeUntil` and the derived `asleep` are surfaced in `currentSettings` / `/api/settings`; the `manualBlank` field on the settings PUT now carries the Sleep/Wake intent (true = sleep, clears any override; false = wake, computes `wake_until` only if a window is live now). `server.js` (`scheduledAsleep` / `nextSleepStart` / the reworked `isAsleep`, the PUT branch, the display reads `settings.asleep`), `control.js` (live-state button via `applySleepState`, `renderSleepStatus`, `toggleBlank` posts the opposite of the live state), `control.html` (button text, tooltip removed). Internal names (`blankBtn`, `manual_blank`) kept. Docs (§13, Setup Guide).
- **Verified on Mac** (Phase 1, isolated preview): from awake, Sleep gave button "Wake" (amber) with `asleep` and the display both true and "Sleeping now"; Wake with no schedule returned to awake with no override; a daily all-day window showed "Wake" by schedule, then Wake set `wakeUntil` to tomorrow 00:00 with `asleep`/display false and "Awake until 12:00am"; editing the schedule cleared the override; no console errors. (Matt, 2026-06-25.)

### 2026-06-25: Library view filter (Show: All / In rotation), persisted, declutter without deleting
As a Library grows, the owner may want to see only what is actually playing without deleting the rest. So the Library tab gains a **Show** filter beside the Sort: **All** (default) or **In rotation**, which renders only the pieces with the rotation checkmark and hides the others. It is a **view filter, not a state on each piece**: nothing is deleted or moved, rotation membership is untouched, and switching Show back to All brings every hidden piece back instantly. Chosen over a per-item "hidden" flag (the Connected Collections Hide/Unhide-all model) because the stated need was "hide *all* the unchecked ones to declutter", which a single reversible view toggle serves with no schema change, no per-card button, and no "hidden but in rotation" ambiguity.
- **Persisted like the sort, applied in the browser.** The choice round-trips as a server setting (`library_filter`, surfaced as `libraryFilter` in `/api/settings` and `currentSettings`, validated against `all|rotation` with a 400 otherwise), the same shared-setting model as `librarySort` so the view holds across reloads and devices. Unlike the sort it is **not** applied in `/api/library`: the server still returns the whole ordered list (sample anchored last) and `control.js#loadLibrary` narrows it with `items.filter(it => it.in_rotation)`. The filter is trivial, so this avoids a second query branch and, deliberately, keeps the **Library tab count as the whole library** (how much art you have) while only the grid narrows. The Rotation tab already shows the in-rotation count.
- **Placement (no grid shift).** The Sort `<select>` was wrapped with the new Show `<select>` in a right-aligned `.lib-view` group on the tab row (renamed from the lone `.lib-sort`), shown only on the Library tab. Same zero-vertical-height-when-it-fits behavior as before; on a very narrow phone the group's two controls wrap onto two lines (`flex-wrap` on `.lib-view`).
- **Empty state.** When the filter hides everything but the Library is non-empty, the grid's empty line reads "No pieces in the rotation yet. Switch Show to All to add some." instead of the misleading "add some art above"; a truly empty Library still shows the add prompt.
- **Files.** `player/server.js` (`LIBRARY_FILTERS` set; `libraryFilter` persisted + validated in `/api/settings`; surfaced in `currentSettings`), `player/public/control.html` (the `.lib-view` wrapper + the Show `<select>`), `control.css` (`.lib-view`/`.lib-ctrl` replacing `.lib-sort`), `control.js` (load/apply/persist the choice; filter in `loadLibrary`; hide the group off the Library tab), docs (§7, Setup Guide).

### 2026-06-24: Rename the "Name" sort to "Title"; prefill the title field with the filename
Two small follow-ups to the title/artist work, from Matt using it:
- **"Name" sort renamed to "Title", key and all.** Beside the new Artist sort, "Name" read as confusing (art has a Title and an Artist), so the Sort option is now **Title**, and its internal key/value was renamed `name` to `title` to match (the data column was already `title`; only the sort key lagged). The key first shipped as `name` (4a5bc26), so `initDb` migrates a persisted `library_sort='name'` to `'title'` once, and the API no longer accepts `name`. The in-card edit field was already labelled Title, so "Name" is now gone everywhere, label and code.
- **Title field prefills the filename.** Opening the editor on an untitled piece used to show an empty field with the filename only as a placeholder, which vanished on focus, losing a useful reminder of what to type. Now the title input is prefilled with the existing title when set, else the **filename**, and fully selected (`focus()` + `select()`), so you can type to overwrite it or arrow through it to read it first. It is a default, not forced: clearing the field and saving still stores NULL (the card falls back to the filename), so nothing is locked to the filename and the Title/Artist sorts are unaffected.
- **Files.** `player/public/control.html` (option value + label), `player/public/control.js` (prefill), `player/src/db.js` (sort key `name`→`title` + persisted-value migration), `player/server.js` (valid-set message), docs (§7, Setup Guide).
- **Verified on Mac** (preview): the dropdown shows Title and sorts by title as before and persists; opening an untitled piece prefills and highlights the filename, typing replaces it, and clearing then saving reverts to the filename; no console errors. (Matt, 2026-06-24.)

### 2026-06-24: Library "Artist" sort (by the artist shown on the card; un-credited last)
Added a fourth Library sort, **Artist**, alongside Recent/Oldest/Name. It orders by the artist **shown on each card** A to Z, then un-credited pieces, with the install sample still anchored absolute-last. "Shown on the card" matters because the two artist sources differ: an uploaded piece's artist is the owner-set `artist` column, while a connected piece's artist comes from the **registry** (`src/collections.js`), not the DB row. So this is the one sort that **does not live in `db.listLibrary`** (which only sees columns): `server.js#libraryByArtist` builds on the name-sorted list (sample already last, title/id tiebreak baked in) and re-sorts it with a stable sort, resolving the effective artist per row (`collections.bySlug(collection).artist` for connected, else the column). 
- **Nulls last (Matt 2026-06-24): "place null values at the end of any sort pattern."** Un-credited pieces (uploads with no artist) sort after every credited one, tiebroken among themselves by the displayed title; within a shared artist, pieces also tiebreak by title. The sample is forced last *before* the null rule, so even though its registry artist resolves to "OpenObject" it never sorts up among the credited.
- **Plumbing.** `librarySort` already round-trips as a persisted setting; the valid set is now `recent|oldest|name|artist` (`server.js`, validated on the settings PUT). The control panel just gains the `<option value="artist">Artist</option>`; the client is otherwise unchanged (it sends the value and re-fetches). `db.INSTALL_SAMPLE_COLLECTION` is now exported so the server's sort uses the same anchor slug as the DB.
- **Files.** `player/server.js` (`libraryByArtist`, the `/api/library` branch, the widened valid set), `player/src/db.js` (export the sample-slug constant), `player/public/control.html` (the Artist option).
- **Verified on Mac** (Phase 1, isolated preview, five pieces): Artist sort produced credited-first by artist (Dana Lee, then Jane Doe), then the un-credited (apple, solo-shot) by title, then the connected sample last despite its registry artist; the choice persisted across a reload; `artist` is accepted (200) and a bogus value rejected (400); no console errors. (Matt, 2026-06-24.)

### 2026-06-24: Editable title + artist on uploaded pieces (tap the card's name; panel-only)
Uploaded pieces showed only their filename and had no artist, while connected pieces show a real title/artist from the chain/registry. Owners can now set an optional **title** and **artist** on an uploaded piece. The whole name/artist block on the Library card is the tap target: tapping it swaps that block for two inputs and the actions row for Save/Cancel, edited **in place** (no modal). Enter saves, Esc/Cancel discards, and a blank field clears to NULL so the card falls back to the filename / file size. A titled upload then renders exactly like a connected piece (the title, with the artist name beneath), so the grid is visually consistent.
- **Affordance (Matt's call).** No always-visible pencil or Edit button, to spare the title's width; the block itself is the target. On a pointer device it shows a pointer cursor and a faint hover tint (zero added height); on touch it is reveal-on-tap, documented in the Setup Guide. Tapping anywhere on the name/artist area opens the editor.
- **Single-file nicety.** Dropping exactly one file opens its new card straight into edit mode with the title focused (placeholder = the filename), so a one-off nudges naming; a multi-file drop adds silently with no per-file prompt (the multi-file friction is why an upload-time prompt was considered and dropped in favour of edit-after).
- **Panel-only (zero-chrome preserved).** Title/artist are control-panel organization metadata; nothing is shown over the art on the display (§6). The **Name** sort now keys on the displayed title (`COALESCE(NULLIF(title, ''), original_name) COLLATE NOCASE`), so titled pieces alphabetize by their title, and the install sample still anchors to the bottom.
- **Data + API.** Two nullable columns `title`/`artist` on `library` (added in place, idempotent). The existing `PATCH /api/library/:id` gains `title`/`artist` (trimmed; blank clears to NULL; capped at 200 chars; rejected for connected pieces with a 400, keeping them authoritative). `original_name` is never overwritten, so the source filename is preserved (shown as the card's hover tooltip) and uploads stay byte-for-byte.
- **Files.** `player/src/db.js` (the `title`/`artist` columns, `setLibraryMeta`, the Name-sort key), `player/server.js` (PATCH validation + connected guard), `player/public/control.js` (`card` display, `enterEditMeta`, the single-file open), `player/public/control.css` (editable affordance + edit inputs/buttons).
- **Verified on Mac** (Phase 1, isolated preview): set title+artist on an upload (leading/trailing whitespace trimmed); the card showed the title + "by artist" with the filename kept in the hover tooltip; it persisted across a reload; the Name sort re-alphabetized by the new title (a file named `Zebra.png` titled "Aardvark Dawn" led the list) with the sample still last; a blank title cleared back to the filename while keeping the artist; a connected piece had no tap target and its PATCH returned 400; a >200-char value returned 400; a single-file upload opened straight into edit mode focused; Esc discarded an edit; clicking either the title or the artist field focuses that field with no bounce, and spaces type normally (the card's tap-to-edit handler is guarded against re-firing once editing); no console errors. (Matt, 2026-06-24.)

### 2026-06-24: Sortable Library grid (Recent / Oldest / Name), persisted, sample stays anchored
The Library grid had one fixed order (newest first). As a library grows that is limiting, so the Library tab now carries a **Sort** control and the owner can order the grid by **Recent** (newest first, the default), **Oldest**, or **Name** (A to Z, case-insensitive). The choice is a **persisted server setting** (`library_sort`, surfaced as `librarySort` in `/api/settings` and `currentSettings`), so it sticks across reloads and across devices until changed, the same model as the rotation duration/order (a deliberate choice over per-browser `localStorage`: the panel is shared and the library is one shared thing, so "the Library is sorted by X" should hold wherever it is opened). Every order keeps the **install sample (the Bouncing OpenObject Logo) anchored to the bottom**: `db.listLibrary(sort)` keeps the `(collection = sample) ASC` primary sort key from the anchor change above and only varies the secondary key, so the sample sorts last in all three. The ORDER BY fragments are a fixed allowlist keyed by the sort name (never interpolated from request input), and the server validates `librarySort` against it (HTTP 400 otherwise), so there is no SQL-injection surface and a stale/unknown value falls back to the default.
- **Placement (no grid shift).** The control sits on the **tab row itself** (right-aligned, after Library/Rotation/Settings), shown only on the Library tab. On any width where the tabs and the sort fit one line it adds **no vertical height** (the `.tabrow` height equals the tab height), so the grid never moves down, the explicit ask. On a narrow phone where they cannot share a line it wraps to its own compact right-aligned line below the tabs (the lone case it shifts the grid, mobile-only) instead of overflowing or clipping; `.tab` gained `white-space: nowrap` so a tab's count never drops under its label. No new CSS breakpoint: `flex-wrap` + `margin-left:auto` give inline-when-it-fits, wrap-when-it-doesn't on their own.
- **"Name" sorts on `original_name`** (the displayed title: the uploaded filename for files, the derived title for connected pieces), so it is correct today and forward-compatible. A planned enhancement will let uploaded pieces carry an optional custom **Artist/Title** (today uploads fall back to showing the filename, while connected pieces already show a real title/artist); when that lands, switch the Name key to `COALESCE(title, original_name) COLLATE NOCASE` and the bottom-anchoring still holds. Noted in `db.js` so the change is obvious.
- **Files.** `player/src/db.js` (`LIBRARY_SORTS` allowlist, `DEFAULT_LIBRARY_SORT`, `listLibrary(sort)`), `player/server.js` (`/api/library` reads the persisted sort; `librarySort` validated + persisted in `/api/settings`; surfaced in `currentSettings`), `player/public/control.html` (the `.tabrow` wrapper + the Sort `<select>`), `control.css` (the row + select styling, wrap-on-narrow, tab `nowrap`), `control.js` (load/apply/persist the choice; hide the control off the Library tab).
- **Verified on Mac** (Phase 1, isolated preview, four pieces: three uploads named so the orders differ, `Zebra`/`apple`/`Mango` uploaded in that order, plus the sample): Recent gives Mango/apple/Zebra, Oldest gives Zebra/apple/Mango, Name gives apple/Mango/Zebra (lower-case `apple` first confirms case-insensitive), and the sample sorts last in all three; the choice persists across a reload; an invalid value is rejected (400); the control shows only on Library and the row height equals the tab height (zero grid shift inline); at 375px it wraps to its own line with no horizontal overflow; no console errors. (Matt, 2026-06-24.)

### 2026-06-24: Seed the sample piece into every fresh install (the Bouncing OpenObject Logo, bundled offline)
A fresh Library now starts with one piece, OpenObject's *"Bouncing OpenObject Logo"*, so a new install greets the owner with art out of the box. It is seeded on first boot from a bundle SHIPPED IN THE REPO (`player/seed/bouncing-openobject-logo/`: the mirrored `index.html`, a downscaled 512px thumbnail, and a `seed.json` manifest), not fetched, so the install is self-contained and needs no network for the default piece (mission: depends on no external service). `src/seed.js` (`seedSampleIfNeeded`, called from `server.js` right after `db.initDb()`) copies the bundle into the runtime collections dir and adds a normal connected Library row; from then on it behaves like any piece (Rotation, Pin, and the bottom-of-Library sort anchor all apply). It is gated by a `sample_seeded` setting so it runs once: an install that already has the piece (a frame where it was added by hand) is adopted without duplicating, and a piece the owner later deletes is never re-seeded. Why bundle rather than seed over the network: a default piece should appear reliably for every new owner, including offline and on a flaky gateway (we saw IPFS 504s), and shipping our own CC-licensed brand sample (~96KB) is the brand-asset side of the "art never touches the repo" rule (which targets user uploads and runtime data), like the committed logo. Trade-off and maintenance: the committed bundle is a snapshot, so if `BOUNCE_HOOK` or the mirror step changes, regenerate `player/seed/` by re-mirroring the piece. Verified on a wiped DB: fresh boot seeds it (in rotation, plays on the display, no network, no console errors), a re-boot does not duplicate, a deleted sample is not re-seeded, and an already-present copy is adopted. (Matt, 2026-06-24.)

### 2026-06-24: Hide p5.js's loading screen on Lost in Moffat County (#p5_loading)
During the crossfade into Jeremy Booth's *"Lost in Moffat County"*, a small purple "loading..." flashed mid-stage. Traced to p5.js's built-in loading screen (`#p5_loading`, which the artist restyled): p5 shows it while a sketch's `preload()` is still fetching assets, and this piece preloads heavy day/night photos. The display reveals a connected piece on the iframe's document `load`, which fires before p5 finishes those image loads, so the loading screen was on screen as the piece faded in over the outgoing one. It is the artist's element (not OpenObject's, and unrelated to the "Click to Bloom" gate), surfaced by the reveal timing. Hidden the same way as The Bloom's overlay: the entry now carries `hideSelectors: ['#p5_loading']`, so `mirrorInto` injects `<style>#p5_loading{display:none!important}</style>` into the bundle's `<head>` at mirror time, keeping the zero-chrome stage (§6). Trade-off: a brief blank while the photo finishes loading replaces the text. Mirror-time injection, so an existing copy must be removed and re-added to pick it up (an add always re-mirrors). Verified by re-mirroring token 4 (the hide rule is in the bundle's head). (Matt, 2026-06-24.)

### 2026-06-24: The install sample (Bouncing OpenObject Logo) anchors to the bottom of the Library
The Library grid sorts newest first (`id DESC`). The Bouncing OpenObject Logo is the install sample (seeded into every fresh Library by the follow-on seed step, and on the bench frame added by hand to an existing library), and it should read as the original piece that "came with the install": at the **bottom** of the grid, beneath the owner's own pieces, regardless of when it was added. So `listLibrary` now anchors it there with `ORDER BY (COALESCE(collection,'') = 'bouncing-openobject-logo') ASC, id DESC` (non-sample pieces newest first, the sample row last). This is **sort-only** and scoped to the Library grid (`/api/library`): the card is otherwise identical to any other piece (no special row, badge, or handling), and nothing in playback, the Rotation, or Pin is touched. A piece deleted and later re-added returns to the bottom, by design. The slug lives in one constant, `INSTALL_SAMPLE_COLLECTION`. The user-facing description ("a sample piece comes with your player") lands with the seed step, since that is when a fresh install actually has the sample; this change alone is a no-op on a library that has no sample. Verified locally: with two uploads and the sample added between them, the sample sorts last despite not holding the highest id. (Matt, 2026-06-24.)

### 2026-06-24: Free the off-screen layer after a crossfade (no hidden iframe rendering behind the visible piece)
The display keeps two stacked layers to crossfade between pieces, but left the outgoing layer in the DOM at `opacity:0` after the fade. An `opacity:0` iframe keeps animating (the browser only stops that work for `display:none`), so a previously shown piece, or the piece that was on screen when another was pinned, kept rendering invisibly behind the visible one and competed for the GPU. On the frame's weak integrated graphics that surfaced as choppiness in the foreground piece, most visible on a DOM/CSS-animated piece like the Bouncing OpenObject Logo. A clean reboot, which leaves no ghost behind a pinned piece, was smooth, which is how the cause was isolated. `display.js` now clears the off-screen layer once a crossfade has settled (`freeHiddenLayer`, with a timestamp guard so it never clears during the fade), and `apply()` calls it for a lone/pinned piece so a ghost left from before a pin is dropped too. The next `show()` repopulates the layer exactly as before, so nothing visible changes and the crossfade is untouched. Helps every connected piece, not just the sample. `display.js` is kiosk-facing, so the frame picks it up by self-update (the §15 reboot reminder fires). Verified on an isolated preview (a planted element in the hidden layer is cleared within a poll while the visible piece stays untouched, no console errors) and on the frame in rotation (smooth). (Matt, 2026-06-24.)

### 2026-06-24: Twelfth connected collection (OpenObject, "Bouncing OpenObject Logo") + a general multi-control model
Added the twelfth supported Connected Collection (§8), and the first authored by the project itself: OpenObject's *"Bouncing OpenObject Logo"* (Ethereum ERC-1155 contract `0xa6279e1bcc8dfab0fef4e556d1cf9d6eee864de7`, token 1), the OPEN/OBJECT wordmark drifting DVD-screensaver style across a black field, recolouring at each wall hit and bursting on a corner strike. It is also intended to become the **sample piece seeded into every fresh Library** (a follow-on change), so it is deliberately our own work, on-chain like any other connected piece, and added by the exact same path the owner uses.
- **Resolve trail.** ERC-1155, so resolution reads `uri(1)` (not `tokenURI`, which reverts on this contract), the Golden Lining path; the entry carries `erc1155: true` and a `fixedToken: '1'` (the whole collection is this single piece, so the add flow skips the Token-ID prompt, still resolving on-chain for faithfulness). The metadata's `animation_url` (`ipfs://.../media`) is stored **verbatim** and its `image` (`ipfs://.../thumbnail`, a 2048² PNG) cached as the Library thumbnail. The artwork is a **fully self-contained** HTML file (an inline SVG wordmark plus inline CSS and JS, no external assets), so the mirror is a clean one-file copy (only the ipfs.io `cdn-cgi` beacon is stripped, as for every gateway-fetched bundle). It self-animates and fills the square stage edge to edge (size is a % of the viewport's short side, so it reads the same on the 1:1 frame and a widescreen Mac), so no `crop`, no `aspect`, no Animate control.
- **General `controls` model (new).** The piece exposes three of its own settings (Speed 1..12 default 2.5, Size 5..40% default 16, and a Corner effect select of Confetti/Flash/Pulse/Shockwave/None default Confetti), which is two sliders plus a select, more than the single-control `speedControl`/`choice` shorthands can hold. So a collection may now declare a **`controls`** array of range/select descriptors (`{key, type, label, default, ...}`), rendered in order in the Settings card, stored per collection as JSON in a new **`collection_state.controls`** column (`mergedControls` clamps ranges and validates selects over the registry defaults), passed to the display as **`?oo_<key>=value`** params, and applied at display time by a per-collection **`controlHook`**. For this piece `BOUNCE_HOOK` sets the artwork's three top-level `let` globals (`speed`, `sizePct`, `cornerMode`) directly, the Chromie Squiggle pattern (a classic injected `<script>` shares the document's top-level scope). The model is **additive**: the eleven existing collections keep their `speedControl`/`choice` paths unchanged, and `controls` is the general form any future multi-control piece uses.
- **Files.** `player/src/collections.js` (registry entry, `BOUNCE_HOOK`, `controlsOf`/`mergedControls`, `getState`/`setState`/`list`), `player/src/db.js` (the `controls` column plus read/write), `player/server.js` (PATCH validation and the `withConnectedFlags` decoration), `player/public/display.js` (signature plus `?oo_<key>` params), `player/public/control.js` and `control.css` (the Settings sliders/select plus wiring).
- **Verified on Mac** (Phase 1, isolated preview): the piece resolves via `uri(1)`, mirrors to a clean one-file bundle with the hook injected and the beacon stripped, adds as a normal Library row, shows Speed/Size/Corner in Settings, and renders on `/display` with `?oo_speed`/`oo_size`/`oo_corner` applied end to end (pushing Size to 40% grew the rendered mark from 16% to 40% of the short side); no console errors. (Matt, 2026-06-24.)

### 2026-06-22: arcade.js counts as kiosk-facing (the reboot reminder now fires for arcade changes)
`isKioskFacingPath` (the §15 check that decides whether a self-update needs a frame reboot) matched only `player/public/display.*` and `installer/kiosk/`. But the Retro Arcade demo's `player/public/arcade.js` is loaded by the same kiosk `/display` page, so an arcade-only update (such as the never-die fix `a20278e`) needs a kiosk reload too, yet showed no reboot reminder. Widened the path test to `player/public/(display|arcade).*`, so any future arcade change flags the reboot. Server-side only (`updater.js`); no display or behaviour change, so it reaches the frame by ordinary self-update with no reboot of its own. (Matt, 2026-06-22.)

### 2026-06-22: Hidden "Retro Arcade" attract-mode demo (an easter egg)
A purely-fun easter egg for the display: a self-playing, non-playable "attract mode" homage to early-1980s fixed-shooter arcade games, hidden behind a key sequence and shown on `/display` in place of the art. Browser-side (Phase 1), no hardware dependency.
- **Homage notice (also at the top of `player/public/arcade.js`).** A trivial, non-playable feature (a self-playing demo, no controls), built as an homage to an era, with stylistic inspiration commonplace in 1980s arcade gaming. Not affiliated with, and reproducing no assets, names, or trademarks of, any specific game or company; every sprite, the bitmap font, the logo, and all code are original. Clean-room only: no ROM, no emulator, no external assets, no network. Two words are deliberately absent from the entire product (code, files, routes, UI, commit text): the name of the specific game it evokes, and the name of the famous cheat sequence.
- **Not in the user docs, by design.** Unlike every other user-facing feature, this is intentionally omitted from `SETUP-GUIDE.md`, the README, and `MAC-DISPLAY-SETUP.md`: it is a hidden surprise, and documenting it defeats it. This is the one sanctioned exception to the §16 lockstep rule, and this engineering note is the record.
- **What it is.** `player/public/arcade.js` is a self-contained vanilla `<canvas>` demo (`window.RetroArcade = { start, stop }`): an insectoid swarm streams in along curved Bezier paths, holds formation, and dive-bombs a self-playing fighter over a parallax starfield under an "OPENOBJECT" bitmap marquee. The fighter never dies: a global one-shot fire throttle plus a predictive dodge (it moves to the x furthest from every predicted bullet crossing and every imminent body crossing) mean nothing ever lands, so it runs forever with no death and no loop seam. The score moves, as in a real attract mode.
- **Square playfield, starfield wings.** The field is authored as a 1:1 logical square, centred and scaled to the screen's shorter side. On the square frame it fills edge to edge; on a wide screen (a Mac display) the square centres and the side margins carry the same starfield, so the action is never cropped or stretched (it reads as an arcade cabinet, not a letterbox). It re-flows on resize, and branches on the measured screen shape, never on the OS.
- **Trigger and exit.** The classic cheat-code key sequence (Up, Up, Down, Down, Left, Right, Left, Right, B, A), captured on the control panel (`control.js`, ignored while typing in a field), flips the display into the demo. The only control is a single **"Return to art"** button that appears in the control panel's top bar while it runs; clicking it returns to the art rotation. Nothing on the panel names or acknowledges the demo, and the display stays chrome-free (a button never appears on the art screen).
- **Runtime-only flag (always wakes to art).** On/off is an in-memory `retroArcade` flag in `server.js` (PUT/DELETE `/api/arcade`, surfaced in `/api/display` and `currentSettings()`), never persisted, so a reboot always returns to art (a frame must never wake into the demo).
- **A findable hint.** Settings > About carries the trigger sequence as invisible text after the license line (`.about-hint`, painted in the card's own `--panel` colour, `aria-hidden`), revealed only by manually drag-selecting the line (plain text in the card's exact background colour, same font and size as the licence line, no special selection styling, so it surfaces just like any other selected text). A discovery path for someone using the panel; the trigger is in the source regardless.
- **Developer flips (no UI).** Two in-code constants tune the look with no user control: `BLAST` ('dust' pixel-burst default, or 'ring' the original expanding circle) and `scheme` (marquee palette, 0 Emerald default, plus Sunburst/Nebula/Ice).
- **Crisp pixels.** Marquee and HUD use a hand-authored 5x7 bitmap font drawn as hard `fillRect` blocks (like the sprites), not a downscaled typeface (an earlier offscreen-buffer downscale looked blurry and was abandoned).
- **Files.** New: `player/public/arcade.js`. Touched: `server.js` (flag + routes), `display.html` / `display.js` / `display.css` (canvas + enter/exit, mirroring Sleep), `control.html` / `control.js` / `control.css` (key sequence, Return-to-art button, About hint).
- **Verified on Mac** (Phase 1, isolated preview): the sequence flips `/display` into the demo and Return to art flips it back; swarm, dives, never-die, pixel-dust explosion, and bitmap marquee render with no console errors; the About hint is invisible until the line is selected, then legible. Display-facing files changed (`display.*`), so on the frame the kiosk must reload (a power-cycle) to pick it up, not just a player restart. (Matt, 2026-06-22.)
- **Still open (optional, not built):** a capture-beam + dual-fighter signature sequence; and two unused helpers from an earlier logo attempt (`ribbon()`, `drawSparkle()`) remain in `arcade.js` pending a decision to remove.

### 2026-06-20: Connected Collections rows go responsive (controls stack on phones, inline on desktop)
The Connected Collections list in Settings decided each row's layout by **control count**: a row with one control rendered it inline beside the collection name, and only the multi-control Chromie Squiggle (a `cc-multi` class) dropped its controls to a second line. On a phone that was backwards. A single inline control (Code art's Snow dropdown, Dune Reveries' Motion slider) squeezed the name into a narrow sliver beside it, while the name was what needed the width, so those rows looked broken. The axis that actually matters is screen width, not control count.
- **Fix (CSS, width-driven).** `control.css` now lays out every row the same way and keys off width. Default (narrow, what phones get): the `.cc-controls` group drops to its own right-aligned row 2, so the name keeps the full width on row 1 with Hide; an inner `flex-wrap` lets a wide group (the squiggle's slider plus dropdown) split across lines on a very narrow phone. A `@media (min-width: 860px)` query pulls the controls back inline beside the name for laptops and desktops. 860px clears the widest row (the squiggle needs about 720px of content), and because the panel itself caps at 980px every row flips together at that point. A row with no controls hides its empty `.cc-controls` wrapper as before, leaving a plain name plus Hide.
- **`cc-multi` removed.** With layout keyed on width, the one-versus-many distinction is gone: `control.js` no longer adds the `cc-multi` class and the matching CSS is dropped. No change to which controls render, what they do, or where they live (Settings, with Fit/Fill still on the Library card).
- **Verified on Mac (isolated preview) at three widths.** Phone (375px) stacks every control row cleanly with Hide on row 1 and zero horizontal overflow (Code art and Dune Reveries now show their full name on one line, the rough case that prompted this); the 860px breakpoint flips the widest row (Chromie) to one line with no overflow; desktop (1180px) shows every row on one line, controls in a tidy column before Hide (single-control rows unchanged, Chromie now on one row as asked). SETUP-GUIDE unchanged: it describes what each control does and that they live in Settings, never their row layout, so it already matches the shipped behavior. Presentation only (`control.css` plus `control.js`), no display-facing change, so it reaches the frame by ordinary self-update with no kiosk reboot.

### 2026-06-20: An add always rebuilds the bundle (a render fix now reaches an existing piece on re-add)
`mirrorBundle` skipped re-mirroring when a bundle already existed on disk, an offline optimisation with a sharp edge: a render fix to the mirror step (a hook, the new `hideSelectors` overlay-hide, `dropScripts`) never reached a bundle that was already mirrored. Updating the player and re-adding the piece reused the **stale** bundle, so the fix looked like it had not worked. This bit the overlay fix below: a frame still carried The Bloom's pre-fix bundle (orphaned on disk, the piece not even in the Library, see the reclaim entry below), and re-adding reused it, so the "Click to Bloom" flash persisted until the bundle was actually rebuilt.
- **Fix.** Drop the skip-if-present shortcut, so an add **always re-mirrors**: removing and re-adding a connected piece is now a reliable way to rebuild it with the current code. `mirrorInto` overwrites in place and writes `index.html` last, so a live shared-collection sibling never sees a missing bundle, and `mirrorBundle` is only ever called from the add flow (never on boot), so the re-fetch stays bounded to an explicit add.
- **Failure stays safe.** The mid-mirror cleanup is now guarded: only a **brand-new** dir is dropped on a failed fetch; an existing bundle is left intact (its old copy still plays), so a failed re-mirror can never destroy a working piece.
- **Verified on Mac:** a fresh mirror carries the fix; corrupting the on-disk `index.html` then re-adding rebuilds it (fix restored, corruption gone); and a re-mirror pointed at a dead source throws but leaves the existing bundle in place. SETUP-GUIDE gains a one-line "remove and re-add to rebuild" tip (the only user-facing effect; the rebuild itself is invisible).

### 2026-06-20: Deleting a connected piece reclaims its bundle (no more stale bundles on disk)
Deleting a connected piece left its mirrored render bundle on disk. `removeBundle` only reclaimed a **perToken** bundle (`collections/<slug>/<tokenId>/`, e.g. send/receive); a **shared** bundle (`collections/<slug>/`, used by most collections, where one bundle serves every token and the per-piece seed rides in the URL query) was kept unconditionally, because there was no way to tell whether another piece still used it, so it lingered forever. Regular uploads were already cleaned up (the delete route unlinks the file from `uploads/`); only connected shared bundles accumulated. Three had already orphaned on the test machine (chazstract, chromie-squiggle, perfect-circles).
- **Fix (reference count).** The delete route deletes the library row before calling `removeBundle`, so `db.countConnected(slug)` then reflects the pieces that remain. `removeBundle` still drops a perToken token dir, and now, when the count is **0** (the collection's last piece is gone), removes the whole `collections/<slug>/` dir, reclaiming the shared bundle, any perToken leftovers, and the cached thumbs in one stroke. Deleting one of several pieces in a collection leaves the bundle in place (count > 0); deleting the last one reclaims it. No registry or display change.
- **One-time sweep.** The three pre-existing orphans were removed by calling the patched `removeBundle` for each (each collection had 0 library rows, so the count-0 path reclaimed it), which also live-tested the fix; a collection still in the library (perfect-everything) was confirmed untouched. SETUP-GUIDE unchanged: deletion already "removes the piece," and freeing its storage is invisible housekeeping (the low-space prompt just has more to reclaim now).

### 2026-06-20: The Bloom's "Click to Bloom" overlay no longer flashes on load (new `hideSelectors` mirror seam)
Chaz Wesley's *"The Bloom"* (§8, the ninth collection) briefly showed its full-screen **"Click to Bloom"** gate on every display before the garden appeared. The gate (`#startOverlay`) is static markup in the artist's HTML, so it painted as soon as the body parsed; the `BLOOM_HOOK` that dismisses it (by firing the sketch's `startExperience()`) can only run once p5 has loaded and `setup()` has created the canvas, and even then the overlay fades over the artist's own `transition: opacity 0.8s`. So the gate was visible for the boot gap plus ~0.8s, a brief flash on a passive frame that can never click it. The hook was working; the overlay simply outlived it.
- **Fix: a reusable `hideSelectors` seam.** A collection may list CSS selectors whose nodes are pure interaction chrome on a passive frame; at mirror time `mirrorInto` injects a `<style>sel{display:none!important}</style>` into the bundle's `<head>`, so the node never paints. The Bloom sets `hideSelectors: ['#startOverlay']`. The `BLOOM_HOOK` is unchanged and still fires `startExperience()` (its real job is to reveal the garden and start the bloom clock); hiding the already-doomed overlay just removes the flash. The rule must be in `<head>` to beat the first paint, which is why this is a static mirror-time injection and not part of the end-of-body hook (SNOW_HOOK hides Binary Mountains' chrome the same way but can do it from JS, because those nodes are created after load).
- **Registry/mirror-only**, no `display.js`/`.css`/`control.js` change, so it reaches the frame by ordinary self-update with no kiosk power-cycle; an already-mirrored bundle must be re-mirrored (re-add, or delete its dir) to pick up the head `<style>`. The desktop archival copy is the verbatim artist HTML and keeps the overlay (mirror-only, like the hooks).
- **Verified on Mac** (Phase 1, isolated data dir): token 28 re-mirrors with `<style>#startOverlay{display:none!important}</style>` in `<head>`; on `/display` the garden appears immediately with no "Click to Bloom" frame at any point, still silent on its 16:9 canvas. SETUP-GUIDE unchanged: it describes the visible art and never mentioned the gate, so it already matches the shipped behavior.

### 2026-06-20: Eleventh connected collection (Snowfro, "Chromie Squiggle"): first multi-control connected piece (speed + background + Fit/Fill)
Added the eleventh supported Connected Collection (§8): Erick Calderon (Snowfro)'s *"Chromie Squiggle"* (Ethereum, the shared Art Blocks Core contract `0x059edd72cd353df5106d2b9cc5ab83a52287ac3a`), the OG Art Blocks piece, project 0. It reuses `perToken` (the generator inlines each token's unique hash into a different HTML per token, so each mirrors its own bundle, like send/receive, but with no live state, so it is fully offline) and `localizeAbsolute` (its one absolute asset, p5.js 1.0.0 from cdnjs, is pulled local). Three new parts:
- **`requireProject` guard.** The Core contract hosts hundreds of unrelated projects under one address, so accepting a raw Token ID would resolve other artworks. `resolveToken` now rejects a token whose metadata `project_id` is not the registry's `requireProject` ("isn't a Chromie Squiggle"), so the whole ~10k Squiggle series is supported (the owner enters their Token ID, not a `fixedToken`, the Pendulum lesson) while non-Squiggle ids on the same contract are turned away. Other collections leave `requireProject` unset and are unaffected.
- **Controls that drive the artwork's own globals, via one combined hook (`speedHook: 'squiggle'`).** The squiggle exposes three top-level `let` bindings: `loops` (colour cycle on/off, the artist's click), `speed` (rate, the arrow keys), and `backgroundIndex` (which `backgroundArray` shade to paint, the spacebar). A classic `<script>` injected into the same document shares that top-level scope, so the new combined `SQUIGGLE_HOOK` sets them directly from the display's params: `?oospeed` drives `loops`/`speed` (0 = the static as-minted still; default **1** = the official piece's own gentle `speed`), and `?oochoice` sets `backgroundIndex`. This is the first speed control to drive the sketch's own state rather than a `window` property (Golden Lining's cosine drives `window.opacity`); the slider plumbing is otherwise reused unchanged, and the `0 = off` point replaces a separate Animate switch.
- **Background choice + first multi-control connected piece.** A White/Black **Background** `choice` (the same dropdown mechanism as Binary Mountains' Snow) maps to `backgroundArray` index 0/10, applied by the combined hook above (so no separate `choiceHook`). The squiggle is the first connected piece to carry more than one control, so `control.js`'s Settings rendering changed from a mutually-exclusive `if/else` to a list: it now shows the speed slider **and** the background dropdown (in that order), while single-control collections render exactly as before. `list()` / `withConnectedFlags` / `display.js` already passed `speed` and `choice` independently, so no change was needed there.
- **Per-piece Fit/Fill on a connected piece (`fitFill`).** Connected pieces had Fit/Fill off (each bundle sizes itself); the squiggle opts in. It declares a 3:2 `aspect` (metadata `aspect_ratio` 1.5), so **Fit** letterboxes it faithfully over the artist's white field (the canonical look) and **Fill** sizes the iframe to cover the stage with the layer clipping the overflow (the iframe equivalent of `object-fit: cover`). `control.js` shows the Fit/Fill button on the card for a `fitFill` collection (it was hidden for connected before) and its thumbnail honours fill; `display.css` adds the `.layer.aspect.fit-fill` cover rule; and `display.js`'s connected signature now includes `fit` so a flip re-renders live (a missing-`fit` sig for connected pieces, fixed here).
Decisions (Matt, 2026-06-20, who wanted this special piece fully kitted out): support the whole series with a project guard (not just his token); a speed slider where 0 = off, defaulting to the official piece's `speed` of 1; a White/Black background option (white default); and a real Fit/Fill option, embracing a multi-control panel for this one piece. Code: `collections.js` (registry entry with `speedControl`/`speedDefault: 1`/`choice`/`fitFill`/`requireProject`, the combined `SQUIGGLE_HOOK`, hook selector, the project guard in `resolveToken`, `fitFill` in `list`), `control.js` (multiple controls per collection in Settings + Fit/Fill on `fitFill` cards), `display.css` (aspect cover rule), `display.js` (connected sig includes `fit`). Verified on Mac (isolated preview): #9756 resolves and the guard rejects projects 1 and 2; the mirror localizes p5, inlines the hash, and injects the combined hook; on `/display` the speed slider runs 0 (frozen) through the default 1 to fast, the background switches White/Black (`backgroundIndex` 0/10, invalid choices rejected), and Fit/Fill switches between the 3:2 letterbox and an edge-to-edge cover-crop; the card shows the Fit button and Settings shows the Motion slider and the Background dropdown together. Display-facing files changed (`display.css`/`display.js`), so the frame needs a reboot to pick them up, not just a player restart (cf. the reboot-flag entry below).

### 2026-06-20: Update view auto-reloads the panel + flags reboot-needing updates
Built the two halves of the §15 plan parked 2026-06-18. (1) After a successful self-update, the control panel **reloads itself** once `/healthz` confirms the new commit (`waitForRestart`), so a control-panel change (e.g. the Snow dropdown, the "Supported Token IDs" hint) is visible without a manual refresh. The earlier staleness was NOT a cache problem (assets are served `max-age=0` + ETag, so a reload already revalidates to fresh code); the page just never reloaded, only the version line did, so this supersedes the parked cache-busting idea (no versioned URLs needed, nothing to bust). (2) `updater.check()` now sets a **`requiresReboot`** flag when the incoming diff touches the kiosk display (`player/public/display.*`) or launcher (`installer/kiosk/`) (`isKioskFacingPath`); the *What's in it* summary shows a reboot line, and after applying such an update the panel keeps the reminder visible (no auto-reload) so the owner reboots the frame (the player restart does not reload the kiosk Chromium). Code-only: `updater.js` (flag + helper), `control.js` (reload-or-remind branch + `updateNeedsReboot`), `control.css` (the line). Verified on Mac (render path + the reboot-vs-reload branch; the live reload fires on a real update). (Matt, 2026-06-20.)

### 2026-06-20: Add modal shows "Supported Token IDs"
The add-connected modal now shows an always-visible **"Supported Token IDs"** line for any collection restricted to specific tokens: a `fixedToken` collection's single id (e.g. Code art → 17) or a curated subset via a new registry **`supportedTokens`** field (Lost in Moffat County → 3, 4, 5, 6, since tokens 1 "Desert Steel" and 7 "Resolute" are static stills with no `animation_url`). Open collections show nothing. Purely informational, to set expectations (Matt's idea, 2026-06-20); independent of the add/resolve/error path. `list()` surfaces the ids (the `fixedToken` value, the `supportedTokens` list, or null); `control.js`'s `syncTokenInput` renders the hint (a small green info glyph + the ids) under the Token ID field, or above the auto-preview for single-piece collections whose field is hidden. Verified on Mac across open / subset / single-token collections. Code-only (`collections.js` registry+`list`, `control.html`/`.js`/`.css`); no DB or display change, so it reaches the frame by ordinary self-update (the kiosk display page is untouched, but the control panel must reload to pick up the new `control.js`/`.css`).

### 2026-06-20: Time-bound the Connected Collections fetches (no more hung adds)
Every outbound fetch in the resolve/mirror path (`collections.js`) now goes through `ooFetch`, a thin wrapper that applies a flat **30-second** `AbortSignal.timeout` (built into Node, no dependency) and turns a stall into a clean, surfaced error instead of an indefinite hang. Why: the RPC node and the IPFS/Arweave gateways can *connect but never answer*, and Node's `fetch` has no default timeout, so a dead source would hang the add/preview forever (the spinner that never returns, the original symptom while testing a Lost in Moffat County token whose metadata sits behind a bare-CID URL). Decisions (Matt, 2026-06-20): one **flat 30s** ceiling on every call (the small RPC/metadata reads and the large asset downloads alike, simplest, and it only bites when something is genuinely stuck; 30s is a failure ceiling, not an expected wait); **hardcoded**, no env knob; and on a mid-mirror failure the **partial bundle dir is removed** (`mirrorBundle` wraps the work and cleans up) so a retry starts fresh. Covered call sites: the `eth_call` (`ethCallTokenURI`), the metadata read (`readTokenMeta`, both the Ethereum and Tezos/TzKT branches), the bundle entry + relative assets + `localizeAbsolute` downloads, the cached thumbnail (`cacheThumb`), and the add-preview data URL (`toDataUrl`). Verified on Mac: a hanging local server trips the 30s ceiling with a friendly "Timed out reaching …" error and the partial dir is cleaned, while normal adds are unaffected. Code-only change in `collections.js` (no DB, UI, or display change); the Setup Guide is unchanged (internal robustness, the only user-visible difference is a clear error in place of a hang).

### 2026-06-20: Tenth connected collection (NFTman76, "Binary Mountains") + a reusable choice control
Added the tenth supported Connected Collection (§8): NFTman76's *"Binary Mountains"* (Ethereum ERC-721 contract `0xe2fe3818c305dfc2d2b9b4646bee95c050b0baf3`), token 17 of the artist's general *"Code art"* contract, a self-contained three.js scene (a wireframe mountain range orbited by a drifting camera, under falling snow). It reuses `fixedToken` (a grab-bag contract, one vetted token, resolved on-chain via `tokenURI`) and `localizeAbsolute` (the page loads three.js by absolute cdnjs URL; that one script is pulled local and the ref rewritten for offline play). The scene self-animates, so there is no Animate control. The new parts are a **reusable single-choice control** and (added the same day) a **landscape `aspect`**:
- **`choice` control.** The artwork's snowfall intensity is a tap-cycled global `snowLevel` (0..3). Rather than a boolean Animate or a 0..10 speed, the registry entry declares a `choice` descriptor (`{ label, default, options: [{ value, label }] }`); we expose the artist's full set, `Snow`: Light(0)/Moderate(1)/Heavy(2)/Blizzard(3). The owner picks from a **dropdown** in Settings, stored in a new `collection_state.choice` TEXT column (NULL = registry default) and validated server-side against the declared option values. `getState`/`list` surface it; the display appends `?oochoice=<value>` to the iframe src.
- **`SNOW_HOOK` (`choiceHook: 'snow'`).** `snowLevel` is a lexical `let`, not a window property, so it cannot be assigned from an injected script, but the artist's click handler is a global function. The hook reads `?oochoice=N` and calls that handler N times once it exists (snowLevel starts at 0, so N taps land on level N, the faithful equivalent of the artist's own tap). It also hides the artwork's two on-screen overlays (the "tap to snow" prompt and the level pill), interaction chrome meaningless on a passive, zero-chrome frame (§6).
- **Landscape `aspect: '16 / 9'` (added 2026-06-20, Matt's call).** On the real frame the filled square read as a stretched-looking crop. The scene is responsive (no inherent shape) and the artist's export is near-square (804×760), so rather than fill the square it now declares a 16:9 `aspect` and the display letterboxes it as a landscape image (the Golden Lining / The Bloom path; black bars top and bottom on the 1:1 panel, full-screen on a 16:9 display). The `SNOW_HOOK` also nudges a resize a few times after load (`dispatchEvent(new Event('resize'))`) so the responsive WebGL canvas re-fits to the final box and can't latch onto a stale, stretched size. There is no single "official" aspect (the metadata only declares the near-square 804×760 and the artwork reflows to any window), so 16:9 is a deliberate display choice for the wide vista, not a spec. The desktop archival bundle is the verbatim responsive artwork and is unaffected (aspect + re-fit are OpenObject display concerns, not baked into the art).
- **Verified on Mac** (Phase 1, against an isolated data dir so the real library was untouched): token 17 resolves via `tokenURI`, mirrors (localizeAbsolute pulls three.js, the gateway's `cdn-cgi` beacon is stripped, the hook is injected before `</body>`), the Settings dropdown renders and its change persists, and the display iframe letterboxes the scene 16:9 (DOM-measured: a centered 16:9 band, canvas aspect matches, undistorted) at the chosen snowfall (Light vs Blizzard visibly differ; overlays hidden; no console errors). This one touches client code (`display.js` for `?oochoice`, `control.js`/`control.css` for the dropdown) on top of `db.js` (the column), `collections.js` (entry + hook + state), and `server.js` (PATCH validation + display flag), so unlike the registry/mirror-only additions before it, the kiosk display must reload to pick up the new `display.js`. The `choice` plumbing is a reusable seam for any future curated-mode collection; only the hook is piece-specific. The token's official `image` is a ~28 MB animated GIF; it is cached locally once like any other preview thumbnail (large but inert after the first download).

### 2026-06-19: Ninth connected collection (Chaz Wesley, "Chazstract" / "The Bloom"), single token + two mirror seams
Added the ninth supported Connected Collection (§8): Chaz Wesley's *"Chazstract"* (Tezos FA2 contract `KT1AATmFFJtPrmpnfdaTcFB1ojhdNJmd92C2`), supporting one piece, token 28, *"The Bloom"* (a p5.js interactive flower garden). "Chazstract" is the artist's general objkt.com contract where every token is a different artwork, so like *"Golden Lining"* the entry carries a **`fixedToken`** and enables just the one vetted piece. Tezos resolve via TzKT is reused as-is. Two new reusable mirror seams plus an auto-start hook:
- **`dirBundle`.** The `artifactUri` is a bare IPFS *directory* CID (`ipfs://<cid>`) whose `index.html` loads its assets (p5) by **relative** ref. `mirrorBundle` derived the asset base from the URL's last path segment, which for a bare CID is the CID itself, so relative assets resolved one level too high. A `dirBundle` flag takes the base from the directory and the entry as its `index.html`; the existing path-carrying collections are unchanged.
- **`dropScripts`.** The bundle also loads `p5.sound.min.js`, which the sketch never uses (all its audio is raw Web Audio API) and which **hangs p5 before `setup()` in restricted contexts** (it froze in a headless browser; it works in normal browsers, which is how objkt shows it). Since OpenObject is **always muted** (§12), a `dropScripts: ['p5.sound.min.js']` flag removes that one `<script>` from the mirror, before the asset scan, so it is not fetched either. The visual is byte-identical and now renders reliably everywhere; the desktop archival copies keep it.
- **Auto-bloom hook + 16:9.** The piece is a fixed **1920×1080** canvas that self-fits to the viewport on black; it declares a 16:9 **`aspect`** so the bare stage letterboxes it on the square frame (and it fills a 16:9 display edge to edge, §6). It opens behind a **"Click to Bloom"** overlay whose click runs the sketch's `startExperience()`, which hides the overlay AND starts the bloom clock (the flowers stay frozen until then) AND plays the music. A passive frame can't click, so a new `BLOOM_HOOK` (`animateHook: 'bloom'`, runs unconditionally) fires `startExperience()` once `setup()` has run, after no-oping `HTMLMediaElement.prototype.play` so the music never sounds. The garden then self-animates (flowers sway/spin, a spaceship drifts); its hand interactions (click a flower, the music panel) are unsupported on a passive muted frame. (That overlay briefly flashed before the hook fired; a later `hideSelectors` seam hides it from the first paint, see the 2026-06-20 entry above.)
- **Verified on Mac** (Phase 1): token 28 resolves via TzKT, mirrors (dirBundle pulls p5 correctly; dropScripts removes p5.sound), the overlay auto-dismisses and the garden blooms silently on its 16:9 canvas. A registry + mirror change (no `display.js`/`.css` edit; `aspect` reuses the Golden Lining path), so it reaches the frame by ordinary self-update with no kiosk power-cycle. Two desktop archival copies were kept, one with the sound components and one without (Matt).

### 2026-06-19: Eighth connected collection (V4w.enko, "Perfect Circles"), a third Tezos/EditART collection
Added the eighth supported Connected Collection (§8): V4w.enko's *"Perfect Circles"* (Tezos FA2 contract `KT1RtgaTm3P35uNzrD2RZPtRwFVCKmjMUbUh`), a second EditART series from the same artist as *"Perfect Everything"*. It introduced **no new mechanism**: resolved and rendered exactly like *"Perfect Everything"* and *"Pendulum"*, so the only change is one registry entry in `player/src/collections.js`.
- **Tezos resolve, reused as-is.** `readTokenMeta` takes the existing `Tezos` branch off the swappable `tzkt` base; the `artifactUri` is `ipfs://<cid>?m0..m4` (ten editions share one self-contained p5 bundle), so the shared mirror, verbatim official-URL storage, query-seed-at-display, cached `displayUri` thumbnail, and offline playback all carried over unchanged. The owner **enters their Token ID** (no `fixedToken`); the square self-looping sketch fills the 1:1 stage edge to edge (no `crop`, no `aspect`, no Animate).
- **Verified on Mac** (Phase 1): token 1 resolves via TzKT and mirrors byte-for-byte (beacon stripped, no hook injected) with its `displayUri` thumbnail cached; the render is the identical EditART path already confirmed on `/display` for *"Perfect Everything"* and *"Pendulum"*, and the owner seeds a piece in the player as the production check. A registry-only change (no `display.js`/`.css` edit), so it reaches the frame by ordinary self-update with no kiosk power-cycle.
- **Archival bundle for token 1** built on the Desktop (the other editions not requested).

### 2026-06-19: Seventh connected collection (Jeremy Booth, "Lost in Moffat County"), time-of-day + an easter-egg Animate
Added the seventh supported Connected Collection (§8): Jeremy Booth's *"Lost in Moffat County"* (Ethereum contract `0x98a8ae5ea04a7cff60cd4877a6e97eb2113b111e`), a time-aware photographic series with an opt-in animated overlay. It reuses three mechanisms already built and adds one new hook; the change is one registry entry plus the hook in `player/src/collections.js`.
- **Reused: per-token + localizeAbsolute + time-aware.** Resolved by `tokenURI` (Ethereum); each token mirrors its own bundle (`perToken`, like send/receive; metadata is split across Arweave and IPFS over the collection). The bundle loads p5 and its day/night photos plus easter-egg GIFs by absolute URL from a public gateway, so `localizeAbsolute` (Golden Lining) pulls them local and rewrites the refs for offline play. The sketch reads the viewer's local clock and crossfades day into night (sunrise 6 to 8, sunset 18 to 20), so the background is automatic with no app timezone UI (like Kittoe; correctness = the frame's OS clock).
- **New: an easter-egg Animate hook.** A tap toggles the sketch's global `easterEgg`, swapping in an animated GIF overlay (also day/night aware). Exposed as the per-collection **Animate** (default **off**, background only), engaged hands-free by a new `EASTER_HOOK` (selected via `animateHook: 'easterEgg'`) that sets `easterEgg` true once setup has run. The existing Azulejo and speed hooks are unchanged; the selector just gained a branch.
- **Square, edge to edge.** The day/night photos are square (2500²), drawn object-fit contain, so on the 1:1 stage they fill edge to edge (no `crop`, no `aspect`). Confirmed from the DOM (the headless screenshot tool mis-composites these p5 pages).
- **Faithful source.** The dynamic piece renders the artist's HTML, which loads the 2500² day/night photos; those are mirrored byte-for-byte (SHA-256 verified). The metadata's `image` is a *separate* static 3840² day-only JPEG (a snapshot, no time-of-day or easter egg), kept as the high-res still but not used for the dynamic render: using it would drop the dynamic behavior, which is the point of the piece (confirmed with Matt).
- **Skipped tokens.** Token 1 ("Desert Steel") is a static landscape image with no `animation_url`; it is intentionally not supported (resolves with a clear "no artwork URL" message) and can be added as a normal upload. Token 2's `tokenURI` reverts on-chain (reads as unminted/burned); the dynamic squares that resolve are tokens 3 to 6.
- **Verified on Mac** (Phase 1): token 4 resolves, mirrors per-token with localizeAbsolute (offline), renders the time-of-day background edge to edge, and Animate engages the easter-egg overlay; tokens 3/5/6 resolve. A registry + hook change (no `display.js`/`.css` edit), so it reaches the frame by ordinary self-update with no kiosk power-cycle.

### 2026-06-19: Sixth connected collection (Cinzia y Gabriel, "Pendulum"), a second Tezos collection
Added the sixth supported Connected Collection (§8): Cinzia y Gabriel's *"Pendulum"* (Tezos FA2 contract `KT1FxLAch681RsZ2UYoSNQ5S5xsgT6mFHwZq`), an EditART generative series. It introduced **no new mechanism**: it is resolved and rendered exactly like *"Perfect Everything"*, so the only change is one registry entry in `player/src/collections.js`.
- **Tezos resolve, reused as-is.** A second Tezos collection, so `readTokenMeta` takes the existing `Tezos` branch: the token's FA2 metadata (`artifactUri` + `displayUri`) is read from the TzKT public indexer API off the swappable `tzkt` base, exactly like *"Perfect Everything"*. The `artifactUri` is `ipfs://<cid>?m0..m4`, so the shared bundle, verbatim official-URL storage, query-seed-at-display, cached thumbnail, and offline playback all carried over unchanged. The same `token_metadata` big_map fallback applies if TzKT is ever unavailable (not built).
- **Whole collection, by Token ID.** The contract holds three editions (#0 to #2), all the same self-contained sketch; like *"Perfect Everything"*, the owner enters their Token ID and any edition resolves (no `fixedToken`). (An early cut hardcoded token 1 via `fixedToken`; corrected the same day to prompt for the Token ID like the other shared-bundle collections, per Matt.)
- **Self-contained, self-animating, edge to edge.** A self-contained p5.js EditART sketch (p5 inlined, no external assets; only the gateway's `cdn-cgi` beacon, stripped by the mirror). Its square canvas is `min(viewport)`, so on the 1:1 stage it fills edge to edge with no `crop` and no `aspect`; `draw()` loops on its own, so `animatable: false` (no Animate control, nothing injected).
- **Seeded with token 1.** Added *"Pendulum #1"* (`m0=0.198, m1=0.675, m2=0.980, m3=0.422, m4=0.192`) as the first piece; the collection accepts any Token ID going forward.
- **Verified on Mac** (Phase 1): a Token ID resolves via TzKT, mirrors byte-for-byte (beacon stripped, no hook injected), caches its `displayUri` thumbnail, and renders its seeded artwork; on `/display` the iframe and its square canvas fill the 1:1 stage edge to edge (confirmed from the DOM, since the headless screenshot tool mis-composites an iframe), matching *"Perfect Everything"*. A registry-only change (no `display.js`/`.css` edit), so it reaches the frame by ordinary self-update with no kiosk power-cycle.

### 2026-06-19: Repositioned around the no-frame path ("your Mac as the display")
OpenObject's **primary on-ramp is now running on a Mac with no frame** (the Mac is the display, full screen in Chrome `--kiosk`); reviving an XXL frame becomes the advanced path. Most people who would want OpenObject do not own an XXL, so the Mac path is the realistic broad door, while the next-stranded-owner mission stays the heart. (Matt, 2026-06-19.)
- **New guide.** Added **`docs/MAC-DISPLAY-SETUP.md`** ("No Frame? Use Your Mac as the Display"), a from-scratch runbook (install Node, download from GitHub, `npm install` / `npm start`, add art, Chrome kiosk at `localhost:3000/display`), validated by a clean-Mac dry-run.
- **README rewritten** to lead with the local art-player capability and a frame-or-not fork (no-frame Mac path first, linking the guide; frame revival second), plus a no-frame pointer under the hardware table and a no-warranty note split so the Mac path is not described as wiping anything.
- **Address-aware hints.** The idle splash and the control panel's reach card no longer hardcode `openobject.local` (false when the Mac is the display). The idle hint shows the host the display was opened at, falling back to the server's advertised name on a loopback host (the frame's kiosk opens `localhost`). The reach card advertises `openobject.local` only where it resolves, detected as `process.platform === 'linux'` (the frame is Debian; a Mac is darwin). Idle is a display change, so the frame picks it up on a power-cycle.
- **Locked terminology.** Fork on frame-or-not, never on "Mac" (a Mac is used in both setups): "the frame" = the Infinite Objects hardware; "your Mac as the display" = the no-frame path; "the control panel" = the shared management page.
- **Reset appendix neutralized.** Renamed `appendix-whitewalls-reset.md` to `appendix-original-reset.md`, retitled "Resetting the original software", no cause or culprit named (White Walls kept only as the on-screen menu label).
- **Still pending:** the openobject.io landing page (`site/index.html`) repositioning to match (last step; does not touch the frame).

### 2026-06-19: Connected Collections promoted from experimental to core
Connected Collections are **no longer experimental**; they are a core part of OpenObject's purpose (showing art that has no downloadable file, alongside ordinary uploads). Dropped the "experimental" qualifier from the feature's current-state descriptions (§6, §8, §12, §17), the Setup Guide, and the code/UI comments. The dated build-log entries below are left as the historical record (the feature was experimental when they were written). No behavior change. (Matt, 2026-06-19.)
- **Placement unchanged for now.** The per-collection controls (Animate / motion-speed, Hide/Unhide) stay as a card under Settings rather than a dedicated tab; the pieces themselves remain ordinary Library rows. Revisit a fourth tab only if the supported list grows enough that the card needs scrolling. (Matt, 2026-06-19.)

### 2026-06-19: Fifth connected collection (V4w.enko, "Perfect Everything") and the first on Tezos (TzKT resolution)
Added the fifth supported Connected Collection (§8): V4w.enko's *"Perfect Everything"* (Tezos FA2 contract `KT1AvGKGryeASE9PXbm345dgPUxVETDLx8qX`), an EditART generative series. It is the **first collection on Tezos** rather than Ethereum, and the only real departure is the resolve trail; everything downstream was reused unchanged. The single code change is in `player/src/collections.js`.
- **Tezos resolution via TzKT.** Tezos has no `eth_call` / `tokenURI`. `resolveToken` now routes through a chain-aware `readTokenMeta`: an Ethereum entry keeps the `eth_call` path, a `Tezos` entry reads the token's FA2 metadata from the **TzKT public indexer API** (`GET /v1/tokens?contract=&tokenId=`, one free read), keyed off a swappable **`tzkt`** base in the registry. Tezos names its fields differently, so the artwork URL is **`artifactUri`** (not `animation_url`) and the preview is **`displayUri`** (not `image`); both are normalized in that one helper.
- **Same shared-bundle shape downstream.** The `artifactUri` is `ipfs://<cid>?m0..m4`: one shared bundle for the whole collection with the per-edition seed in the query string, exactly like Azulejo. So the IPFS mirror, the verbatim official-URL storage, the query-seed-at-display (display.js), the cached thumbnail, and offline playback all worked with no change. The official `artifactUri` is stored verbatim (TzKT surfaces the contract's own metadata, not a marketplace render, so the verbatim rule holds).
- **Self-contained, self-animating, edge to edge.** The piece is a p5.js EditART sketch (p5 inlined, no external assets; only the gateway's `cdn-cgi` beacon, already stripped by the mirror). Its square canvas is `min(viewport)`, so on the 1:1 stage it fills edge to edge with no `crop` and no `aspect`; `draw()` loops continuously on its own, so `animatable: false` (no Animate control, nothing injected).
- **TzKT fallback documented.** TzKT is an indexer service, not a node. If it ever goes away, the same `token_metadata` can be read straight from a public Tezos RPC node's big_map; recorded as a refactor seam in `collections.js` and §8 (not built today, per Matt 2026-06-19).
- **Seeded with token 9.** Added *"Perfect Everything #9"* as the first piece (`m0=0.038, m1=0.932, m2=0.909, m3=0.209, m4=0.276`); the collection accepts any Token ID going forward.
- **Settings list now sorts by collection name.** The supported-collections list (the Settings card and the add-modal picker, both fed by `list()`) sorts by **collection name** instead of by artist; control.js renders in the server's order.
- **Verified on Mac** (Phase 1): #9 resolves via TzKT, stores the verbatim `artifactUri`, mirrors fully offline, and on a 1:1 stage renders edge to edge and animates continuously (frameCount advancing, center pixel repainting), matching the official `displayUri` preview, clean console. On the frame this arrives via the normal self-update (the player restart reloads `collections.js`); **no kiosk power-cycle is needed** because no display/kiosk code changed, the new slug folds into the rotation on the usual poll.

### 2026-06-18: Fourth connected collection (Juicy Julio, "Golden Lining") + ERC-1155, aspect-fit, motion speed
Added the fourth supported Connected Collection (§8): Juicy Julio's *"Golden Lining"*, token 1 of *"Dune Reveries - Editions"* (Ethereum ERC-1155 contract `0x45dcaec1c3c51148771a7a32669ea0adab051b3e`). Only this edition needs special handling (the collection's other two are plain static images), so the registry supports exactly token 1. The piece is a p5.js sketch over a single 5:4 landscape JPEG: each frame it draws the photo, grays the whole canvas, then redraws the same photo in color on top at a variable opacity, a global black-and-white to color crossfade the artist drives with horizontal mouse position. Five additive changes:
- **ERC-1155 resolution.** `resolveToken` picks the selector by collection: `uri(uint256)` for an `erc1155` entry, `tokenURI(uint256)` otherwise (and substitutes an `{id}` template if present). First non-ERC-721 collection.
- **fixedToken (no Token-ID prompt).** A `fixedToken` registry flag resolves that one token and hides the Token-ID field in the add modal (control.js); still resolved on-chain. Treated as a normal one-piece collection everywhere else.
- **Absolute-asset localizer.** The sketch loads p5 (cdnjs) and its JPEG (ipfs.io) by absolute URL, the image from inside an inline `loadImage('…')` the relative-asset scan can't see. A `localizeAbsolute` flag downloads every absolute http(s) asset into the bundle, rewrites references to local files, and strips now-pointless `integrity`/`crossorigin`, so it plays fully offline like the self-contained collections.
- **Motion speed (0 to 10), in place of Animate.** Rather than feed input, an injected hook drives the sketch's global crossfade `opacity` with a fluid cosine sweep (eases in/out at both turns, anchored on full color); a per-collection `speed` (0 = static full color, default 2) rides in on `?oospeed=` and replaces the on/off Animate control. Stored in `collection_state.speed` (new column, in-place migration); a slider in the Settings card (control.js/.css).
- **Aspect-fit framing + full-color thumb.** The piece declares a 5:4 `aspect`; the display sizes the iframe to it and centers it so the bare black stage letterboxes it top and bottom (the iframe equivalent of `object-fit: contain`, §6), no painted background and no zoom-crop. The Library thumbnail is sourced from the artwork's own color master, not the token's stylized split preview.
- **Verified on Mac** (Phase 1): token 1 resolves on-chain via `uri()`, mirrors fully offline (no cdnjs/ipfs refs left, p5 + JPEG local), the cosine sweep runs at the chosen speed, and the piece renders edge-to-edge in width with native black bars top and bottom on the 1:1 stage. On the frame this arrives by self-update plus a kiosk power-cycle, because display.js/.css changed (§15). Confirmed live on the frame after update + reboot (Matt, 2026-06-18).

### 2026-06-18: SVG supported as a normal upload (rendered as a safe image)
Realizes the §17 "SVG support" seam (§6). SVG is now an accepted upload type, no longer skipped. Prompted by a specific piece the owner holds: *"The Vice City"* (token 31 of **TESSERACTS** by The Digital Asset Museum), a self-contained animated SVG on Arweave (800×800 `viewBox`, its own black background rect, 34 SMIL `<animate>` loops set to `repeatCount="indefinite"`, no script, no external references).
- **Deliberately not a Connected Collection.** Connected Collections resolve and mirror a *pointer* (contract + token id → on-chain URL). This is a *file* the owner already has, so the honest path is the ordinary upload, not the curated registry. Decided after confirming it renders cleanly as a plain upload; the white space the owner saw was only a browser's page background around a fixed-size SVG opened as a top-level document, not part of the art (Matt, 2026-06-18).
- **One-line engine change.** Added `svg` to `player/src/formats.js` (`format: svg`, `kind: animated`, `mime: image/svg+xml`); everything downstream already worked. The display renders any non-video/non-connected item as an `<img>` with `object-fit: contain` (the default Fit), which centers the square and scales it edge to edge on the 1:1 panel; the control-panel thumbnail uses the same `<img>`.
- **Safe by default, no new policy.** Rendering as an `<img>` means the browser never runs the SVG's scripts or fetches its external references; the existing global CSP (`script-src 'self'`, no inline) neutralizes inline script even on a direct open, so no SVG-specific header was needed. Declarative SMIL/CSS is not script and keeps animating.
- **Best-effort, not generic.** A clean `viewBox` (the norm) scales and centers well; a file with no intrinsic size or with script-driven motion may render oddly or sit static. Documented as such in §6; vetted, self-contained pieces are the intended use.
- **Verified on Mac** (Phase 1): the file uploads (accepted, not skipped, stored byte-for-byte as `.svg`), folds into the rotation, and renders centered on pure black with its SMIL animation looping continuously, clean console. On the frame it arrives by the normal self-update with **no kiosk power-cycle** needed (only `formats.js` changed, no display/kiosk code), folding in on the usual poll.

### 2026-06-18: Documented frame serviceability (Tier-1/2 update model + console access) in §15
Promoted two operating facts that were only in working notes into the spec, under §15 "Servicing
the installed frame": the Tier-1 (rides self-update) vs Tier-2 (needs an `install.sh` re-run) split
for what a self-update can actually ship, and the `Ctrl+Alt+Fn+F2` console chord (cage `-s` + the
Fn key on a media-key keyboard) with `Ctrl+Alt+F1` back to the art. Doc-only; engineering audience,
so no SETUP-GUIDE change.

### 2026-06-18: Third connected collection (Snowfro, "send/receive") + live on-chain art
Added the third supported Connected Collection (§8): Snowfro's *"send/receive"* (Art Blocks Flex on-chain, Ethereum contract `0xababababab20053426ad1c782de9ea8444358070`). It is the first **live/networked** piece: each token's generator reads the collection's global on-chain state (the send/receive balance across all tokens, block by block) and animates from it. Three additive changes:

- **Per-token bundles.** Art Blocks' generator returns a different fully-inlined HTML per token (token id in the URL path, no query seed), unlike the shared-bundle collections (Azulejo/Kittoe, where one bundle serves every edition and the per-piece seed rides in the URL query). A `perToken` registry flag mirrors each token into its own dir (`collections/<slug>/<tokenId>/`) and the display builds the per-token path; shared collections are untouched. Deleting a per-token piece reclaims its bundle (a shared bundle stays while other pieces use it, and is reclaimed once the collection's last piece is deleted, see the 2026-06-20 cleanup entry). The auto-animate hook is now injected only for collections with an Animate control, so self-animating pieces mirror verbatim.
- **Live RPC, the one online exception.** The piece needs a reachable Ethereum node to animate. We override its embedded endpoint with our own **swappable public node** via the piece's documented `?rpc_url=` query (so we never depend on the artist's key), and scope a **`connect-src` exception to just this collection's bundle path** (server.js, keyed off the registry's `liveRpc`); every other collection and the rest of the frame stay locked to same-origin, no phoning home. This is the only connected piece that is **not fully offline**: with no network it falls back to a static sprite carrying the artwork's own red network-error badge. Approved as a deliberate exception because the artwork is *about* the live network and the offline fallback is not presentable (Matt, 2026-06-18).
- **Edge-to-edge crop.** The generator composes its sprite card in the centered 60% of the panel with a 20% black margin. To honor §6 (edge to edge, no border) a per-collection `crop` (0.6, measured off the static "white version" of the card, constant across the collection) **oversizes the iframe** to 1/0.6 and centers it, so the generator renders the card larger and crisp while the layer clips the margin (display.css/.js). Oversizing rather than CSS-scaling keeps the pixel art sharp; on the 1920 panel 1.6667 lands on exact pixel boundaries, so no black sliver.
- **Verified on Mac** (Phase 1): both of the owner's tokens (5008760, 5008372) resolve on-chain, mirror per-token, animate from live data under the scoped CSP (clean console, no error badge), and fill the panel edge to edge; Azulejo and the other collections stay regression-clean. On the frame this arrives by self-update **plus a kiosk power-cycle**, because display.js/.css changed (the player restart reloads the server, but the kiosk keeps the old display page until it reloads, §15).

### 2026-06-18: Second connected collection (Alex Kittoe, "As the Days Go By") + IPFS resolution
Added the second supported Connected Collection (§8): Alex Kittoe's *"As the Days Go By"* (Ethereum contract `0x9a9b9b14581136cb2f0f53e2b65ba6c74fd660b4`), a 150-edition photographic piece. Unlike Azulejo (a generative p5 sketch on Arweave), this is the first collection whose metadata and render bundle live on **IPFS**, and the first that is a **time-aware still** rather than an animation.

- **IPFS resolution.** `resolveToken`, the bundle mirror, and the thumbnail/preview now map `ipfs://` to a public gateway (`https://ipfs.io/ipfs/`) at fetch time only; the official `ipfs://` URL is still stored verbatim. Arweave/https paths are unchanged. One small `toHttp()` helper applied at each fetch point (src/collections.js).
- **Day/night is the artist's own code, not ours.** The bundle is a self-contained HTML carrying both a day and a night photo as embedded base64; six lines of its own script pick one from `new Date().getHours()` (day = 6am to 6pm) and recheck each minute. We mirror it verbatim and let it read the frame's clock, exactly as it does on any collector's device. We deliberately did **not** build a parallel timeframe switcher (that would be less faithful, and wrong for an owner in another zone). Correctness therefore depends on the **frame's system timezone** being set: an OS-level concern, no app setting, the same precondition every other device the art runs on already meets. On the Mac it follows the Mac clock.
- **No Animate control for stills.** A new registry flag `animatable: false` marks a collection with no motion to engage; the Settings card hides its Animate switch (control.js). Azulejo keeps its Animate toggle.
- **Faithful mirror.** The mirror now strips a tracking beacon some public gateways inject into the served HTML (Cloudflare's hidden `cdn-cgi` `<a>`); it is not part of the artist's file.
- **Editions are identical.** All 150 are numbered copies of the same media, so the one-bundle-per-collection mirror is correct.
- **Verified on Mac** (Phase 1). On the frame this arrives via the normal self-update: the player restart reloads `collections.js`, and **no kiosk power-cycle is needed** because no display/kiosk code changed (the display already renders any connected slug; the new piece folds into the rotation on the usual poll).

### 2026-06-17: Wi-Fi rejoin race fixed (openobject-netcheck watchdog)
Second of the two follow-ups parked after the reboot fix. (The first, syncing the frame's installer checkout, was closed by an ordinary self-update: the frame's `/opt/openobject` now matches `main`, so re-running `install.sh` re-applies reboot=pci + SSH-on instead of stripping them.)

Symptom: on a cold boot the frame could come up display-but-no-network (art on screen, control panel unreachable from any device) and stay that way until a manual power-cycle. Root cause: on the installed frame Wi-Fi is owned by **ifupdown** (the `allow-hotplug wlp0s20f3` + `wpa_supplicant` connection the Debian netinst created), not NetworkManager (NM is installed but leaves the device `unmanaged`, so the installer's §8 NM handoff no-ops). ifupdown brings Wi-Fi up once at boot and never retries, so a race where the radio or the access point isn't ready in time leaves the frame offline with nothing to recover it.

Fix (additive, low-risk: the "small reconnect unit" option we parked): a new **`openobject-netcheck`** systemd timer + oneshot service runs `installer/net/oo-netcheck.sh` every ~30s (first check 60s after boot). It re-ups Wi-Fi (`ifdown --force` then `ifup` on the auto-detected wireless device) **only when there is already no LAN connectivity** (no default route, or the default gateway does not answer two checks ~5s apart). Because it acts only when the frame is already offline, it can never disturb a working connection or an SSH session. A raced cold boot now self-heals in roughly 60-90s instead of needing a power-cycle. Device-agnostic (it finds the Wi-Fi interface itself), so it serves the next owner's hardware too.

Deliberately **not** done now: the full NetworkManager migration (which would bring autoconnect/retry natively). It is riskier to flip over a live SSH session and belongs to the later §11 setup-AP milestone; this watchdog is the safe interim fix and stays useful regardless. Tier-2 change (new systemd units under /etc), so an existing frame needs an `install.sh` re-run, not self-update alone; the kiosk is unaffected (no power-cycle needed). (Matt, 2026-06-17.)

### 2026-06-17: Control-panel Reboot hang fixed (reboot=pci)
The Reboot button intermittently hung this MeLE N5105 / AMI board on the firmware POST splash (the Infinite Objects logo) and needed a cold unplug to recover, while Shut down was always fine. Root cause: the board's default reset method is ACPI (`/sys/kernel/reboot/type` read `acpi`, with mode already `cold`), and that ACPI reset intermittently fails to survive this firmware on a warm reboot. Fix: add `reboot=pci` to the kernel command line (the `DESIRED` string in install.sh §7), forcing the Intel 0xCF9 platform reset, the software equivalent of the power-cycle that already recovers the frame. Bench-verified 2026-06-17: tested live first via `/sys/kernel/reboot/type`, then persisted via GRUB, with clean reboots and no hang. Shut down (`poweroff`, ACPI S5) uses a separate path and is unaffected. Tier-2 change (writes /etc/default/grub + runs update-grub), so an existing frame needs an install.sh re-run, not just self-update; Matt's frame already has it applied manually. (Matt, 2026-06-17.)

### 2026-06-17: SSH on by default (installer)
`install.sh` now **enables** SSH on every frame (`systemctl enable --now ssh`) instead of installing it and disabling it. A fresh frame is reachable from another computer for servicing without a console trip; an owner who wants it closed runs `sudo systemctl disable --now ssh`. This reverses the 2026-06-15 "opt-in SSH" decision below: the posture is now a known, documented open port rather than closed-by-default, traded for serviceability on this single-owner LAN appliance. The enable is idempotent, so re-running the installer (a Tier-2 step, e.g. to land the reboot fix) no longer drops the SSH session it is run over, which the old every-run `systemctl disable --now ssh` did. SETUP-GUIDE "If something seems stuck" updated to match. (Matt, 2026-06-17.)

### 2026-06-17: Pruned §17 future enhancements (allow-audio, smart-plug)
- Removed the global "allow audio" toggle. The frame has no speaker; adding one (e.g. a USB speaker) is possible but would break the original intention of silent art on a wall. Audio stays **muted, always**, by design, not as a deferred limitation. Updated the §12 Audio row to match.
- Removed the **smart-plug integration** item. Remote power-cycling via an optional smart plug is already documented as an owner choice in §10 (hard-lockup recovery becomes a phone tap, not required), so it does not need to live here as a feature to build.
- (Matt, 2026-06-17.)

### 2026-06-16: Connected Collections (experimental web/on-chain art) built and verified on the frame
Realizes the §17 "web / HTML art pieces" seam, opt-in and off the default path (normal uploads, rotation, and display are unchanged). First supported collection: Bryan Brinkman's *"Azulejo Galo"* (a p5.js sketch on Arweave).
- **Model.** A built-in registry of supported collections (code, not user-authored). The owner picks a collection and enters the piece's **Token ID**.
- **Resolution is on-chain and official.** The player reads the token's `tokenURI` via a free `eth_call` to a public Ethereum node, follows it to the canonical metadata, and stores the **official `animation_url` verbatim**, never a marketplace-rendered copy. (Confirmed the OpenSea/Highlight web pages bury the real URL among their own proxy copies; the contract's `tokenURI` is the source of truth, reachable from any Ethereum node.)
- **Plays offline.** The shared render bundle is mirrored to `player/data/collections/<slug>/` and served same-origin; the network is touched only once, at add time. A `connected` render kind shows it in a sandboxed same-origin iframe, and an injected hook auto-engages the collection's motion when **Animate** is on.
- **UI.** "Add connected artwork" in the Library (pick collection, enter Token ID, see a derived preview, Add); a "Connected" library card (badge + cached thumbnail, no Fit/Fill); a Settings "Connected Collections" card (per-collection Animate, Hide / Unhide all) placed directly under Sleep Schedule.
- **CSP.** `/collections/*` gets its own policy (`script-src 'self' 'unsafe-inline'`, `frame-ancestors 'self'`) so the mirrored art runs and the display can frame it; the rest of the panel keeps the strict policy.
- **Verified on hardware (after a cold power-cycle).** The earlier wrong render was the **stale kiosk**: self-update restarts the player but not the kiosk Chromium, which kept running the pre-feature `/display`. A power-cycle loaded the new display code and Azulejo #101 renders and animates correctly. Separately observed: the control-panel **Reboot** button hung on the firmware boot splash on this unit (warm `systemctl reboot` stuck at POST; a cold unplug/replug recovered) (FIXED 2026-06-17 with `reboot=pci`, see the top of this log).
- Docs: this section, plus §6, §8, §12, §17.

### 2026-06-16: Tab title separator + real favicon on openobject.io
- **Control-panel tab title** is now "OpenObject · Control Panel". It previously showed "Control" with an em-dash separator; the clearer name plus the middot match the house separator used across the app and the landing page (the em dash also broke the no-em-dash rule for committed text).
- **Landing-page favicon fixed (real files).** openobject.io was shipping a placeholder inline-SVG mark, not the brand. First tried the real favicon inlined as a base64 data URI; it deployed correctly (verified live with curl), but Safari kept showing the old cached icon (Safari caches favicons aggressively and is unreliable with data-URI favicons). Switched to real favicon files at the site root (favicon-16/32.png + apple-touch-icon.png), the same setup the player serves on openobject.local that Safari renders correctly. gh-pages now publishes those files alongside index.html, with CNAME preserved. Follow-up: also added a real /favicon.ico at the root (it had been 404ing). Safari auto-requests /favicon.ico and favors it, and its favicon cache is shared even with Private windows, so the .ico plus a Safari favicon-cache clear is what refreshes a stuck tab.
- **Landing-page title shortened to "OpenObject"** (was "OpenObject · revive a stranded art frame"). Safari truncated the long title in the tab down to its tail ("revive a stranded art frame"); the tab now leads with the brand. The tagline stays in the meta description and the og/twitter tags, so search and social are unchanged.

### 2026-06-16: Settings tab pass (reorder, password copy, stacked fields, About card, Wi-Fi link color)
A pass over the Settings tab for the next owner: clearer ordering, tighter copy, and consistent link styling. No behavior changes. Order is now Sleep Schedule, Software Update, Power, Password, Wi-Fi, About (matches the Setup Guide walkthrough).
- **Password card moved beneath Power.** It's opt-in and off by default, so it now sits below the everyday frame controls instead of above them.
- **Enable button relabeled** from "Turn on password" to "Set password". The New and Confirm fields sit right in the card, so the click saves a value you've already typed (a "Set" action, not a toggle that prompts afterward); it also matches the card's own wording ("set a password") and pairs with the existing "Change password". The red "Turn off password" control is unchanged.
- **Entry fields stacked.** The New and Confirm password inputs were side by side and stretched the full card width, which looked odd; they now stack vertically at a normal field width (capped to match the login box) in both the set and change states.
- **Password copy tightened**, keeping the "open to anyone" preface (its value is the "why" you'd want a password): "The control panel is open to anyone on your network. Optionally set a password. This will not affect the display of art."
- **New About card** at the bottom (after Wi-Fi, the lowest-interaction spot): the OpenObject name and one-line description, a link to the project home (openobject.io) and to the source on GitHub, and a license line ("© 2026 OpenObject · Free for noncommercial use."). The source link is deliberate: it points the next owner of a stranded frame back to the project so they can revive it. Static HTML in control.html.
- **Wi-Fi link color fixed.** The Wi-Fi card's addresses and the openobject.local link were falling back to the browser's default blue: a refactor had moved the card to inline paragraphs and left the old `.reach-list` link CSS unmatched. Gave them the house near-white + underline treatment (matching the About links) and removed the five dead `.reach-list` rules that caused the gap.
- **Settings stays single-column.** A two-column / half-width layout was considered and declined: the panel is mostly phone-driven (columns collapse on a narrow screen anyway) and the two tallest cards (Sleep Schedule, Password) already use the full width, so halving them would just wrap their controls. If Settings grows, the cleaner lever is grouping, not columns. (Done 2026-06-21: Sleep Schedule, Connected Collections, and the infrequently-used Password and Wi-Fi cards are now collapsible from their headers, default closed, with each card's open/closed state remembered per browser.)

### 2026-06-16: Security follow-ups (filename XSS, CSP, upload/disk guards, path scrub)
A read-only security review (private home-network deployment) found no committed secrets; the items it flagged were fixed this session.
- **Stored XSS fixed.** The control panel rendered an uploaded filename (`original_name`, attacker-controlled) into `innerHTML` unescaped. Added an `esc()` helper, used for `original_name` in the Library and Rotation views. Verified in-browser: a `"><img onerror=...>` filename now renders as literal text, no script runs.
- **Content-Security-Policy added** (defense in depth): a strict header (`script-src 'self'`, no inline, etc.). All scripts and styles are external same-origin files, so it broke nothing; the control panel and display both load clean.
- **Upload + disk guards (§8).** Per-file size cap (`OO_MAX_UPLOAD_MB`, default 512 MB) and a per-request file count (`OO_MAX_UPLOAD_FILES`, default 50); an oversize file is refused (HTTP 413) with no orphan left on disk. A pre-flight free-space check (`OO_MIN_FREE_MB`, default 2048 MB) refuses uploads (HTTP 507) before the eMMC can fill and wedge the frame. The control panel surfaces the server's message on a failed upload.
- **Privacy.** Scrubbed the local macOS home path from this doc (now `~/Code/OpenObject`); future commits use a GitHub noreply email (past commits keep the old address).
- **Optional control-panel password (built, §12).** Was deferred; now implemented, still OFF by default so an open frame is unchanged. When the owner sets a password in Settings, the control panel and every mutating API require a session (login overlay); the kiosk surface (`/display`, `/api/display`, `/healthz`, `/assets`, `/uploads`) stays open so the display never needs a credential. Stateless HMAC-signed httpOnly cookie, scrypt-hashed password in the settings table, no new dependency. Verified in-browser: off, turn on, logout, wrong password, login, and the kiosk reachable while locked. Folded in a small cleanup: control.js now reuses the existing escapeHtml helper instead of a duplicate.

### 2026-06-15: Reboot / Shut down made real on the frame (§17 closed)
The Reboot and Shut down buttons were inert stubs; they now actually reboot / power off the installed frame, closing the §17 "real device power-off and reboot" item.
- **server.js:** Reboot runs `systemctl reboot`, Shut down runs `systemctl poweroff`, but only on the Linux device (`process.platform`); off-device (the dev Mac) both stay inert stubs, so a dev machine is never powered off. A missing grant is reported rather than failing silently, and the panel treats a dropped connection as the frame going down (polling it back after a reboot).
- **installer/install.sh:** installs polkit and writes a scoped polkit rule letting the `openobject` user run those two actions without a password (the `-multiple-sessions` variants included, since the kiosk session is always active).
- **Existing frames** (provisioned before this) apply the grant once: self-update, then re-run `sudo bash /opt/openobject/installer/install.sh`. New installs get it automatically. The real path was written on macOS (where it verifies as a safe stub) and confirmed on the frame.

### 2026-06-15: Control panel polish: doc-only updates no longer nag, plus a Reboot button
Two next-owner UX fixes the landing-page work surfaced:
- **Update check is now path-aware.** `check()` reads the files an incoming fast-forward would change and reports an update only when something the frame actually runs is among them (`player/`, the served `assets/`, `installer/`). Commits confined to `docs/`, the website (`site/`), or repo meta now report "up to date" instead of prompting a pointless Update & Restart. Denylist-based, so unknown or new paths still count as a real update (a genuine update is never hidden). Verified against the real `a134b72..HEAD` range (all docs/site) reading as up to date.
- **Reboot button added** to the Power controls, between Restart and Shut down. Like Shut down it is an inert stub for now; the real `systemctl reboot` / `poweroff` wiring stays the §17 task (one polkit grant in `install.sh` away).

### 2026-06-15: openobject.io landing page live
A public landing page now lives at **https://openobject.io** (and `www.` redirects to the apex), served by GitHub Pages with an enforced HTTPS certificate.
- **Source:** a single self-contained `site/index.html` on `main` (dark wiki-style page, inline CSS, the logo recolored white, Open Graph and Twitter card tags, a "Find us on X" footer), with friendly copy drawn from the README and Setup Guide voice.
- **Hosting:** an orphan `gh-pages` branch holding exactly `index.html` plus a `CNAME` file (`openobject.io`); the Pages source is that branch at its root. To update the live page later, edit `site/index.html` and re-publish to gh-pages keeping both files (dropping the CNAME would disconnect the domain).
- **DNS (GoDaddy):** apex A and AAAA records pointing at the GitHub Pages addresses (unchanged by the org move), a `www` CNAME (should now target `queueue-studios.github.io`; may still be the old `mattonchain.github.io` until updated, a low-priority loose end since the apex is what matters), and a domain-verification TXT. **Post-transfer (2026-07-03):** transferring the repo to the `queueue-studios` org **disabled Pages and dropped the custom domain** (the personal-account domain verification did not carry over), which 404'd openobject.io until fixed: re-enabled Pages (source = `gh-pages` / root), re-verified the domain at the **org level** via a new `_github-pages-challenge-queueue-studios` TXT record, then re-set `openobject.io` as the repo's custom domain. The apex is now verified and served under the org.

### 2026-06-15: Kiosk cursor hidden, console access, opt-in SSH
All delivered over the air (self-update + a power-cycle, no re-imaging), which exercised the Tier-1 update path end to end.
- **Cursor solved.** cage takes its pointer from the cursor theme literally named `default` and ignores `XCURSOR_THEME`, so the earlier blank-theme attempt was a no-op even though cage had the variable. `start-kiosk.sh` now exposes the shipped transparent `blank` theme under the name `default` (a symlink rebuilt on each start, so it self-heals and fresh installs get it). Verified gone on the frame; the matching §17 enhancement item is removed.
- **Console access (`cage -s`).** cage locks the VT unless launched with `-s`; added it, so `Ctrl+Alt+F2` reaches a Debian text console for servicing. On the bench K400 the top-row keys are media keys, so in practice it is `Ctrl+Alt+Fn+F2` (`Ctrl+Alt+F1` returns to the art; from the shell, `sudo chvt 1` / `chvt 2` switch reliably if the function keys fight you). git against `/opt/openobject` must run as the owning service user (`sudo -u openobject git ...`) or git refuses with "dubious ownership."
- **SSH: installed but OFF by default.** *(Superseded 2026-06-17: SSH is now ON by default, see the top of this log.)* `install.sh` now installs `openssh-server` and disables it, so a shipped frame opens no port until its owner runs `sudo systemctl enable --now ssh` (Matt's call, for everyone). Frames provisioned before this need a one-time `sudo apt install openssh-server` first, since their installer already ran.
- **Lockfile heads-up, fixed:** the "local file changes are present" line on the Software Update card was only `player/package-lock.json`, which `npm install` rewrote at provisioning. Fixed by switching the installer and the updater to **`npm ci`** (commit `f3e209b`), which installs exactly what the lockfile pins and never rewrites it (committed lockfile verified in sync, no devDependencies). An already-provisioned frame clears its existing copy once with `sudo -u openobject git -C /opt/openobject checkout -- player/package-lock.json`. (Matt, 2026-06-15.)

### 2026-06-14: Public launch, self-update verified on the real frame, public-facing copy pass
The repo went **public** under the **PolyForm Noncommercial License 1.0.0** (source available for noncommercial use), and over-the-air self-update was **verified end to end on the actual frame**: from the control panel the frame fetched the now-public repo, fast-forwarded `5b69400` to `da15978` (nine commits), reinstalled dependencies, and restarted the player, with the commit on the Software Update card flipping to confirm it. The version stays `0.1.0` and the date stays today because every commit landed in one day, so the **commit hash is the precise "it updated" indicator**, which is exactly what that field exists for.
- **Public-facing copy pass:** README and SETUP-GUIDE rewritten in a casual, human voice. The original decay is described generically, with the **White Walls app credited as the stranded hero** (the cloud services it relied on going away was not its fault). Added a **No warranty** section and an **Independence / not-affiliated** notice. SETUP-GUIDE gained a frank "So you want to try this?" preface, and its install steps now point to the real `installer/` runbook instead of promising the unbuilt one-tap image and setup-AP Wi-Fi.
- **Em dashes removed from all repo copy** (README, SETUP-GUIDE, HANDOFF, both appendixes, the installer runbook, INSTALL-NOTES, CLAUDE.md, the player README): Matt considers heavy em-dash use an AI tell. Headers take a colon, body text a comma. `LICENSE` left verbatim; code comments left for now.
- **Attempted to hide the kiosk cursor** (`cursor: none` on `/display`) so the setup keyboard's receiver can stay attached for local access. On the first booted frame it does NOT take effect: the CSS loads (the sleep-mark dim from the same file applies) but cage still draws a pointer on the art. Logged in §17 for a proper compositor-level fix.
- **Logged in §17:** wire a real device power-off and a true device reboot, since the bench self-update exposed Shut down as a still-inert stub on the real frame. (Matt, 2026-06-14.)

### 2026-06-14: License chosen + repo prepped to go public (source available, noncommercial)
- **License = PolyForm Noncommercial License 1.0.0** (Matt's call): the source is **available for
  noncommercial use**, anyone may use, modify, and share OpenObject to revive and run their *own*
  frame (personal/hobby), but **may not** sell it, charge for it, or build it into a product or other
  commercial/revenue venture. Deliberately **not** "open source" (the noncommercial limit), call it
  *"source available, noncommercial"* everywhere. **Copyright holder = The Museum of Digital Art
  (@mymoda_io)** (Matt's established X handle/brand).
- Added a root `LICENSE` (verbatim official PolyForm text + a `Required Notice:` copyright line);
  set `player/package.json` `license` → `PolyForm-Noncommercial-1.0.0` + `author` (kept
  `"private": true`, that only blocks an accidental npm publish, unrelated to GitHub visibility).
- **README refreshed for a public front page** and corrected stale facts: CPU **N5100 → N5105**,
  added **WebM**, dropped the removed **Random** order (Sequence/Shuffle only), status now "working
  on real hardware," `installer/` described, added a Get-started section + the 1920×1920 panel spec.
- **The visibility flip itself is Matt's manual step** (GitHub → Settings → Change visibility →
  Public); the repo *content* is ready. Once public, the frame's self-update can finally pull
  (resolves the repo-public/license trigger). (Matt, 2026-06-14.)

### 2026-06-14: Phase 2B VERIFIED on hardware: the frame runs OpenObject
Bench bring-up of the installer succeeded **end-to-end on the real MeLE frame, first run.**
- Flashed Debian 13.5 netinst (balenaEtcher) → wiped the eMMC → minimal Debian (no desktop) →
  `installer/install.sh` provisioned Node 22, Chromium + cage, Avahi, NetworkManager, the systemd
  units, and **passed its self-test** (`/healthz` on :80). After `reboot` the panel came up
  **edge-to-edge on `/display`** (cage + Chromium kiosk) with the branded idle screen, the single
  riskiest piece, working on the first boot.
- **Verified:** `http://openobject.local` reachable from a phone over Wi-Fi; uploads work
  (incl. a **285 MB video** looping); rotation cycles; the cursor is gone once the setup keyboard's
  USB receiver is unplugged (the wall-mounted state, so no `cursor: none` code was needed).
- **Hardware confirmed (`lspci`):** Wi-Fi **Intel Jasper Lake CNVi `[8086:4df0]` / `iwlwifi`**
  (Debian in-box firmware, no dongle), iGPU **`[8086:4e61]` / i915**, NIC **Realtek / r8169**,
  panel **1920×1920** square. Filled the §19 Wi-Fi placeholder + the §2 table.
- **NM Wi-Fi handoff** no-ops on this ifupdown-managed install (skipped, non-fatal, by design);
  Wi-Fi + Avahi work regardless. Full NM ownership stays with the later setup-AP milestone.
- **Polish from the bench test:** video Library cards now show a first-frame poster (`#t=0.1`
  media fragment, no transcode); the blank/sleep mark dimmed **0.2 → 0.1** (Matt).
- **Deployment note:** code fixes reach the installed frame only via self-update (needs the repo
  public) or a re-seed, the first concrete case of the repo-public/license trigger. Running bench
  log: `installer/INSTALL-NOTES.md`. (Matt, 2026-06-14.)

### 2026-06-14: Phase 2B: UEFI Debian + Chromium-kiosk installer built (Mac side)
Built the installer that turns a minimal Debian into an OpenObject appliance; bench test pending.
- **OS + kiosk locked (§3):** minimal **Debian stable** (UEFI, no desktop) via the standard
  netinst, then an idempotent **`installer/install.sh`** provisions on top. Display surface =
  **`cage`** (Wayland kiosk compositor) running **Chromium `--kiosk`** at `localhost/display`,
  no X / WM / DM. X11 + Openbox documented as the fallback.
- **systemd replaces `supervisor.js` (§15)** with **no player code change**, the player already
  honored `OO_SUPERVISED` + exit-75 and `PORT`/`OO_DATA_DIR`/`OO_UPLOADS_DIR`, so Phase 2 is pure
  *configuration*. `openobject-player.service` (`Restart=always`, `SuccessExitStatus=75`,
  `CAP_NET_BIND_SERVICE` → **port 80**) + `openobject-kiosk.service` (cage on tty1 via the
  logind-seat `PAMName=login` pattern).
- **Layout:** code at `/opt/openobject` (git checkout, `origin` → GitHub), runtime data at
  `/var/lib/openobject/` (outside the checkout, so self-update/re-seed can't touch the library).
- **Private-repo seeding:** the bench unit is seeded from a **`git bundle`** of the local repo
  (no clone-auth needed, history preserved). **Self-update is wired but goes live only when the
  repo is public** (`GIT_TERMINAL_PROMPT=0` makes the private-repo fetch fail fast/graceful).
- **NetworkManager** becomes the network manager (foundation for the §11 setup-AP); the handoff
  from the netinst's Wi-Fi runs **last** and is **non-fatal** (proves itself before retiring the
  old config, else leaves the working Wi-Fi alone).
- **Mac-side verification (no hardware):** `bash -n` on all scripts ✓; a **`git bundle`
  round-trip** ✓, clones to a clean committed-only checkout (no `node_modules`/`data`/`uploads`,
  §8) and `npm install` resolves. Files: `installer/install.sh`, `installer/systemd/*.service`,
  `installer/kiosk/{start-kiosk,chromium-kiosk}.sh`, `installer/README.md`. **Out of scope this
  session:** the §11 setup-AP/captive portal and the §15 one-file release image. (Matt, 2026-06-14.)

### 2026-06-13: Phase 2 bench: live boot, Wi-Fi confirmed, factory eMMC cloned
Second hands-on bench session, booted our own Linux on the unit and made the pre-wipe backup.
- **Live USB:** flashed **Ubuntu 26.04 LTS desktop** (current LTS, newest kernel/firmware) to a USB stick from the Mac (`dd`), booted the MeLE via **Save & Exit → Boot Override → `UEFI: <stick>`** (the factory `ubuntu (eMMC A3V012)` entry is the eMMC; `Del` → BIOS as expected), chose **Try Ubuntu** (live; no install).
- **Wi-Fi smoke test PASSED:** the onboard Wi-Fi scanned, associated, and carried traffic under Ubuntu 26.04, the single biggest Phase-2 unknown, now de-risked (Wi-Fi-only install is viable, §11). Exact chip/driver name still to capture (`lspci -nnk`).
- **eMMC size corrected, ~128 GB, not 256 GB.** Linux `lsblk` shows **`/dev/mmcblk0` = 116.5 GiB** (EFI `p1` 512 MB + ext4 `p2` 116 GB factory Ubuntu root, + 4 MB `mmcblk0boot0/1`). The earlier "~256 GB" came from the Android storage screen and was wrong; `lsblk` is authoritative, so the original "128 GB" was right. **§2 / §4 / §19 corrected**, and the Linux path `/dev/mmcblk0` resolves a §19 TBD.
- **Factory eMMC cloned (capture-only backup):** mounted a 2 TB exFAT USB drive in the live session and ran `ddrescue /dev/mmcblk0 → xxl-emmc.img` (read-only on the eMMC; ~117 GB image). Distilled into a new **optional** appendix, `docs/appendix-backup-original.md` (bold "already unusable / no supported way back / capture-only" disclaimer, per the install-doc strategy). SETUP-GUIDE parts list + the White Walls appendix updated in lockstep; the White Walls appendix's "Android-x86" line corrected to Waydroid-on-Ubuntu.
- **Bench-process note:** SSH from the Mac to the live unit could not be established (Mac is multi-homed Ethernet + Wi-Fi, and Mac→unit stayed unreachable even Wi-Fi-only, a deeper Mac-side routing/policy issue; the unit could reach the Mac fine). Worked around by hand-typing commands at the frame and reading the screen via phone photos. SSH is a build-time convenience only and never appears in the next-owner flow. (Matt, 2026-06-13.)

### 2026-06-13: Hardware bench identification (Phase 2 kickoff)
First hands-on session with the actual XXL unit (original OS reachable). Identity **confirmed**; several prior assumptions **corrected**:
- **CPU = Intel Celeron N5105** (Jasper Lake / "JasperLake ULX"), 2.00 GHz, **§2 corrected from the wrong "N5100."** BIOS board name is **`XXL`** (IO custom badge; same N5105 platform as the MeLE Quieter 3Q label).
- **Original OS is Ubuntu + Waydroid, not "Android-x86."** Host = **Ubuntu Linux**; "White Walls" runs as an **Android app inside Waydroid** (a **LineageOS Android 11** container, `lineage_waydroid_x86_64`, `RQ3A.211001.001`). The Android-looking UI is the container. **§2, §4, §18 corrected.** (§18's notification-shade / Settings→Apps reset steps still apply, that's the container's UI.)
- **Firmware:** AMI Aptio `ML_JPL1 V1.0.0 x64` (core 2.22.1282, 12/2023). **`Del`** enters Setup (Quiet Boot hides the prompt, tap from the black screen). **No BIOS password** (Administrator access). **Secure Boot off.** **UEFI.** Boot entry `ubuntu (eMMC A3V012)`. → the **UEFI USB installer** path is clear, no signing/Secure-Boot hurdle.
- **Storage = ~256 GB eMMC** (id `A3V012`), **§2 corrected from "128 GB."**
- **Auto-Power-On: no BIOS toggle exists** (checked `PCH-IO Configuration`; no "State After G3" / "Restore AC Power Loss"). Auto-on is the firmware default, matches the confirmed replug-boot (§10). The §19 "Auto-Power-On label" item resolves to *there isn't one*. **§4, §10, §19 updated.**
- **Access kit / BOM confirmed:** UGreen 4-port USB 3.0 hub (**25946**); CableCreation **left-angle** USB 3.0 extension (**CC0516**), the **left** angle clears the controller's power socket beside the USB port; Logitech **K400 Plus**. Insert trick: slide the MeLE up to free the HDMI/USB-C, seat the extender, reseat, slide back down. **§4, §16, §19 + Setup Guide updated** ("right-angle" → "left-angle").
- **Reference:** factory stack boots slowly (~1 min to art) → our native Node + Chromium-kiosk should beat it easily; Ubuntu already drives the N5105 iGPU + square panel + Wi-Fi, so **Debian** is low-risk (§3).
- **Still TBD:** Wi-Fi module/driver under Linux (FCC TX ID `PD99560D2`), RAM, exact eMMC Linux path/free space, all gettable once our OS boots.

### 2026-06-13: Shut down / Restart countdown-cancel; power-cycle boots confirmed
- **Power actions now arm a countdown the owner can cancel** (Matt): **Shut down** counts down
  **10s**, **Restart** **5s**, shown inline in the Power card with a **Cancel** button (replacing the
  native confirm dialogs). At zero the action fires; Cancel aborts and re-enables the buttons, a
  safety net against a misclick on a hard-to-undo action.
- **Confirmed (Matt): the unit boots when power is unplugged and restored**, so cycling the outlet
  is a reliable boot, the hard-to-reach physical power button is a non-issue, and after a Shut down
  you power back on by replugging (or a smart plug). Recorded in §10; the exact Auto-Power-On
  default/label is still bench-TBD (§19). (Matt, 2026-06-13.)

### 2026-06-13: Terminology nailed down; control-panel copy pass; onboarding reference
- **Canonical vocabulary (user-facing):** **frame** = the physical device; **control panel** = the web
  UI you control it from; **screen** = the display surface; **display** = the `/display` kiosk page only.
  Swept the control panel + Setup Guide to match, e.g. header **"Blank panel" → "Blank screen"**, restart/
  update status copy now says "frame" (the device) not "panel," and "Reach this panel" → "Reach the control
  panel." (Engineering prose in this doc still says "panel" for the physical LCD where that's the precise
  hardware term, the scheme governs user-facing copy.)
- **Power-card copy tightened** (Matt): Restart note → **"Restarts the frame."** (dropped "and brings it
  right back", restart already implies the return); Shut down note → **"Turns off the frame."** Button
  stays **"Shut down"** (two words, it's the verb, matching macOS/Windows).
- **Wi-Fi card:** removed "no keyboard needed" (don't tell users what they don't need); "frame" throughout.
- **Onboarding reference recorded (§11):** Matt shared the stock Infinite Objects Wi-Fi flow; logged it
  plus our refinements (QR codes on the setup screen, plain labels, pick-from-scanned-list, two-screen
  confirmation, recovery/self-heal, AP naming) as the Phase-2 design. Build none now. (Matt, 2026-06-13.)

### 2026-06-13: Hardware stubs (Restart / Shut down / Wi-Fi); Phase 1 feature-complete
- **Settings → Power card:** **Restart** ships **live**, an app-level soft-restart through the
  supervisor (exit → relaunch, the §15 path), so it genuinely works browser-only with no hardware
  and is identical to what systemd will run on the device. **Shut down** is a visible-but-inert
  **stub** (can't/mustn't power off the dev Mac); Phase 2 wires a real OS power-off. Restart was
  made live rather than a stub (Matt's call) because the supervisor already made it durable and
  free, the old "inert stub" note predated the supervisor.
- **Settings → Wi-Fi card:** an explanatory **stub** for the first-run OpenObject-Setup AP +
  captive-page flow (§11; Phase 2), plus a **real** "reach this panel" helper showing the live LAN
  address(es) and the `openobject.local` name.
- **Backend:** `GET /api/system` (supervised flag, port, mDNS name, LAN addresses),
  `POST /api/system/restart` (live under the supervisor; reports `needsManualRestart` under
  start:direct), `POST /api/system/shutdown` (inert stub + message). `/healthz` gained a per-process
  **`boot`** id so the panel can confirm a plain restart, unchanged version, actually bounced. All
  owner-initiated, none in the playback path.
- **Phase-2 power note recorded:** a full **device reboot** (`systemctl reboot`) and real
  **power-off** (`systemctl poweroff`) are easy on the Linux device; with BIOS Auto-Power-On a
  power-off returns on next power, so a true "off" is the outlet/smart plug (§10).
- **Phase 1 is now feature-complete on macOS**, server, library/upload, control panel, display +
  behaviors, progressive sync, sleep hours, self-update, and these stubs. Remaining work is
  hardware/Phase 2. (Matt, 2026-06-13.)

### 2026-06-13: Self-update UI redesign + control-panel layout
- **Software Update card decluttered to the traditional shape** (Matt's review): a clear
  **Current version** line, `version · date · commit` (e.g. `0.1.0 · June 13, 2026 · a1b2c3d`,
  the commit a small unlabeled link to GitHub), then **Check for updates**, and on an available
  update a plain-English recap of what's in it, a **What's new ↗** link to the GitHub diff, and
  **Update & Restart** / **Not now**. The version number alone doesn't change every update, so the
  **build date** is the human "it updated" signal and the short **commit** the precise one (covers
  two-updates-in-a-day); the jargon (`commit`, `tracking main`, raw hashes in the body) is gone.
  New backend fields feed it: commit `date` (`git %cs`) and `repoUrl`/`compareUrl` derived from the
  origin remote (suppressed for non-web remotes).
- **Update channel toggle removed from the UI**, track the latest on `main` only. The channel
  machinery stays in the backend (`update_channel`, `PUT /api/update/channel`), so a plain "vetted
  releases" choice can return if/when we publish tagged releases (pairs with going public).
  "Stable releases" was dropped from the UI because we have none yet, it would only read "no
  releases yet."
- **Section headers Title-Cased** (Sleep Hours, Software Update) via a shared `.section-title`.
- **Control-panel layout: tabs moved to the top; the upload dropzone now lives inside the Library
  tab** (no longer pinned above all tabs). Uploading is a Library action, so seeing it from
  Rotation/Settings was odd. **This reverses the earlier "upload pinned above the tabs" call** (see
  the Rotation-curation entry below). A "drop a file anywhere → Library" convenience was considered
  and declined, keep it simple. (Matt, 2026-06-13.)

### 2026-06-13: Self-update from GitHub built (§15)
- **Self-update shipped, browser-driven end to end (§15).** The **Settings** tab gains a
  **Software update** card: it shows the running version + commit + tracked branch, a **Check
  for updates** button (git fetch + compare to the channel's target), and, when a newer,
  fast-forwardable version exists, an **Update & restart** button that pulls
  (`git merge --ff-only`), reinstalls dependencies **only if the manifest changed**, and
  relaunches. `/healthz` now also reports the running **commit**, which the panel polls to
  confirm the new version came back up. Everything happens over the browser, **no hardware
  access**, which is the whole point of building it now.
- **Restart mechanism = a tiny supervisor (`player/supervisor.js`).** `npm start` now runs the
  supervisor; it spawns `node server.js` and relaunches it when the player exits with restart
  code **75** after an update. `npm run start:direct` runs the server alone (no auto-relaunch,
  self-update then asks for a manual restart). This is the Phase-1 stand-in for the device's
  service manager; **Phase 2 swaps it for a systemd unit** (`Restart=always`), the player side
  unchanged. The launch config now points at the supervisor.
- **Guardrails (all verified):** **fast-forward only**, a diverged checkout (local commits)
  refuses with a clear message and changes nothing, never a force-reset; **runtime data
  untouched**, `player/data/` + uploads are gitignored, invisible to the pull; **offline-safe**
, a failed fetch reports gracefully and never touches playback; **owner-initiated only**,
  never automatic, never in the playback path. Art still never touches the repo (§8).
- **Channel setting** (`update_channel`): track **`main`** (default) or **tagged releases**
  only (§12). New routes: `GET /api/update` (instant, no-network status), `POST /api/update/check`,
  `POST /api/update/apply`, `PUT /api/update/channel`.
- **Tested end-to-end on macOS without real upstream commits** via a throwaway bare-clone
  "fake origin" sandbox (snapshot the working tree → bare repo → work checkout → push synthetic
  commits): proved no-update → update → fast-forward + restart (`/healthz` commit flips) →
  divergence refusal, plus the offline and not-a-git-checkout paths. The sandbox is disposable
  and never touches the real repo or GitHub. (Matt, 2026-06-13.)

### 2026-06-13: Sleep hours built (+ manual Blank)
- **Sleep hours shipped (§13):** up to **two daily blank windows**, each with an **enable
  checkbox** (work + night); times on a **12-hour clock with an AM/PM** segmented toggle,
  wrap-past-midnight supported with an auto **"overnight"** tag. Off by default.
- **Manual "Blank panel"** toggle added to the header, art off on demand, independent of the
  schedule (the parked Blank/Pause companion; it lives in the header we said we'd revisit).
- **Sleep screen** mirrors the boot/idle mark exactly, same size/placement, **dimmed, no
  caption**, with a slow **pixel-shift** (~90 s) for burn-in insurance. Pixel-shift was chosen
  over a periodic fade (the standard technique, sufficient on its own; Matt's call). Playback
  stops while asleep and resumes on wake.
- **Server-computed `asleep`** (from settings `sleep_ranges` + `manual_blank`) is returned in
  `/api/display`; `PUT /api/settings` gained validated `sleepRanges` + `manualBlank`. No DB
  change, both are key/value settings. Phase 1 blanks in software; backlight dimming is a
  Phase 2 hook. (Matt, 2026-06-13.)
- **Control panel reorganized to three tabs, Library · Rotation · Settings.** Sleep hours
  moved into the new **Settings** tab (its future home for self-update, restart/shutdown,
  Wi-Fi onboarding); the **duration/order** bar moved into the **Rotation** tab (it governs how
  the rotation cycles). The top of the page is now just upload + tabs. Sleep mark dimmed from
  0.3 to **~0.2**. (Matt, 2026-06-13.)

### 2026-06-13: Logged: Web/HTML art pieces (future seam, §17)
- **Logged a future enhancement, a "HTML" content type** (working name): add a **live web
  page** (generative/interactive `index.html`, e.g. Arweave-hosted) as a piece via
  **paste-URL + optional name + special functions**, handled through a **curated by-name
  registry for featured collections** (first target: Bryan Brinkman's *"Azulejo Galo"* and
  its in-page "Toggle Rotation" control). It's a new render kind (`web`, iframe/webview) and a
  §8 source-layer seam, distinct from the on-chain/NFT seam (which resolves to a media file).
  **Not built**; captured in §17 with its constraints (Fit/Fill semantics, §9 local-first
  tension, cross-origin control → likely Phase-2 kiosk, sandboxing). (Matt, 2026-06-13.)

### 2026-06-13: Rotation curation (membership + manual order); settings bar redesign
- **The Rotation is now a curated subset, not the whole Library**, completes the §7
  Library/Rotation/Pin model. Two new `library` columns, `in_rotation` (default 1) and
  `position`, added by an **idempotent PRAGMA-guarded migration** that backfills existing
  rows to in-rotation in upload order, so prior behavior is unchanged. New uploads
  **auto-join** at the end of the order, keeps the zero-effort "everything I upload plays"
  default; unwanted pieces are removed in the Rotation tab. (Matt, 2026-06-13.)
- **API:** `PATCH /api/library/:id` now also accepts `inRotation`; added `GET /api/rotation`
  (curated members in order, *not* pin-collapsed, that's display-only) and
  `PUT /api/rotation/order { order:[id,…] }` (renumbers members 0..n-1 in one transaction).
- **Pin now overrides Rotation membership** (§7 "overriding the cycle"): `/api/display`
  resolves the pinned piece from the **full Library** and collapses to it even if it isn't
  in the Rotation, fixes the case where a pinned non-member would vanish once
  Rotation ≠ Library. `display.js` is unchanged (its own collapse became a harmless no-op).
- **Control panel reorganized into two tabs, Library and Rotation** (Matt's "separate page
  you toggle to, then back" call, over reordering inside the Library grid). Library tab:
  each card gains a corner **add/remove-from-rotation** toggle. Rotation tab: the ordered
  list, reorderable by **drag *and* ↑/↓ arrows** (arrows are the touch-safe path on
  iPhone/iPad, where native drag is unreliable), **✕** to remove; a hint notes the order
  drives Sequence and is cosmetic under Shuffle. Rotation-list thumbnails **honor each
  clip's Fit/Fill** so they match the Library cards and the panel. Control icons (grip,
  arrows, remove) are **inline SVG, no webfont**, so the panel works on an offline frame.
  Upload + settings stay persistent above the tabs.
- **Settings bar redesigned (Option A of three mockups):** a −/+ **stepper** on the
  duration, **segmented** unit (sec/min/hr) and **segmented** order (Sequence/Shuffle)
  replacing the dropdowns. (Matt picked Option A, 2026-06-13.)
- **Sleep-screen direction captured for §13** (design intent; Sleep Hours not built yet).

### 2026-06-12: Display rotation engine; global equal-time duration
- **Duration is one global, equal-time setting for every piece**, no per-clip duration
  (revises §7/§12). Stills hold for it; animated + video loop to fill it; a clip longer
  than the duration is cut when the timer advances; video always loops. Dropped the
  per-clip `duration_ms`/`video_full` columns from the `library` table. (Matt, 2026-06-12.)
- **/display rotation engine built** (§6, §7, §9): renders the Rotation (v1 = the whole
  Library, upload order) edge-to-edge with per-clip Fit/Fill, crossfades between pieces,
  Sequence/Shuffle, always muted. New uploads/deletes, Fit/Fill flips, and
  duration/order changes fold in live via a ~5s poll, no loop restart (§9 progressive).
- **Control panel** gains a global Settings bar (duration + order) and a per-clip
  Fit/Fill toggle. New endpoints: `GET/PUT /api/settings`, `GET /api/display`,
  `PATCH /api/library/:id {fit}`.
- **Pin** (hold one piece permanently, collapses the rotation to it, resumes on unpin)
  and **duration units** (seconds / minutes / hours) included. New endpoints
  `PUT /api/pin/:id` + `DELETE /api/pin`; deleting a pinned piece clears the pin.
- **Rotation default = the whole Library**; new uploads auto-join. Curation
  (remove-from-rotation, reorder) comes next. (Matt, 2026-06-12.)
- **Web-app favicon + apple-touch-icon** added (white-bg logo at 16/32/180 px), the
  apple-touch-icon gives a proper home-screen icon for phone/iPad control.
- **Random rotation order removed**, Shuffle already gives random order with no
  near-term repeats; pure Random (independent pick, can repeat back-to-back) was an odd
  fit for an art frame. Modes are now **Sequence / Shuffle** only. (Matt, 2026-06-12.)

### 2026-06-12: Upload + Library shipped; node:sqlite locked
- **SQLite library chosen: Node's built-in `node:sqlite`** (over better-sqlite3), zero
  native deps, no build step, best for revivability; all DB access is contained in
  `player/src/db.js`, so a later swap stays local. Its lone startup ExperimentalWarning is
  silenced surgically (only that one line; every other Node warning still prints). (Matt's
  call, 2026-06-12.)
- **Web upload + Library built** (§7, §8): drag/tap multi-upload via `multer` (pure-JS,
  no native build), stored **byte-for-byte** under `player/uploads/`; a format gate by
  extension accepts JPEG/PNG/GIF/AVIF/WebP/MP4/MOV/WebM and **skips the rest silently** (§6).
  Adds a `library` table + `GET/POST/DELETE /api/library` (delete removes the row *and*
  the file).
- **WebM added to the v1 video formats** (after MOV): open, royalty-free VP8/VP9/AV1,
  always compiled into Chromium, so it's the most reliably-playable video on a minimal
  Linux frame (H.264/MP4 may need a codec package there). Same `kind: video` loop path,
  so trivial to add. List order also set to AVIF before WebP. (Matt, 2026-06-12.)
- **`/` is now the control panel** (was a redirect to `/display`); the kiosk stage stays
  at `/display`. The display still renders only the Rotation, uploads fill the Library;
  the rotation engine that puts them on the panel is the next checkpoint.

### 2026-06-12: Self-update from GitHub (Phase 1)
- **OpenObject updates itself from its GitHub repo via the control panel** (Check for
  updates → Update & restart). The player runs as a git checkout; update =
  `git pull --ff-only` + dependency reinstall + relaunch. Fast-forward only (refuses on
  local divergence); runtime data (`player/data/`, uploads) is gitignored and untouched;
  owner-initiated, never automatic, never in the playback path. Channel setting: track
  `main` (default) or tagged releases. (§15, §12)
- **Placed in Phase 1, not Phase 2:** the check / compare / pull / relaunch runs entirely
  on the dev Mac and is built + tested there now; only the production restart shim
  (systemd on the device) is Phase 2. It lands after the core control panel, since the
  update UI lives there. (Requested by Matt 2026-06-12.)

### 2026-06-12: Bench caution: screws above the MeLE
- **Do not remove the two screws on the bracket above the MeLE** when working at the
  back of the XXL, they retain captive nuts inside the chassis that fall in if the
  screws are backed out, forcing a near-full teardown to recover. Documented in §4 and
  the Setup Guide. (Bench lesson from Matt.)

### 2026-06-11: Branding asset approach
- **Idle/boot-screen logo variants will be produced with Potrace → SVG**
  (`openobject-logo.svg`): single-color and transparent so CSS recolors it
  white-on-dark or black-on-light; transparent PNG exports derived from it as needed.
  Trace source: the **2k master** (`Logo/logo-2k.png`), falling back to `logo_orig.png`
  if the higher-res export proves a soft upscale rather than added detail, the 2k/4k
  files are near-uncompressed, so verify edge crispness at trace time. The committed
  PNG marks (≤1024) stay sourced from the clean `logo_orig.png`.
  A plain raster color-inverse was rejected (trivial, Matt does that in Photoshop if
  ever needed). **SVG here is a UI/brand asset only**; displayed user *art* in SVG
  stays deferred per §6. (§14)
- **Logo source re-centered** by Matt (subtle but critical glyph centering fix); the
  committed marks were refreshed from the new source (commit `df57ec6`).

### 2026-06-11: Phase 0 kickoff
- **Content model confirmed:** Library + select, persistent library, curated rotation, pin one clip. Not replace-on-upload. (§7)
- **Stack chosen:** Node.js + Express + vanilla HTML/CSS/JS + SQLite, no build step, one language across server and the browser display page, chosen for revivability. (§5)
- **Display is edge-to-edge with zero chrome:** full-panel black stage, no decorative frame/border/padding, media to the physical edges. (§6)
- **Default render mode is Fit** (original aspect ratio); per-clip Fill override retained. An earlier "Fill by default" idea was rejected. (§6, §12)
- **Rotation order gains Random** alongside Sequence and Shuffle, three modes, defined in §7.
- **Repo is private for now,** intended to go public later; the next-owner mission still drives design. (§15)
- **Logo supplied** by Matt; optimized marks committed under `assets/branding/`, large masters kept out of git. (§14, §19)
- **Workflow:** Claude drives all git/gh; commits + pushes happen at checkpoints on Matt's approval; outward-facing actions gated. No hardware access yet, Linux/kiosk/installer is Phase 2.
